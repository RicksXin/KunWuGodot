# Map01 阵灯（Approved）

用户于2026-09-24确认修订后的残破/完整修复激活双状态动画。
一张2048×1536 RGBA图集；256×384帧，8列4行，8FPS。
第一行broken、第二行active各8帧循环；第三/四行repair共16帧，2秒单次修复，再进入active。共同锚点(128,350)，主体宽224。
用户反馈切换太快后增加由下到上的光效修复过渡；加载已修复存档不重播，同状态刷新不重置。
只按命名动画播放，三盏灯各自独立；LAMP_REPAIRED对应active。

生成母图及本地确定性循环来源见 `art/candidates/map01-formation-lamp/`。
构建工具 `tools/build_map01_lamp_sequence.py`；已确认图片由内置imagegen生成，灯芯/符纹循环由本地组帧。
当前由地形编辑器及正式高清地图共用；正式地图读取Game状态并响应真实修灯选项，地形编辑器仅本地预览。作者地形尚未晋升正式地图。
