# Map01 探索地图状态包

## 唯一目标

把正式野外探索页面改为 `817×375` 横屏布局，地图仍是大于一屏、可拖动缩放的 Map01；营地和战斗继续使用 `375×817` 竖屏。旧 `landscape_map_preview` 与本任务无关。

## 当前事实

- 正式工作树：`/Users/zhangxiaoen/Desktop/Game/KunWuGodot`，当前分支：`master`。
- 原候选工作树 `/Users/zhangxiaoen/.codex/worktrees/b9f7/KunWuGodot` 保留为历史来源，不再作为启动入口。
- 正式契约不变：`28×64`、逻辑格 `48×48`、背景 `1344×3072`；玩法事实只来自 `data/maps/map_01_formal.json`，背景只负责视觉。
- 本轮只改地图页面方向、HUD、弹层和窗口切换；不改正式背景、地图尺寸、坐标、碰撞或对象。
- 候选根目录：`art/candidates/map01_modular_background/`；该目录被 Git 忽略，只保留在专用工作树，不进入运行时或提交历史。
- 旧 Codex“横版地图”任务因历史请求体过大返回 413；横屏实现已迁入正式 Desktop 路径。后续只读取本状态包、必要事实源和目标文件，不携带图片、完整日志或旧对话。

## 已完成

- 已用 `df8d852` 撤销误将候选背景晋升为正式地图的提交；正式背景与 manifest 已恢复，并已从候选工作树迁入 Desktop `master`。
- `map_scene.gd` 进入时切换 `817×375`，桌面窗口同步横置并居中；移动端请求横屏方向。
- 顶部状态/目标/快捷操作、中部全宽地图、底部移动/缩放栏，以及休整、背包、归营、事件弹层均已重排。
- 离开地图进入营地或战斗时恢复之前的 `375×817` 画布、窗口尺寸和移动端方向。
- 修复营地“选择地图”点击无响应：地图选择面板设为纯视觉层，避免吞掉列表行 Button；出发校验失败时先关闭选择层再显示 HUD 提示，避免错误被弹层遮住。
- 补齐 `Game.settle_stamina()`：按配置每 300 秒恢复 1 点，保留周期余数；仅在营地结算，出征/旧存档中的野外时段不会计入；出发和归营会重置恢复计时锚点。
- Debug 构建的营地“设置与存档”新增“打开调试面板”：可补满灵粮（含当前探索携带池）和恢复全队灵息至上限；Release 构建不显示入口。
- 地图进入后新增下一帧的设备/窗口横向强制：再次调用 `DisplayServer.screen_set_orientation(SCREEN_LANDSCAPE)`，并在独立桌面窗口被启动器改回竖向时旋转最终窗口几何；地图内容与整窗方向保持一致。
- 已确认用户当前看到的是 Godot 编辑器内嵌游戏视图（外层 `749×1633` 竖屏容器），不是独立游戏窗口；新增 `run_kunwu_game.command` 并在 README 说明内嵌容器不会跟随运行时旋转。

## 验证

- Godot `4.7.1` 解析和导入通过。
- 横屏场景验证通过：画布 `817×375`、地图滚动容器 `817×327`、主体拖动区 `817×279`、四类弹层不越界、`50%` 缩放居中、离开恢复 `375×817`。
- Map01 正式契约、正式战斗绑定、项目数据和主场景短运行通过。
- 灵息边界测试通过：营地经过 600 秒从 90 恢复到 92；活动探索中保持 90。
- Debug 操作测试通过：灵粮从 1 补至当前容量 200，全队灵息从 1 恢复至 100；Godot headless 导入无脚本错误。
- 横屏窗口 headless 检查通过：地图 `content_scale_size=(817,375)`、viewport `(817,375)`；正式 Map01 与战斗校验仍通过。

## 剩余

- 从 Desktop 项目路径启动：`/Users/zhangxiaoen/Desktop/Game/KunWuGodot/project.godot`，进入 Map01 后人工确认实际桌面窗口、文字密度、拖动、缩放和所有弹层观感；确认“传送 → 破禁山麓”可点击，若已有未结束探索应能看到明确提示。
