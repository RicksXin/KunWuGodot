# 上传两张图后复制以下全文

每项只生成一张候选；原生尺寸是后续接入目标，生成器不必精确遵循。请下载原始透明PNG，不要截图。保持像素比例，不要自行缩放或裁切。

```text
为中国修仙像素游戏扩展已确认的台地素材。图1是唯一造型与材质基准，图2只展示已确认的台阶高度和实机像素比例，不复制截图UI、文字、角色或落脚处旧泥土地块。
本次只输出以下一个完整接口模块：
完整一体的长直台地边，崖沿从画面左下向右上延伸，坡度2:1，目标原生投影横向长度384、高差48。两端保留至少64投影像素可用于硬接缝的连续内容，末端不要增加拐角或端头立柱。光照仍左上方，不能简单镜像另一个朝向的受光。
必须把浅褐土面、稀疏裂纹与碎石、起伏不规则崖沿、横向连续岩层、墙脚碎石作为同一张图一体绘制。顶面向内至少保留64原生像素供拼接；不能只画岩壁纹理或一个规则几何盒子。保持参考的岩层厚度、岩石大小、颜色和左上受光，2:1斜俯视正交、无透视汇聚。原生高差48；可按整体整数倍率画大，顶面和墙面倍率一致，不单独拉高墙。两端不能裁断主体，不画悬空台阶，不画岛屿底座。
透明RGBA背景，仅本模块，无底色、棋盘格、天空、山景、建筑、草丛、角色、文字、网格、边框、箭头、光晕。不输出多宫格或多个模块。轮廓清楚，保留原图像素簇和不规则岩层，禁止把表面变成细密重复噪点、砌砖墙或平滑3D材质。
```

返回后检查：自然边缘是否保留；两端能否在不缩放的情况下接上原图；顶部与墙脚是否完整；光照和岩层是否一致。未检查前不需要生成第二张或反复修改。
