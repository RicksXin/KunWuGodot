# 已确认完整台地：扩展接口包

当前实机默认回到已确认完整L形台地与台阶。旧全营地纹理铺贴标记未通过，禁止再作为最终效果。

此包是下一步外部生成输入，尚未生成、尚未接入。需要5个候选模块：两个方向的带顶面长直边、正面凸角、左右端凸角。每个文件夹两张输入图与一条完整提示词，独立生成一张即可，不用逐个确认。

先做01长直边可以先验证拼接路线，再继续其余4项；也可整批交回。原图的凹角保留复用，本次全营地拓扑没有凹角，不重复生成。现有台阶继续复用。

核对结果：69段全部边界、45段可见边界、4处台阶开口、12个分层凸角。边界数不是生成张数；同向直段与同类角应复用。

原图48高差不能直接覆盖展示外边的22/70/118高差。当前不增加第六张高度图：先检查同一岩层能否局部层叠并保留墙脚，无法通过视觉检查时单独提出缺口，不能把整图纵向拉伸。

返回后本地工作：原始像素尺度匹配 → 端部硬接缝与自然角拼接预览 → 带顶面完整接口接入边界规划器 → 台阶开口遮罩 → 全营地实机验收。未验收素材不进入Approved。不能保证生成一次即满足接口。
