# 正式横版营地接入（完成）

- 最新范围：已确认的 Demo 营地视觉用于正式横版营地；原 UI 保持，玩家只能查看放大的营地局部，不能缩小到全貌。
- 正式入口 scenes/camp.tscn → scripts/scenes/camp_landscape.gd，继承 camp.gd 原业务。直接复用原头像、资源、任务和底部五图标，仅适配横版位置和等比显示；撤销新增条形 HUD、文字导航和全景按钮。
- 1280×720；默认缩放 1.1，限定 1.0～1.35，左右上边沿用参考边界；下沿改为传送阵可见像素底部以下 15 个逻辑屏幕像素，随缩放计算。中心范围由视口半尺寸/缩放动态计算。编辑器不受玩家视角限制。
- 世界复用 scripts/prototypes/camp_tile_rebuild.gd 与 data/prototypes/camp_tile_rebuild.json；正式营地和编辑器共享唯一布局。保留建筑动画、传送阵、三位 NPC、溪流瀑布、流雾、灰青阶梯，隐藏调试控件。
- 旧业务面板维持 375×817 坐标，0.84 倍居中；全屏遮罩阻止底图操作。业务规则、存档流程保持；招贤馆仍提示未开放。
- 验证：tools/validate_formal_camp.gd 通过八建筑像素点击、原 HUD 固定、拖动点击区分、缩放上下限和拖动边界、居中面板、模态拦截、实验返回、出征与归营横屏恢复。git diff --check 通过。使用 --no-profile-write --ignore-config-cache。
- 一次验证遇到磁盘存档字节变化；确认编辑器另有正常游戏进程运行。本验证进程全程禁止存档写入，最终重跑通过且无字节变化警告；不干预用户游戏进程或回写旧存档。
- 最新截图 formal-camp.png 为恢复原 UI 后的局部视角；garden-panel.png / treasury-panel.png 属于上一版 HUD，不能作为当前全画面验收。
- 已同步 README、MIGRATION、PRD、总案、AGENTS。无新素材生成、无积分支出；无关用户改动保留。
- 复核入口：Godot F5 或直接运行 scenes/camp.tscn；检查原 UI、默认镜头距离和缩放手感。技术验证完成，视觉细节由用户确认。
- 2026-09-24 边界修正：原下沿 y=930 已废弃，改为传送阵底部 + 15px；左沿 x=-720、右沿 x=1030；上沿延续原默认缩放的上限。最小/默认/最大缩放的四边越界断言及原业务回归通过，图形截图 boundary-bottom.png、boundary-left.png、boundary-right.png。

- 玩家：库中 1787918845829-frames64-oldfix2.png 棕发青衣角色，正式营地独立 Sprite2D，复用现有 actor/AStar 路线，四向行走/待机；隐藏调试菱形。点击空地行走、点击建筑沿用原业务，镜头平滑跟随并受边界限制；手动拖动暂停跟随，再次点地移动恢复。模态及隐藏营地暂停玩家移动。
- 验证新增：三个缩放档传送阵底部距离屏底恰为 15px，玩家沿路线前进、镜头移动、行走转待机均通过；八建筑及出征归营回归通过。截图 player-follow.png。

- WASD/E：正式控制器新增屏幕方向 WASD 连续移动，110 世界像素/秒，斜向归一化、小步碰撞，仅允许当前可行走菱形及相邻连通区域/阶梯。按键接管时取消点击路线并恢复跟随。E 选择最近可达目标（88 像素内，图路线限 155），建筑复用原面板，三位 NPC 使用简短寒暄，不引入任务奖励。提示在玩家上方，模态或隐藏状态屏蔽操作。
- tools/validate_camp_controls.gd 通过：移动速度、非法区域、所有导航边双向行走（含阶梯）、E 建筑/NPC、距离范围、模态拦截；正式营地回归通过。图形验收截图 wasd-interaction.png。测试进程禁止写存档；另有用户正常游戏进程可能并行更新磁盘存档。
- 视觉复核发现历史 target.node 与建筑美术偏移不一致；E 建筑范围现以当前 sprite.position 门槛位置及最近可达节点计算，避免远离实际门口出现提示。修正后控制测试通过。

- 平台误挡修复：旧 WASD 仅查四邻格，屏幕水平/垂直行走会跨菱形对角；且以欧氏中心距离更新所属格会滞留旧格。现按实际包含菱形更新 current，四周均连通且同平台时允许对角跨格，阻挡角落仍禁止切角。396 个实际平台对角案例、全部阶梯/导航边及 E 交互测试通过。
- 已定位旧交易行占格压住 [8,2] 阶梯；废弃以历史 origin/footprint_size 直接阻挡的路径，改为跟随当前可见门槛的显式碰撞多边形。

- 本轮阶梯/建筑：共享 scripts/prototypes/camp_collision.gd 读取布局 collision_polygon、collision_enabled、collision_reference_width、collision_scale，平移/旋转/缩放跟随建筑；运行时、NPC 导航和编辑器验证共用。导航节点和边都避开建筑，WASD 用脚底线段与多边形检测。遮挡沿用脚底 Y 排序，occlusion_enabled 可关闭。
- 阶梯改为完整宽度的连接四边形（两侧各 Vector2(±32,16)，两端延伸12），只允许对应连通节点进入；高差仍需阶梯。旧占格删除后四段阶梯均存在。
- 编辑器新增碰撞/遮挡开关、宽度和深度倍率，选中建筑显示真实碰撞轮廓；修改可撤销。JSON 为唯一配置，无额外资源生成。碰撞初始为门槛后方地基菱形，可按视觉细化。
- 验证：404 个平地对角、全部导航边、四段阶梯24次双向侧缘通行、建筑内部阻挡、E交互、正式营地八入口与归营通过；Godot headless 导入无错误。截图 collision-editor.png；用户复核入口为营地编辑器选建筑看橙色轮廓，以及正式营地 WASD 沿阶梯两侧上下、绕建筑前后。

- 用户确认营地达到相对满意结果；制作复盘已整理至 Docs/Tech/营地制作复盘与野外地图参考.md，README 提供入口。该文为后续野外制作参考，不改变正式 Map01 或现有产品规范。本次仅写文档，不改运行代码。
