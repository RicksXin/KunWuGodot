# Demo 美术 04：入山整备、编辑队伍与地图选择

版本：0.1\
日期：2026-08-01\
状态：0.1 Demo D0/D1 美术制作资料\
上级索引：[0.1 Demo 美术制作资料](README.md)


本节集中记录 0.1 Demo 入山整备、编辑队伍和地图选择的裁剪素材、灰盒输入与制作记录。
Demo 范围读取 [Demo 范围与体验闭环](../01_Demo范围与体验闭环.md)；通用像素与许可要求读取
[美术设计规范](../../02_美术设计规范.md)。正式 1.0 页面读取
[正式整备素材分册](../../ArtAssets/04_入山整备编辑队伍与地图选择.md)。

## 6.1 本批制作范围

本批入山专属正式视觉共需新增 9 张 PNG，直接复用营地大厅与灵源院已经生成的
7 张 PNG；另需使用 [灵源院分册 §5.4.2](03_灵源院生产弹窗.md#542-全建筑共用列表项普通态与选中态完整底板) 规划的 2 张全局通用列表项素材，以及
[灵源院分册 §5.4.4](03_灵源院生产弹窗.md#544-通用行内按钮与通用底部按钮) 规划的 2 张
全局通用按钮，并保留 4 张非阻塞的可选优化素材。现有面板、行底板、头像框和资源
图标继续作为风格输入；列表背景与按钮统一迁移到全局组件，不再生成“入山专用”
基础行底板或按钮。

不制作完整页面截图后切图，不制作单独的修士列表头像，也不把任何文字或数值画进图片。

| 批次 | 类型     |  数量 | 内容                            |
| -- | ------ | --: | ----------------------------- |
| 首批 | 修士卡    | 6 张 | 修士卡框、空位剪影、四名初始修士静态立绘          |
| 首批 | 功能图标   | 3 张 | 开山镐、探灵镜、通用锁                   |
| 复用 | 已生成 UI | 7 张 | 灵源院面板、资源行、加减按钮、灵粮、头像框、灵石       |
| 共享新增 | 通用列表项 | 2 张 | 普通态、同尺寸选中态完整底板                  |
| 共享新增 | 全局通用按钮 | 2 张 | 通用行内按钮、通用底部按钮                  |
| 可选 | 视觉强化   | 4 张 | 专用面板、灵息条两张、负重图标               |

1.0 固定四人正常整备不显示空槽，也不允许补入第五名修士。已经生成的空位剪影只保留给历史灰盒引用，
不计入 1.0 正式页面必用资产；正式卡面只使用修士卡框和四名固定修士立绘。

截至 2026-08-01，首批 9 张原始图均已收到，并已规范化为 §6.5 规定的运行时尺寸，存放于
`res://assets/camp/ui/expedition/`。入山整备 Presenter 已按稳定资源路径接入四张立绘、
修士卡框、空位剪影、开山镐、探灵镜和通用锁；当前状态为“待 Godot 编辑器首次导入生成
`.import`、待实机视觉验收”，不得在完成编辑器导入前误记为已正式验收。

截至 2026-08-10，石岩新增一套 `4 列×2 行`、8 帧自然呼吸 Sprite Sheet。源图实际为
`615×462 px (@3x)`；运行时按 `153×231 px` 切帧并忽略最右侧多出的 3px，以 `4 FPS`
循环播放。它只替换入山整备主面板中的石岩卡面，编辑队伍列表仍使用静态立绘；图集未导入
或加载失败时同样回退到静态立绘。运行时文件为
`res://assets/camp/ui/expedition/animations/shi_yan/shi_yan_idle_sheet.png`。

截至 2026-08-10，陆清新增 `4 列×2 行`、8 帧 Idle 与 Attack Sprite Sheet，源图均为
`1448×1086 px`，运行时按 `362×543 px` 切帧。Idle 以 `4 FPS` 接进入山整备主卡，加载
失败时回退静态立绘；Attack 仅入库供后续战斗表现接线，当前不加载。运行时文件分别为
`animations/lu_qing/lu_qing_idle_sheet.png` 与 `animations/lu_qing/lu_qing_attack_sheet.png`。
Attack 源图含大面积低透明灰色残留，正式接入战斗前需由美术清理并重新验收透明通道。

[灵源院分册 §5.4.4](03_灵源院生产弹窗.md#544-通用行内按钮与通用底部按钮) 的通用行内按钮已生成；通用底部按钮现版因阶梯形外轮廓未通过视觉验收，待重做。重做完成前，入山页面可以保留现版或灰盒占位以验证交互，但不得将它视为最终视觉或用它继续派生新素材。

四名立绘同时服务两个位置：完整立绘作为 `79×205` 修士卡背景；选人列表在原
`62×76` 头像布局区内，在通用方形图像槽生成并验收前继续使用现有 `50×50` 大厅头像框，
验收后改为居中使用 [正式通用方形图像槽](../../ArtAssets/06_通用方形图像槽.md) 的 `48×48` 底框。框内
头像通过 Godot `clip_contents` 或遮罩材质/AtlasTexture 从同一张立绘裁切。除非实际裁切清晰度不合格，不得
另做四张头像，避免同一角色出现两套脸或服装细节漂移。

## 6.2 尺寸与导出总则

- UI 唯一逻辑基准和玩家可见窗口为 `375×817`。
- 每张新 UI 素材都必须同时标注逻辑尺寸和最终 PNG 实际尺寸；本批默认交付
  `@3x` RGBA PNG。
- `360×640` 不参与本节任何 UI 布局或 UI 素材尺寸推导；`1080×1920` 只表示迁移中的
  旧工程坐标，不能静默换算成新素材尺寸。
- 像素边缘必须落在整数坐标，使用硬边缘、无抗锯齿和 Nearest 过滤。若 GPT Image
  无法直接输出精确尺寸，先生成高清透明母版，再离线裁切、像素化并缩放到表中尺寸。
- 九宫格素材表中填写的是“九宫格源图逻辑尺寸”，运行时可拉伸到指定目标尺寸；边角、
  纹样和描边必须全部位于不可拉伸区。
- 修士名、职业、等级、灵息、战力、境界、灵根资质、队伍编号、地图名、地图消耗、数量、重量、
  锁定原因和所有按钮文案均由 Godot Label/本地化表渲染，不得烘焙进 PNG。
- 模态暗色遮罩、禁用态降亮度和按钮按下反馈优先由 Godot `modulate`/透明度过渡完成，不单独
  占用美术图片。修士卡信息叠在人物立绘上；Approved Figma 基准为 `58×48`、34% 淡黑、
  `2px` 图层模糊与 `1px` 背景模糊，当前深色动态立绘使用约 71% 中心暗衬与 `6px` 多层羽化
  作光学校正。运行时组合绘制，不新增卡片专用 PNG。

## 6.3 需要提供给 GPT Image 的参考图

当前原始参考图位于用户资料目录；进入团队制作流程前，应复制到非运行时目录
`art/source_archive/expedition_gpt_image_20260801/references/`，不得直接作为商业成品或打入游戏包。

| 建议归档文件名                                   | 当前原始文件                                                                    | 只参考什么                              |
| ----------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------- |
| `expedition_preparation_reference.jpg`    | `/Users/zhangxiaoen/Desktop/个人资料/referPng/传送阵面板.jpg`                      | 主面板从修士卡、操作栏、负重、三类物资到底部按钮的信息顺序和纵向密度 |
| `expedition_hero_selection_reference.jpg` | `/Users/zhangxiaoen/Desktop/个人资料/referPng/微信图片_20260801145431_50_209.jpg` | 可滚动修士行的信息分组、选择/取消按钮位置和已选槽位的识别关系    |
| `expedition_map_selection_reference.jpg`  | `/Users/zhangxiaoen/Desktop/个人资料/referPng/微信图片_20260801145430_49_209.jpg` | 地图列表的两行信息层级、行间距和锁定项与可点击项的差异        |
| `map_item_current_baseline.png`           | `/Users/zhangxiaoen/Downloads/02 后续页面设计稿 2/启程二级面板/地图选择/列表 Item 背景板/当前基准.png` | 本次编辑目标的宽高、深黛青内底和旧青铜边框方向；重点修正边框厚度与整体高度 |

生成正式素材时还必须提供三张本项目自身的 `375×817` 灰盒截图：

- `expedition_preparation_375x817.png`
- `expedition_hero_selection_375x817.png`
- `expedition_map_selection_375x817.png`

每次生成新素材还必须按类型上传已经生成并接入游戏的本项目素材，优先级高于外部参考图：

| 新素材类型     | 必须上传的既有素材                                                                                | 继承内容                                 |
| --------- | ---------------------------------------------------------------------------------------- | ------------------------------------ |
| 修士卡框、空位剪影 | `ui_expedition_hero_card_frame.png`、`ui_expedition_hero_selection_row_default.png`、`ui_camp_avatar_frame.png` | 深木、黑玉、旧青铜的材质比例，边角结构和像素密度 |
| 四名修士立绘    | `portrait_player_placeholder.png`、当前营地背景                                                 | 左上方受光、人物像素密度、黛青暖褐色盘；不得复制占位人物的脸、发型和身份 |
| 开山镐、探灵镜、锁 | `icon_resource_spirit_grain.png`、`icon_currency_spirit_stone.png`、`icon_action_plus.png` | 图标画布留白、轮廓粗细、明暗层次和小尺寸识别度              |

上述文件都从 `res://assets/camp/ui/` 或营地 资源目录 读取；`art/source_archive/camp_gpt_image_20260731/`
中的高清母版仅用于二次编辑，不替代运行时 PNG 的最终尺寸事实。

外部参考图仅用于信息层级与密度，不参考或复制其中的银色双龙/哥特边框、西方奇幻人物、
红蓝飘带、原作物品图标、黑色金属按钮纹样、精确构图、文字、UI 符号或专有剪影。
所有正式资产必须转译为原创中国修仙视觉：深木、黑玉、旧青铜、低饱和暖褐、青灰、
少量朱砂，保持《昆吾禁地》既有像素营地色盘。

## 6.4 所有新素材共用提示词

把下面一段放在每条具体提示词前面：

```text
为原创竖屏2D像素修仙游戏《昆吾禁地》制作“入山整备、编辑队伍、地图选择”UI美术资产。玩家可见窗口和UI唯一设计基准为375×817。参考截图只用于理解信息层级、列表密度和交互状态，不得复刻其中的具体构图、人物、边框、按钮、图标、文字或符号。

视觉使用中国修仙营地语言：深色旧木、黑玉、风化青石、旧青铜包角、低饱和暖褐和青灰，少量朱砂用于重要状态，冷青色用于选中高光。2D pixel art，清晰像素簇，硬边缘，无抗锯齿，无模糊渐变，轮廓在手机屏幕缩小后仍清楚；材质和像素密度必须与《昆吾禁地》营地大厅一致。所有装饰克制，中央信息区留白充足。
```

每条提示词末尾都加：

```text
不要任何文字、汉字、字母、数字、星星数量、按钮文案、角色名、职业名、地图名、水印或Logo；不要银色双龙、哥特尖刺、西式铠甲、欧洲卷草、红蓝飘带、现代图标、科幻霓虹、日式神社、Q版大头、卡通描边、3D渲染、写实照片、油画笔触、平滑矢量、抗锯齿或模糊渐变；不要复制参考截图中的具体人物、边框和图标。
```

## 6.5 必做素材规格表

|  # | 素材      | 数量 |     逻辑尺寸 |          最终 PNG 尺寸 | 文件名                                       | 运行时用途                   |
| -: | ------- | -: | -------: | -----------------: | ----------------------------------------- | ----------------------- |
|  1 | 修士卡通用边框 |  1 | `79×205` | `237×615 px (@3x)` | `ui_expedition_hero_card_frame.png`       | 叠在四张立绘之上，不拉伸            |
|  2 | 空槽人物剪影  |  1 | `64×112` | `192×336 px (@3x)` | `ui_expedition_hero_empty_silhouette.png` | 修士卡空位；“人选未定”仍由 Label 渲染 |
|  3 | 石岩卡面立绘  |  1 | `79×205` | `237×615 px (@3x)` | `portrait_hero_shi_yan_expedition.png`    | 金刚禅修卡背景与列表头像裁切          |
|  4 | 陆清卡面立绘  |  1 | `79×205` | `237×615 px (@3x)` | `portrait_hero_lu_qing_expedition.png`    | 五行法修卡背景与列表头像裁切          |
|  5 | 白灵卡面立绘  |  1 | `79×205` | `237×615 px (@3x)` | `portrait_hero_bai_ling_expedition.png`   | 玄箓修卡背景与列表头像裁切           |
|  6 | 墨言卡面立绘  |  1 | `79×205` | `237×615 px (@3x)` | `portrait_hero_mo_yan_expedition.png`     | 剑修卡背景与列表头像裁切             |
|  7 | 开山镐图标   |  1 |  `24×24` |   `72×72 px (@3x)` | `icon_expedition_pickaxe.png`             | 居中放入装载行 `38×38` 图标区     |
|  8 | 探灵镜图标   |  1 |  `24×24` |   `72×72 px (@3x)` | `icon_expedition_lens.png`                | 居中放入装载行 `38×38` 图标区     |
|  9 | 通用锁图标   |  1 |  `14×14` |   `42×42 px (@3x)` | `icon_expedition_lock.png`                | 队伍栏锁定、地图锁定共用            |

石岩自然呼吸图集是已接入的增量动画素材，不计入 2026-08-01 的首批 9 张静态 PNG。其同批
`shi_yan_punch_sheet.png` 已进入相同运行时目录，仅作为后续战斗表现资源保留，当前整备页不加载。

六档灵根资质当前已由 `SpiritualRootFrame` 使用 Godot `_draw()`/`StyleBox` 运行时绘制，不再要求
先制作图片。代码层同时使用轮廓、灵纹数量和颜色区分资质，不能只依赖颜色。下列 6 张
透明覆盖层作为 1.0 可选的正式美术替换，不计入当前首批 9 张，也不阻塞页面结构制作：

| 灵根资质 | 逻辑尺寸 | 最终 PNG 尺寸 | 文件名 | 覆盖层语言 |
|---|---:|---:|---|---|
| 杂灵根 | `79×205` | `237×615 px (@3x)` | `ui_hero_root_mixed_overlay.png` | 灰白单线、五个短断点 |
| 伪灵根 | `79×205` | `237×615 px (@3x)` | `ui_hero_root_pseudo_overlay.png` | 青绿双角、四个短断点 |
| 三灵根 | `79×205` | `237×615 px (@3x)` | `ui_hero_root_triple_overlay.png` | 湛蓝云角、三个灵点 |
| 双灵根 | `79×205` | `237×615 px (@3x)` | `ui_hero_root_double_overlay.png` | 靛紫双生符纹 |
| 天灵根 | `79×205` | `237×615 px (@3x)` | `ui_hero_root_heavenly_overlay.png` | 灵金单一完整日纹 |
| 异灵根 | `79×205` | `237×615 px (@3x)` | `ui_hero_root_variant_overlay.png` | 玄青底、青白虹彩、不对称变异灵纹 |

覆盖层只画边缘和角标，中间完全透明，叠在通用修士卡框上；不得改变人物亮度，不画文字、
字母、星星或元素属性图标。异灵根不得使用暗红、血滴、骨刺或魔气纹样。生成时必须把
`ui_expedition_hero_card_frame.png` 作为结构输入，六张保持完全相同的外轮廓与透明区。

修士卡立绘的画布和人物位置必须统一：人物躯干中轴在卡框内水平居中，头部与肩部位于
上半区；灵息、姓名、职业·等级和境界文字直接叠在人物下半区，不设置独立信息遮罩。
列表头像裁切安全区按立绘逻辑画布左上角计为
`x=14–64、y=8–58`（`50×50`）；四张立绘的双眼高度偏差不超过 `4px`。

## 6.6 修士卡框与空位剪影提示词

#### 修士卡通用边框

```text
生成一张79×205逻辑尺寸、237×615像素@3x的竖向修士卡边框，透明中心，透明画布。使用纤细黑玉外框、旧青铜小护角和极少量云纹刻线，边框宽度控制在2–3个逻辑像素。人物信息会直接叠在立绘下半区，因此边框下段保持简洁且中心完全透明，不制作信息底板。边框不表示资质和职业，不使用固定颜色品质光，不画人物、文字、星星或底色，不允许拉伸。
```

#### 空槽人物剪影

```text
生成64×112逻辑尺寸、192×336像素@3x的匿名修士空位剪影，透明背景。正面略侧身，中性人物比例，束发、短披肩和简洁道袍形成原创中国修士轮廓；人物内部使用黛黑与极暗青灰两级像素块，只保留头、肩、衣摆的大形，不出现面部、武器、职业、性别特征或文字。剪影四周至少留4个逻辑像素透明边。
```

修士卡边框必须把现有 `ui_camp_avatar_frame.png` 与 `ui_ling_pu_panel_frame.png` 作为
图像输入，只延展其黑玉、旧青铜、深木的边角语言，不另创一套银色或高亮品质框。

## 6.7 四名初始修士静态立绘提示词

四张立绘共用以下构图要求：

```text
输出单名原创修士的竖向修士卡静态立绘，逻辑画布79×205，最终237×615像素@3x RGBA PNG，透明背景，不带卡框。人物正面三分之四视角，从头部到大腿中段，人物躯干中轴与画布水平中心重合，头部中心和双眼高度在四张图中一致。上方126个逻辑像素必须清楚呈现脸部、肩部、职业轮廓和关键道具；下半区需要保持足够明暗对比，让运行时灵息、姓名、职业·等级和境界文字直接叠加后仍可读，但不要预画信息底板或渐隐。左上方受光，深色偏色轮廓线，清晰像素簇，24–32个主色以内。头像裁切安全区x=14–64、y=8–58内必须完整包含头部和双肩。不要背景、地面、卡框、文字、星星、数值、技能特效或夸张光环。
```

角色的修炼境界由卡底最后一行文字表达；灵根资质不显示文字，只由卡框内的运行时灵根
发光框表达。两者都不得通过人物服装的金框、紫框、皇冠或华丽程度画进立绘。异灵根使用
玄青底配青白虹彩灵纹；暗红、血色和紫黑留给魔道职业与危险状态，避免与资质视觉混淆。

#### 石岩｜金刚禅修

```text
青年男性金刚禅修，赤铜色皮肤，已经正式剃度，头顶完全无发，不画短发、发髻、发冠或额外头饰；宽肩结实体格和稳定下盘。穿有修补痕迹的旧褐僧衣，领口与前臂露出长期炼体形成的赤铜色肉身，赤足，腕间或胸前只有一串颗粒清楚的黑木念珠。双手自然收在近身伏魔掌的预备姿势，人物轮廓依靠光头、宽肩、僧衣斜襟、念珠和赤足辨认。表现炼气期佛门苦修、克制、可靠，不提前出现护法金身的完整金轮或魔相力士的魔纹臂影；不持剑、不持盾、不穿重甲，不画锡杖、巨大佛像、西式圣骑士、金色全身发光或现代健美肌肉。
```

#### 陆清｜五行法修

```text
青年女性五行法修，冷调浅肤，深蓝长发束入简洁道冠，身形修长、站姿端正；穿蓝紫与冷白为主的长袖法袍，袖片宽而有秩序，衣缘只以五枚克制的金、青、蓝、赤、土色小嵌片暗示五行，不画彩虹。身侧一高一低悬浮一枚薄型法盘和一面手掌大小的法镜，一手推演术式，另一手引导灵力。突出五行术式的计算感、单群法术和归元辅助；不使用法杖，不画女巫帽、魔法书、巨大八卦、宫装飘带、翅膀或科幻全息圆环。
```

#### 白灵｜玄箓修

```text
年轻女性玄箓修，暖浅肤，墨褐长发以偏侧低髻和一枚朴素木簪固定，与陆清的深蓝束冠明显不同；穿白青道袍，袖口利落，衣襟和腰带只有少量朱砂符线。腰侧固定一只朱砂符匣，肩侧悬浮一枚小型青金法印，数张无字符纸连接成一条有清楚折点的长带，沿身体外侧形成不对称弧线但不遮脸。突出符纸化刃、回元灵符和护神金符的职业血缘；不画药篓、葫芦、玉针、草药、护士、圣光祭司、巨大八卦或满屏文字符咒。
```

#### 墨言｜剑修

```text
中性青年剑修，肤色为中性小麦色，窄而清楚的面部骨相完整露出，黑色及肩直发半束，身形清瘦挺拔；穿墨青长袍和利落束袖，衣摆纵向分片，不使用贴身夜行衣。背后竖向固定一只窄长旧木剑匣，一枚尚未完全开锋的青黑剑胚悬在肩侧，手掌与剑胚之间只有一段短促青白御剑光。表现炼气期飞剑点星、剑光分化和养剑的克制锐利；不画魂灯、阴气、缚魂纹、双匕首、蒙面、兜帽、烟雾、忍者、鬼修或手持巨剑，面部不得藏在阴影中。
```

以上外观与 `Docs/1.0策划案/15_1.0版本完整策划案_草案.md` §4.2–4.5 对齐，是本批
角色视觉基线。现有四张若由旧“剑修/法修/医修/潜修”提示词生成，只能作为卡面尺寸和
构图的临时占位，不能再上传给 GPT 充当人物身份输入，也不能继续据此派生战斗动作；旧文件
及其 `.import` 暂不删除。后续以新四人差异设定板和同名身份裁切为人物身份事实源，再回补
同名卡面；卡面构图和裁切安全区保持不变，不得在同一版本中混用两套脸与服装。

## 6.8 三个功能图标提示词

生成时先使用 §6.4 的共用提示词，再接下列对应图标的具体提示词，最后追加 §6.4 的共用
禁用项。三个图标均使用 RGBA 透明背景，主体不得自带按钮底板或圆形外框；`24×24` 图标
使用约 `2` 个逻辑像素的深色偏色轮廓，`14×14` 通用锁使用约 `1` 个逻辑像素轮廓。
输出必须由整齐的 `3×3` 实际像素块组成，缩小到逻辑尺寸后检查轮廓，而不是把平滑高清图
直接缩小。开山镐与探灵镜主体占画布约 `75%–82%`，通用锁可占约 `80%–86%`。

#### 开山镐图标

```text
生成“开山镐”物品图标，文件名 icon_expedition_pickaxe.png。它是修士入山开采矿脉、破除岩障使用的便携工具，不是普通武器。逻辑画布严格为24×24，最终交付严格为72×72像素@3x RGBA透明PNG；每个逻辑像素对应整齐的3×3实际像素块，使用硬边缘像素簇，不使用抗锯齿。

画面仅绘制一把短柄开山镐，采用轻微三分之四视角，整体从左下向右上倾斜。风化深木短柄约占主体长度三分之二，握持端略粗；镐头使用旧黑铁，横向两端一端尖锐、一端略扁，中央以两道旧青铜绑箍固定，使缩小到24×24时仍能一眼识别为矿镐。黑铁边缘只保留少量冷灰高光，绑箍可有一道极短的冷青封灵刻痕，用于表达修仙工具，但不得形成明显发光特效。主体占画布约75%–82%，四周至少保留2个逻辑像素透明安全边，重心居中，不画投影。

必须把 icon_resource_spirit_grain.png、icon_currency_spirit_stone.png 和 icon_action_plus.png 作为风格输入，只继承其画布留白、深色轮廓、明暗层次和小尺寸像素密度，不复制任何具体造型。不要矿石、碎石、火花、手掌、人物、背包、按钮底板、圆形银框、交叉双镐、现代工程镐、战斧、武器血迹、数字、数量角标或文字；不得复制外部参考图中的镐头轮廓。
```

验收：在 `24×24` 逻辑尺寸下仍能分清木柄、镐头和青铜绑箍；不得被误认成斧头、锤子
或两把工具交叉形成的徽章。

#### 探灵镜图标

```text
生成“探灵镜”物品图标，文件名 icon_expedition_lens.png。它是修士在遭遇战前探查敌方属性与灵力波动的便携观测法器，不是宝石、眼球或西式放大镜。逻辑画布严格为24×24，最终交付严格为72×72像素@3x RGBA透明PNG；每个逻辑像素对应整齐的3×3实际像素块，使用硬边缘像素簇，不使用抗锯齿。

画面仅绘制一面手持探灵镜，采用正视略带三分之四角度。主体为一枚黑玉圆形镜片，外圈嵌窄薄的旧青铜法器框，右下侧连接一段短小深木握柄；镜片内部使用玄青与极暗蓝灰像素块，保留一条不闭合的冷青弧形反光，并加入四个极淡、彼此断开的阵法刻点，表达感知灵气而不是镜面玻璃。圆镜、青铜外圈和短握柄三部分在24×24下都必须清楚，主体占画布约75%–82%，四周至少保留2个逻辑像素透明安全边，不画投影或外部光环。

必须把 icon_resource_spirit_grain.png、icon_currency_spirit_stone.png 和 icon_action_plus.png 作为风格输入，只继承其画布留白、深色轮廓、明暗层次和小尺寸像素密度，不复制任何具体造型。不要蓝色宝石球、水晶球、人物眼睛、妖兽眼球、现代摄像头、望远镜、西式单片眼镜、完整八卦图、按钮底板、圆形银框、大面积霓虹、魔法爆光、数字、数量角标或文字；不得复制外部参考图中的透镜外框和高光构图。
```

验收：在 `24×24` 逻辑尺寸下能同时读出黑玉镜面、青铜窄框和短握柄；不得与灵石图标、
水晶球、眼睛状态图标或普通放大镜混淆。

#### 通用锁图标

```text
生成“通用锁”状态图标，文件名 icon_expedition_lock.png。它同时用于未解锁队伍栏和未解锁地图，只表达“当前不可用或尚未解锁”，不表达付费、危险或任务失败。逻辑画布严格为14×14，最终交付严格为42×42像素@3x RGBA透明PNG；每个逻辑像素对应整齐的3×3实际像素块，使用硬边缘像素簇，不使用抗锯齿。

画面仅绘制一个小型旧青铜封禁锁，正视构图。上半部为清楚闭合的倒U形锁梁，下半部为略扁的方形封印锁牌；锁牌中心只保留一个由2至3个逻辑像素组成的黛青封印凹点，不画现代钥匙孔。使用旧青铜、暗褐阴影、灰青高光共4至6个主色，外轮廓约1个逻辑像素；左右基本对称，但在一侧加入一处极短的磨损缺口，避免现代扁平图标感。主体占画布约80%–86%，四周至少留1个逻辑像素透明安全边，不画投影、链条或底板。

必须把 icon_resource_spirit_grain.png、icon_currency_spirit_stone.png 和 icon_action_plus.png 作为风格输入，只继承其深色轮廓、明暗层次和小尺寸像素密度，不复制任何具体造型。不要文字、锁字、钥匙、金币、灵石、宝石、价格、红点、红色付费提示、红色警告三角、任务感叹号、长锁链、盾牌、宝箱、按钮底板、圆形外框、现代密码锁、指纹锁、数字或数量角标。
```

验收：在 `14×14` 逻辑尺寸下仍能一眼识别出闭合锁梁与方形锁牌；分别放入队伍栏和地图
列表时含义中性，不会被误读成付费按钮、危险提示、护盾或宝箱。

## 6.9 现有素材复用与按钮替换

下表同时记录已存在的复用素材和全局按钮的当前验收状态。接入前确认其 AtlasTexture
使用 Nearest；按钮和行底板按九宫格拉伸，文字仍由 Label 渲染。

| 复用素材    |     逻辑尺寸 | 实际文件与尺寸                                                                           | 本模块用途                                   | 状态  |
| ------- | -------: | --------------------------------------------------------------------------------- | --------------------------------------- | --- |
| Level 2 面板九宫格 | `96×96` | `res://assets/camp/ui/ling_pu/ui_ling_pu_panel_frame.png`，`288×288 px` | 只供二级确认框等可拉伸面板复用 | 保留 |
| 旧资源行九宫格 | `64×24` | 原 `ui_ling_pu_resource_row.png` | 已由 Figma 页面专用行底板或原生 Graphics 替代 | 已退役并移除 |
| 通用列表项普通态 + 选中态 | `64×32` | `res://assets/camp/ui/common/ui_common_list_item_{normal,selected}.png`，计划各 `192×96 px` | 两张都是完整不透明的无框底板；选中时切换 AtlasTexture，锁定仍用 `modulate`、锁图标和原因文字 | 待生成 |
| 通用行内按钮 | `72×28` | `Button/Resizable/Inline` | 编辑队伍、调息、列表行选择/取消等紧凑操作 | 原生绘制，无 PNG |
| 通用底部按钮 | `132×44` | `Button/Resizable/Footer` | 面板底部传送/返回、完成/返回等面板级操作 | 原生绘制，无 PNG |
| 旧横向按钮九宫格 | `132×44` | 原 `ui_ling_pu_action_button_normal.png` | 已由原生按钮替代 | 已退役并移除 |
| 灵粮图标    |  `24×24` | `res://assets/camp/ui/top/icon_resource_spirit_grain.png`，`72×72 px`            | 灵粮装载行，居中放入 `38×38` 图标区                  | 已生成 |
| 加号图标    |  `40×40` | `res://assets/camp/ui/ling_pu/icon_action_plus.png`，`120×120 px`                | 三条装载行增加按钮；可见图不决定触控区                     | 已生成 |
| 减号图标    |  `40×40` | `res://assets/camp/ui/ling_pu/icon_action_minus.png`，`120×120 px`               | 三条装载行减少按钮；可见图不决定触控区                     | 已生成 |
| 大厅头像框   |  `50×50` | `res://assets/camp/ui/top/ui_camp_avatar_frame.png`，`150×150 px`                | 选人列表头像以 `50×50` 显示，境界文字徽记放在框外下方；不拉伸为竖框 | 已生成 |
| 灵石图标    |  `24×24` | `res://assets/camp/ui/bottom/icon_currency_spirit_stone.png`，`72×72 px`         | 队伍解锁费用；不得另画金币或宝石                        | 已生成 |

旧资源行与三类按钮 PNG 已从 Godot 场景解绑并连同 `.import` 移除。按钮按下状态使用 `100ms`
可见子节点缩放回弹，禁用状态使用 Godot 原生颜色与层级；
修士选中态再叠加描边与“取消/槽位编号”，地图锁定态再叠加锁图标与原因文字，均不只依赖颜色。

新通用列表项生成并验收前继续使用现有资源行底板和运行时描边；生成并验收后，入山
不得保留 `ui_expedition_list_row_*` 页面专用副本。普通项、选中项必须使用与灵源院及
后续建筑列表相同的全局文件。

## 6.10 可选优化素材

| 素材         |     逻辑尺寸 |          最终 PNG 尺寸 | 文件名                                           | 启用条件                                 |
| ---------- | -------: | -----------------: | --------------------------------------------- | ------------------------------------ |
| 地图选择细边矮型 Item 底板 | `84×20` | `252×60 px (@3x)` | `ui_expedition_map_item_compact_frame.png` | 当前地图 Item 基准图边框显得过厚、行高过高时启用；只替换地图选择页，不改变全局无框列表项 |
| 入山专用面板九宫格  |  `96×96` | `288×288 px (@3x)` | `ui_expedition_panel_frame.png`               | 共用灵源院面板在 `359×777` 拉伸后标题区或边角明显失真时启用   |
| 灵息条底       |   `64×6` |  `192×18 px (@3x)` | `ui_expedition_stamina_track.png`             | 修士卡文字灵息在 `375×817` 真机上难以扫读时成对启用      |
| 灵息条填充      |   `64×6` |  `192×18 px (@3x)` | `ui_expedition_stamina_fill.png`              | 与灵息条底成对启用，低灵息仍必须有数值/文字提示             |
| 负重图标       |  `18×18` |   `54×54 px (@3x)` | `icon_expedition_burden.png`                  | “负重 x/y”纯文字在真机上定位不清时启用               |

可选灵息条不得替代 `灵息 x` 文本，也不得只用绿/黄/红颜色传达是否满足地图门槛。

### 6.10.1 地图选择细边矮型 Item 底板提示词

本素材只针对地图选择页的横向 Item 背景板。它是当前基准图的重做候选，不替换
`ui_common_list_item_{normal,selected}.png` 的全局无框列表项规范。先上传
`map_item_current_baseline.png` 作为唯一编辑目标；若需要锁定项目色盘，可再上传一张已经
通过的营地或地图选择 `375×817` 截图作为风格参考，但不要上传其他带厚重边框的 Item 图。

```text
Use case: precise-object-edit
Asset type: 《昆吾禁地》地图选择列表专用的细边、矮型横向 Item 背景板；不是按钮、卡牌、弹窗或头像框
Input images: 图片1是唯一编辑目标，即当前基准图。只保留它的横向矩形用途、深黛青/黑玉内底、旧青铜与深木的低饱和材质方向；不要照搬它过厚的多层边框、四角台阶和外伸装饰。若提供图片2，它只参考《昆吾禁地》已有像素密度、左上方受光和黛青/深木/旧青铜色盘，不是编辑目标
Primary request: 在图片1的基础上重做一个更轻、更薄、更矮的地图列表 Item 背景板，文件名 ui_expedition_map_item_compact_frame.png。它应当一眼读作“列表内容的背景板”，不能读作缩小弹窗、木牌、按钮或厚重卡牌框
Shape and proportion: 输出单个连续横向矩形，最终逻辑尺寸严格为84×20，交付PNG严格为252×60像素（@3x）。相较图片1明显压低整体高度；宽度方向保持横向 Item 的比例。四角只允许一次、最多1–2个逻辑像素的极小斜切，外轮廓必须能被一个规整矩形紧密包住；不得有多层退台、阶梯、端盖、侧翼、立柱、脚座或突出底座
Border: 使用总厚度约2–3个逻辑像素的单层细边框：最外侧1逻辑像素黛黑硬边，内侧1逻辑像素暗旧青铜或深木细线，最多再加1逻辑像素黑玉收口。边框线要连续、均匀、低反光，不能像金属浮雕；不要把上下边框做成粗横梁，也不要在四角堆叠装饰
Interior: 中央是完整、连续、完全不透明的低反光深黛青/黑玉实色内底，允许极轻、均匀、无方向性的像素颗粒，保证地图名、两行说明、锁图标或资源消耗等运行时内容清楚可读。内底不能透明、镂空、发光、渐变或出现大块污渍、裂纹、符纹、云纹
Style/medium: 中国修仙营地 2D pixel art，清晰硬边像素簇，无抗锯齿、无平滑矢量感；低饱和黛青、深木、黑玉、旧青铜共同基底，装饰克制，视觉权重低于地图名、地图图标、锁图标和按钮。不能引入银色、金色高亮或西式奇幻装饰
Nine-slice: 适合横向九宫格使用；建议左右各8个逻辑像素、上下各5个逻辑像素为不可拉伸区，中间保持均匀低纹理。横向拉伸时只允许中央内底变宽，细边框、四角和斜切保持不变；纵向不要再拉高成旧版厚重卡片
Opacity and output: 整张252×60画布由背景板本体完整填满，所有像素完全不透明，Alpha为100%。不要透明背景、透明留白、棋盘格、绿色底、纯色伪透明底、页面截图、多个版本拼版或使用示意图
Content constraints: 只输出一个空白普通态 Item 背景板；不要文字、汉字、字母、数字、地图名、地图图标、人物、头像、锁、按钮、对勾、红点、选中标记、价格、星级、进度条、水印或Logo。选中态、锁定态和禁用态由运行时 `modulate`、独立图标和文字表达
Avoid: 厚重多层边框、双重内框、粗金属包角、四角建筑装饰、龙凤兽首、哥特尖刺、牌坊、城门、木制牌匾、卡牌外框、银色高光、金色描边、霓虹、玻璃、水晶、宝石镶嵌、强投影、外发光、3D渲染、写实照片、油画笔触、模糊渐变、抗锯齿
```

生成后先将图片缩小到 `84×20` 逻辑尺寸，并与地图选择页的 `375×817` 灰盒截图合成检查：
两行文字仍可读，锁图标不会挤压标题，连续排列时不会因为细边框重新形成卡牌墙。只有该
候选底板通过用户视觉确认后，才复制到 `res://assets/camp/ui/expedition/` 并交给
Godot 编辑器自动导入生成 `.import`；不要手写导入缓存或 UID。

## 6.11 输出目录与文件命名

GPT Image 原始输出和运行时文件分开保存：

```text
art/source_archive/expedition_gpt_image_20260801/
├─ references/
└─ raw_outputs/
   ├─ panel/
   ├─ heroes/
   ├─ list/
   └─ icons/

res://assets/camp/ui/expedition/
├─ ui_expedition_*.png
├─ portrait_hero_*_expedition.png
└─ icon_expedition_*.png
```

- 原始大图、参考图和失败版本只留在 `art/source_archive/`，不得进入运行时资源目录。
- 最终文件名必须与 §6.5 完全一致，不添加 `(1)`、日期、`final`、中文名或模型后缀。
- 四张立绘必须使用同一画布、同一眼高、同一像素密度和同一左上方光源。
- 新 PNG 进入 `assets/` 后必须由 Godot 编辑器自动导入并生成 `.import`，不得手写 UID。

## 6.12 制作顺序

1. 先从 Godot 导出三张 `375×817` 灰盒截图，锁定实际信息密度和安全区。
2. 把已生成的灵源院面板、资源行、加减按钮、大厅头像框、灵粮和灵石图标，以及
   [灵源院分册 §5.4.4](03_灵源院生产弹窗.md#544-通用行内按钮与通用底部按钮) 的通用行内按钮和重做后的通用底部按钮合成到三张灰盒截图；底部按钮重做前可使用纯色占位，
   不把现有阶梯形按钮作为最终合成基线。
3. 制作修士卡框并先生成石岩一张立绘，验证卡内人物居中、直接叠字可读性和 `50×50` 头像裁切。
4. 石岩通过后，以其眼高、像素密度和光照为参考生成陆清、白灵、墨言。
5. 制作空槽剪影、开山镐、探灵镜和通用锁，同时检查正常、选中、禁用与锁定状态。
6. 只有合成或真机验收明确失败时，才制作 §6.10 对应的可选优化素材。
7. 统一离线裁切为目标 `@3x` 尺寸，再由 Godot 编辑器自动导入并生成 `.import`。

## 6.13 快速验收清单

- [ ] 所有新 PNG 的逻辑尺寸、实际像素尺寸、文件名和透明通道与 §6.5 一致。
- [ ] 复用的灵源院面板拉伸到 `359×570`、`359×777` 时边角不变形、边线不断裂。
- [ ] 复用的资源行拉伸到 `327×32`、`327×50`、`319×106`、`319×91` 时纹理不变形，文字区对比充足。
- [ ] 编辑队伍、调息和列表行选择/取消使用通用行内按钮；启程/离开、完成/返回使用通用底部按钮。
- [ ] 两类按钮的高度、轮廓和材质一眼可区分；行内按钮不抢列表文字，底部按钮必须是连续规整的长方形，不得出现阶梯形边缘、外扩侧翼或底部基座。
- [ ] 四张立绘放入 `79×205` 卡面后人物中轴居中，人物比例、眼高和光照一致；灵息、姓名、职业·等级和境界直接叠字后仍可读且没有信息底板。
- [ ] 同一立绘裁切为 `50×50` 头像并放入现有大厅头像框后仍能辨认角色与职业，没有第二套脸。
- [ ] 空槽在不看“人选未定”文字时也明显不是已上阵修士。
- [ ] 选中、锁定和普通态不只依赖颜色；同时存在轮廓/纹样和运行时文字说明。
- [ ] 开山镐、探灵镜和锁图标在逻辑尺寸下无需放大查看即可识别。
- [ ] 页面中没有烘焙文字、数字、星星、地图名、按钮文案、水印或外部作品标志。
- [ ] 不含银色双龙、哥特尖刺、西方人物、原作图标或可识别的外部专有构图。
- [ ] 所有像素边缘清晰，Nearest 过滤下没有半像素、线性模糊或透明白边。

正式视觉验收至少保留以下本项目截图：

- `expedition_preparation_full_party_375x817.png`
- `expedition_preparation_empty_slots_375x817.png`
- `expedition_preparation_low_stamina_375x817.png`
- `expedition_preparation_overweight_375x817.png`
- `expedition_hero_selection_selected_375x817.png`
- `expedition_map_selection_locked_375x817.png`

截图必须覆盖满队、空位、低灵息、负重阻断、修士选中和地图锁定；仅交付单张“正常态”
截图不能完成本批美术验收。
