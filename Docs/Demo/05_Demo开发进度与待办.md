# 《昆吾禁地》Godot Demo 开发进度与待办

更新日期：2026-08-11  
引擎：Godot 4.7.1  
当前目标：维持 D0 可玩闭环，逐步用正式 Godot 场景、地形和美术替换灰盒

> 迁移前的完整开发日志保留在
> [`../LegacyCocos/Demo/05_Demo开发进度与待办.md`](../LegacyCocos/Demo/05_Demo开发进度与待办.md)。

## 1. 当前可玩流程

```text
Boot
→ Camp 营地横滑
→ 灵源院生产/招募/储量升级
→ 入山整备
→ map_01 迷雾探索、宝箱和故事
→ 残禁石傀固定遭遇
→ 胜负/撤离、战利品
→ 回入口归营结算
```

## 2. 迁移完成状态

| 模块 | 状态 | Godot 事实源 | 说明 |
|---|---|---|---|
| 工程与启动 | 🟢 | `project.godot`、`scenes/boot.tscn` | Godot 4.7.1、GL Compatibility、`375×817` |
| 全局状态/存档 | 🟢 | `scripts/autoload/game.gd` | `user://kunwu_profile.json` |
| 营地全景/HUD | 🟢 | `scenes/camp.tscn`、`camp.gd` | 横滑、建筑、顶底 HUD 已接线 |
| 灵源院 D0 | 🟢 | `camp.gd`、`ling_pu_config.json` | 生产、调岗、招募、升级、存档 |
| 入山整备 | 🟢 | `camp.gd`、`expedition_preparation.json` | 四人队、灵息、负重和装载 |
| `map_01` D0 | 🟢 | `scenes/map.tscn`、`map_01_demo.json` | `15×15` 可玩灰盒，非正式地图 |
| 战斗 D0 | 🟢 | `scenes/combat.tscn`、`combat_resolver.gd` | 20 Hz、手动/自动、撤离、结算 |
| Dual Grid 工具 | 🟢 | `addons/TileMapDual`、`resources/tilemapdual_standard.tres` | v5.0.2，15 掩码可达 |
| 策划/PRD/美术文档 | 🟢 | `Docs/` | 已迁入，Godot 执行指南已更新 |
| 美术源文件 | 🟢 | `art/source_archive/` | `.gdignore` 归档 |
| 第三方原始包/许可 | 🟢 | `third_party/` | `.gdignore` 归档 |

## 3. 正式化进度

| 内容 | 状态 | 下一个可验收结果 |
|---|---|---|
| `map_01` Figma 灰盒 | 🟡 | `48×64` 规则网格、碰撞、对象静区与 `375×817` 视窗 |
| `map_01` 视觉母版 | 🟡 | 低饱和灰青山岩、枯黄旧路、三阵灯和万修之门纯俯视母版 |
| `map_01` 正式 Tile Set | 🟡 | 基础地面、道路连接、山体阻挡、阵灯三状态试制视窗 |
| 地图正式 `TileMapLayer` | 🟡 | 用 Godot TileSet/TileMapDual 替换 Puny Dungeon D0 绘制 |
| 战斗正式美术 | 🟡 | 单位、技能、状态、背景和战利品逐项替换灰盒 |
| 音频/VFX | 🟡 | 建立 Audio Bus、一组技能序列帧与命中反馈 |
| `map_02` 预览 | ⚪ | `map_01` 首轮试制验收后再开始独立灰盒 |

## 4. 已知的产品差距

- D0 灵源院生产常数仍是兼容口径，尚未升级为 PRD-03 的 1.0 产出、维护、灵液周期和五资源容量。
- `map_01` 玩法灰盒小于 1.0 `48×64` 正式边界，对象数量也是 D0 裁剪。
- 当前战斗只覆盖一组固定遭遇，不表示四图敌人、Boss 和 36 技能均已完成。
- 当前没有账号、服务端、跨设备同步或远程配置发布。

## 5. 开发验证

| 验证 | 命令/入口 |
|---|---|
| 数据与 `res://` 路径 | `Godot --headless --script res://tools/validate_project_data.gd` |
| 工程导入/脚本解析 | `Godot --headless --editor --path <project> --quit` |
| Dual Grid | `Godot --headless --script res://tools/validate_tilemapdual_brush.gd` |
| 整体流程 | Godot 编辑器 `F5` |
| 视觉与触控 | `375×817` 逻辑视口与实际目标设备 |

## 6. 近期顺序

1. 先完成 `map_01` 灰盒、视觉母版和最小 Tile Set 试制。
2. 把试制 Tile 导入 `TileMapLayer`/TileMapDual，完成一个典型视窗。
3. 完成 `map_01` 正式地形与关键地标后，再开始 `map_02`。
4. 生产、成长和战斗数值按对应 PRD 分别升级，不与引擎迁移混为一次无法验证的大改。

## 7. `map_01` 制作顺序 TODO

用途：把 `map_01` 从当前 `15×15` 可玩灰盒推进到 D1 可公开展示的地图试制。以下顺序是强依赖关系，
前一 Gate 未通过时不进入后一阶段。`48×64` 全图内容、完整敌群和章节任务仍属于 1.0 后续依赖；D1
只接入已经冻结且能形成完整体验的子集，不借地图美术之名扩张正式内容。

### Gate 0：保护现有闭环

- [x] 当前 `Ground / DifficultTerrain / Markers` 场景职责已建立。
- [x] 当前四方向移动、迷雾、灵粮、休整、宝箱、故事、固定遭遇和归营闭环可运行。
- [x] TileMapDual `v5.0.2` 技术刷已覆盖 `0x1–0xF` 掩码。
- [ ] 为本轮建立独立视觉验收截图，至少包含当前 `375×817` 探索页和一个 `5×5` 地图裁切。
- [ ] 冻结本轮只改地图表现与经批准的布局；不得顺带改奖励、战斗数值或玩家存档结构。

完成口径：现有 D0 流程可随时作为回归基线；所有自动测试显式传入 `-- --no-profile-write`。

### Gate 1：冻结地图灰盒

- [x] 依据地图详细设计建立 `48×64`、每格 `16×16` 的规则灰盒，不从当前 `15×15` 对称迷宫放大。
- [x] 明确 A 入山坡、B 废营灯坪、C 万修门外坡、D 山腹暗道、E 东壁阵灯台、F 北坡残阵带、
  G 万修之门七区的边界和进出关系。
- [x] 冻结“南宽入口 → 废营 → 残碑 → 双路绕脊 → 北坡汇流 → 门前展开”的主轮廓。
- [x] 标出普通地形 `1/步`、碎石危险地形 `2/步`、不可走山体、可淡出前景和对象静区。
- [x] 检查西暗道与东壁两路都能进入 F 区，不形成长死胡同；Boss 前保留安全整备区和回程方向。
- [x] 只把上游已确认内容作为对象锚点；候选坐标统一标 `UNVERIFIED`，不直接写入正式场景或 JSON。
- [ ] 导出以下四张灰盒输入：
  - `map01_master_annotated.png`
  - `map01_layout_collision.png`
  - `map01_gpt_clean_guide.png`
  - `map01_marker_test.png`
- [ ] 导出一张 `screenshot_exploration_viewport_375x817.png`，检查 HUD、世界视窗和底部操作区压力。

完成口径：无文字时能看出纵向山谷、斜山脊双路和北侧山门方向；可走、危险、阻挡和前景语义无冲突。

### Gate 2：确认视觉母版

- [ ] 以灰盒 clean guide 为结构事实源制作 `map01_visual_mother.png`，不得让生成图自行改路或补点位。
- [ ] 冻结低饱和灰青山岩、枯黄旧路、深灰阻挡、暖白古阵和少量叶家暗红的色盘。
- [ ] 保持接近 `90°` 垂直俯视、正交和平面 2D；不出现崖壁侧面、平台厚度、透视或投影。
- [ ] 普通区域保持低噪点和连续大色面；细节预算只给万修之门、斜山脊和阵灯。
- [ ] 保证对象格中央约 `32×32` 逻辑像素的安静区，不用裂纹、高光或小物件抢 Marker。
- [ ] 将当前石板/泥地地图只作为失败对照，不作为正式结构或纹理事实源。
- [ ] 由用户确认色盘、俯视感、路线可读性和细节密度。

完成口径：视觉母版通过人工 Gate；它只负责美术方向，不作为可直接导入 Godot 的整图。

### Gate 3：建立 Tile Set 需求与覆盖表

- [ ] 为灰盒每格记录 `terrain_id`、移动成本、可走/阻挡和上下左右连接。
- [ ] 建立基础地形需求：灰青地面 `4` 变体、枯黄旧路 `3` 变体、碎石危险地形 `3` 变体。
- [x] 建立道路 Dual Grid `0x1–0xF` 四角掩码覆盖：实心、横竖走廊、L 角、凹角、单格、单格洞和两种对角。
- [ ] 建立四向边、`4` 外角、`4` 内角，以及西北—东南、东北—西南两组斜山脊需求。
- [ ] 建立 `6–8` 个大型阻挡占格，以及山脊压顶、暗道顶石、万修门顶部的前景/fade 需求。
- [ ] 建立阵灯、残碑、废营、暗道、阶梯和万修之门的尺寸、锚点、碰撞、前景与状态表。
- [ ] 建立“灰盒实际拓扑 → 正式资产”的覆盖矩阵；未确认项保留 `UNVERIFIED`，不靠整图补洞。

完成口径：灰盒中每种实际地形、连接、阻挡、前景和地标状态都有明确资产去向。

### Gate 4：生产最小美术试制包

- [x] 分批制作并人工清稿基础地面、道路连接、山体边缘/阻挡和一盏阵灯三状态。
- [x] 正式源 Tile 为真实 `16×16` 像素、硬边、nearest、无 mipmaps；阻挡、前景和地标使用真实 Alpha。
- [x] 阵灯先完成 `LAMP_BROKEN / LAMP_REVERSED / LAMP_REPAIRED`，三态保持相同尺寸、锚点和碰撞。
- [x] 如果正式地形继续使用 Dual Grid，先生成一张连续的 `1024×1024` 风车母图，不能生成
  `15` 张互不连续的小 Tile。
- [x] 检查风车母图严格为 `4×4`、每逻辑格 `256×256`，没有文字、网格、透视、裁切或错误背景。
- [x] 编译并检查 `dual_grid_15piece.png`、`dual_grid_15piece_check.png` 和 manifest；重点检查
  `0x6`、`0x9` 两种对角掩码。
- [ ] 不在这一阶段生产万修之门、废营、暗道、阶梯和整套对象，避免视觉方向未确认就扩大返工。

进度注记（2026-08-21）：阵灯完成后，万修之门已作为单独地标家族完成三态组件和验收；废营随后在
Gate 7 完成“固定底座＋独立尸首证据层”组件。暗道与阶梯也已完成各自运行素材、组件及独立验收，
这些地标均只接入候选构图预览，未写入正式 `48×64` 场景。

完成口径：最小包能拼出一个有地面、道路、阻挡、前景和阵灯状态的典型视窗，且不存在模糊、假 Alpha 或接缝。

### Gate 5：接入 Godot 典型视窗

- [x] 新资源交给 Godot 自动导入，不手写 `.import` 或 `.godot/` 缓存。
- [ ] 保留 `Ground` 作为可走格事实、`DifficultTerrain` 作为高成本可走格、`Markers` 作为语义坐标；
  道路、阻挡视觉、前景和贴花使用独立表现层，不把业务规则写入 Sprite。
- [x] `Ground` 只绘制完整块 `0xF`；不得对它调用 Godot 原生 `terrain_connect`/`terrain_path`。
- [x] TileMapLayer 修改固定走“检查 → 事务预览 → 检查差异与 PNG → 应用 → 复查”流程。
- [x] 先接入一个 `375×817` 典型视窗，不用新素材整体覆盖 `scenes/map.tscn` 或整张正式场景。
- [ ] 叠加当前队伍、入口、敌人、剧情和宝箱 Marker，检查浅色、深色和迷雾边缘下的可读性。
- [ ] 在 `100% / 75% / 50%` UI 缩放下检查 Marker，不依赖临时白边、黑底盘或放大补救。

完成口径：典型视窗在 Godot 运行时保持 48×48 逻辑格、nearest、四方向移动和迷雾规则，视觉层不改变领域结果。

进度注记（2026-08-20）：已新增独立的 `48×64` 无物件环境场景
`scenes/maps/map_01_d1_environment.tscn`，包含 `2184` 个可走格、`378` 个道路格、`121` 个危险格、
`888` 个不可走格，以及岩壁底层和可淡出前景；可走区为单一连通分量，西暗道与东壁路线到 F 区分别为
`21` 步和 `25` 步。已导出全图与 `375×817` 视窗截图。该场景不含 Marker、地标、敌人或资源，且未覆盖
现有 `15×15` D0 可玩场景；`(31,49)` 与 `(39,31)` 的对象共享格冲突仍留待后续冻结。

进度注记（2026-08-20）：`121` 个危险格已从“普通地面叠半透明深色”切换为独立的低频碎石
Dual Grid 笔刷 `resources/tilemapdual_map01_difficult.tres`；语义层 `DifficultTerrain`、格子坐标、
碰撞与路线均未改变。该笔刷已覆盖实心块、横竖走廊、L 角、单格、单格洞和两种对角接触，
并确认 `0x1–0xF` 全部可达；仍需用户在 Godot 正常探索尺度确认最终明度和碎石质感。

进度注记（2026-08-21）：用户已确认 Meowa 生成的“灰蓝岩地 × 枯黄旧路”双材质过渡候选，并晋升为
`resources/tilemapdual_map01_road.tres` 正式道路笔刷。正式 1024×1024 图集与确认候选 SHA-256 一致；
D1 的 `378` 个道路格、可走性、碰撞和路线未修改，正式全图及 `375×817` 截图与候选逐字节一致。

### Gate 6：D1 视觉试制验收

- [x] 运行 `tools/validate_map01_scene.gd -- --no-profile-write`。
- [x] 运行 `tools/validate_tilemapdual_brush.gd -- --no-profile-write`，确认 `0x1–0xF` 全部可达。
- [x] 执行 Godot headless editor 导入/脚本解析和一次短 headless 项目运行。
- [ ] 人工检查实心块、横竖走廊、L 角、凹角、单格、单格洞和两种对角接触。
- [ ] 人工检查 Tile 接缝、迷雾、困难地形、前景淡出、Marker 静区和 `375×817` 构图。
- [ ] 由用户明确确认：风格、纯俯视感、路线可读性、标记可读性和 Tile 拼接。

完成口径：D1 典型视窗通过自动与人工 Gate。未通过时只回退对应灰盒、母版或素材批次，不扩大到全图。

### Gate 7：扩展关键地标与共用 Marker

进入条件：Gate 6 通过。

- [x] 完成三盏阵灯的共用三态并接入各自独立运行状态。
- [x] 分别生产并验收万修之门、残碑和废营；每次只处理一个地标家族。
- [x] 生产并验收暗道入口。
- [x] 生产并验收东壁阶梯。
- [x] 完成门的 `LOCKED / BOSS_READY / OPEN` 和废营尸首处理前后状态。
- [x] 完成阶梯 `CLOSED / OPEN`。
- [x] 完成暗道 `DEFAULT / DISCOVERED / CLEARED` 的正式运行素材。
- [ ] 完成队伍、资源、普通敌群、副本入口、出生点、跨图出口和 Boss 共 `7` 个地图 Overlay Marker。
- [ ] 完成扎营、归营、队伍、背包以及进食、疗伤、继续探索共 `7` 个操作图标。
- [ ] 完成路径点、终点、阻挡、中断和低频贴花；迷雾继续使用运行时状态，不导出三张完整地图。

进度注记（2026-08-20）：万修之门和残碑两个地标家族已完成三态素材、独立碰撞、状态组件、
`375×817` 验收场景及自动验证。废营也已完成母图归档、红色杂点清稿、`96×56` 分层运行素材、
两顶帐篷独立碰撞、处理前后状态组件、`375×817` 验收场景及自动验证；原生成要求的 `72×56 / 4×3`
因右侧尸首证据空间不足，验收后调整为 `96×56 / 5×3`。粮册保持独立地图对象，不并入废营状态。
暗道入口的纯俯视修正版母图已归档，并完成 `TUNNEL_DEFAULT / TUNNEL_DISCOVERED / TUNNEL_CLEARED`
三态 `72×56 / 4×3` 运行素材、常驻山石底座、独立可淡出顶石、中心状态层、两侧永久碰撞、仅清除态
开放的中心通路、独立 `375×817` 验收场景及 D 区三态同镜头候选预览。三态运行轮廓最低 IoU 为
`0.990385`，自动验证通过；候选坐标 `(8,28)` 仍未冻结。东壁阶梯已归档透明两态母图，并完成
`STAIRS_CLOSED / STAIRS_OPEN` 的 `72×56 / 4×3` 运行素材、底部中心锚点、两侧静态碰撞、仅封闭态启用的
中心阻挡、独立 `375×817` 验收场景，以及 E 区关闭/开启同镜头候选预览。两态运行轮廓 IoU 为
`0.995725`，自动验证通过；仍需用户确认阶梯在全图中的视觉体量。
正式 `48×64` 坐标仍未冻结，因此这些地标均未写入正式 Map01 场景。

进度注记（2026-08-20）：已新增独立候选构图场景
`scenes/maps/map_01_d1_landmark_preview.tscn`，在不改环境格、D0 场景、运行 Marker 和产品数据的前提下，
按旧候选锚点叠加万修之门、三灯、残碑、废营、暗道三态组件与东壁阶梯两态组件。共享格冲突的候选拆分为：尸首保留
`(31,49)`、`m1_g03` 移至 `(31,45)`；阶梯保留 `(39,31)`、`m1_e01` 移至 `(36,29)`。
四个位置均为可走、非碰撞格，但在产品正式冻结前仍统一标记 `UNVERIFIED`，不得写入运行场景或任务数据。

进度注记（2026-08-21）：七个共用 Overlay Marker 的尺寸、命名和锚点已冻结；已从当前
`375×817` Tile 重拼视窗制作 Map01 `5×5` 尺度/迷雾参考、明确标注“非 Map04”的深色对比测试板、
项目小图标风格合成参考和七类尺寸锚点规格板，并整理第一张 `marker_explore_party.png` 的逐张生成提示词。
这些文件只用于生成和视觉 Gate，不是运行素材；正式地图、运行 Overlay 和对象坐标尚未修改。

完成口径：关键地标和 Overlay 在正常、已发现、可交互、已处理和锁定状态下语义明确，未知迷雾不泄露对象。

### Gate 8：1.0 全图内容依赖，不计入当前 D1 完成

进入条件：D1 PASS、正式项目解冻、新版固定对象坐标与内容表重新冻结。

坐标审计（2026-08-20）：旧候选 `(21,52)/(38,39)/(15,20)` 及其领域坐标换算只保留在
`art/candidates/map01_layout/map01_landmark_coordinate_audit_20260820.json`。当前灰盒仍有
`(31,49)` 与 `(39,31)` 两处共享格冲突，解决并经产品冻结前不得写入正式场景。

- [ ] 将 `scenes/maps/map_01.tscn` 扩展到正式 `48×64` 布局，并保持场景坐标为唯一布局事实源。
- [ ] 冻结并接入 `10` 组普通敌、`2` 个精英、`1` 个 Boss、`8` 个资源点、关键事件、
  山腹暗道副本和永久捷径；不使用旧对象表候选坐标冒充正式坐标。
- [ ] 在 `data/maps/map_01_demo.json` 的正式后继配置中维护对象文案、奖励、遭遇和任务语义，
  不把这些内容复制进 Marker 或纯表现节点。
- [ ] 接入顾南炉救援、三灯聚合条件、精英捷径、Boss 失败恢复、门状态和 `map_02` 出口幂等结算。
- [ ] 完成敌人/精英/Boss 世界棋子、处理后场景状态、关键 VFX、环境声和 Boss 区表现。
- [ ] 完成完整流程视觉验收后，才允许启动 `map_02` 独立灰盒。

完成口径：正式 Map01 的布局、内容、状态恢复、视觉与结算同时通过 1.0 验收；D0/D1 灰盒不得被误标为正式终稿。
