# 《昆吾禁地》技术实现方案

版本：0.3  
日期：2026-08-02  
目标：竖屏2D、Web首发、单机数据优先，支撑0.1 Demo与1.0前四章正式版本

> IP 边界：本文中的“昆吾山、万修之门、昆吾殿、灵宝阁、镇魔塔”等名称仅用于内部原型和授权版设计说明。未取得《凡人修仙传》相关权利人的书面授权前，不得以这些专有名称、剧情、人物或法宝进行公开发行、商业宣传、众筹或收费测试。代码和数据统一使用中性 ID，发行时可切换为原创名称。

## 1. 技术目标

本项目不是动作游戏，而是由三种交互形态组成：

1. 山外营地：建筑菜单、资源生产、招募、培养、装备、NPC 对话。
2. 禁地方格地图：竖屏俯视、四方向逐格移动、迷雾、补给与负重管理。
3. 半自动战斗：四人小队，依靠职业搭配、装备、技能策略和手动技能时机取胜。

Demo 的技术成功标准：

- Chrome、Edge、Firefox 和移动端主流浏览器中稳定运行；玩家可见UI以`375×817`为唯一设计基准，
  720×1280与1080×1920只作为适配验收尺寸。
- 冷启动进入营地不超过5秒，移动端低画质战斗稳定30 FPS。
- 0.1 Demo完成“营地生产 → 固定四人编队 → 探索 → 战斗 → 回城结算”闭环；招贤馆保持锁定。
- 1.0底层支持`map_01`至`map_04`四张完整大地图、四个初始职业和八条筑基分支；`map_05`
  只保留锁定预告，不加载第五张地图内容。
- 所有数值从数据文件读取，不在界面或战斗代码中硬编码。
- 存档可升级、可校验，异常退出不损坏主存档。
- Web 首次下载的压缩资源目标小于 25 MB，进入可操作状态前不加载未使用章节的大图集和音频。

## 2. Unity、Cocos Creator、Godot 对比

| 维度 | Unity | Cocos Creator | Godot |
|---|---|---|---|
| 主要语言 | C# | TypeScript | GDScript / C# / C++ |
| 2D 工作流 | 完整，Tilemap、2D Animation、URP 2D 成熟 | 轻量，UI 与移动端 2D 高效 | 原生 2D 节点体系，TileMapLayer、动画、粒子直接 |
| 竖屏 UI | 成熟，工具与插件多 | 很强，接近前端开发体验 | 足够，Control/Container 需要团队建立规范 |
| 包体与启动 | 相对较重 | 较轻 | 较轻 |
| 标准 Web | WebGL/Wasm 成熟，包体偏大 | Web Mobile/Web Desktop 是核心平台，TypeScript 运行路径直接 | GDScript + Compatibility 可导出，Wasm 初始体量需控制 |
| 移动端/小游戏 | Android/iOS 成熟；小游戏常依赖第三方方案 | 微信等小游戏生态更顺手 | 浏览器/PWA 可用；国内小游戏需额外适配 |
| PC/Steam | 很强 | 可行但不是主战场 | 后续封装很合适 |
| 动画生态 | Spine、Live2D、DragonBones 等集成成熟 | Spine/DragonBones/帧动画便利 | 帧动画与骨骼动画够用，商业中间件支持弱于 Unity |
| 特效 | URP 2D、Shader Graph、VFX 生态强 | 2D 粒子、材质够用 | GPUParticles2D、CanvasItem Shader 足够本项目 |
| 商店/插件 | 最大 | 国内移动端资源较多 | 社区插件较少，质量差异较大 |
| 学习和迭代 | 功能多、工程管理成熟，编辑器较重 | Web 开发者上手快 | 小团队迭代快，节点/场景结构清晰 |
| 授权与成本 | 需持续关注 Unity 商业条款和版本政策 | 引擎开源；云服务和第三方服务另行评估 | MIT，无版税；发行时保留许可证声明 |
| 主机平台 | 官方商业生态成熟 | 弱 | 依赖获批后的第三方移植商 |
| 本项目适配度（Web 首发） | 6.5/10 | 9/10 | 8/10 |

### 2.1 结论

主选 **Cocos Creator 3.8 LTS + TypeScript + WebGL**。

原因：

- Cocos Creator 的 Web Mobile 构建原生支持竖屏铺满浏览器，TypeScript 代码直接进入浏览器运行路径。
- Asset Bundle 能把场景、贴图、脚本和音频按地图拆包，首屏只加载营地与地图 1，未来增加第 6–30 张地图时不会线性放大首次下载。
- UI、Sprite、Animation、TiledMap、ParticleSystem2D 和自定义材质足以覆盖纯 2D 像素项目。
- TypeScript 便于写数据 Schema、领域模型、浏览器 IndexedDB 存档和 Web Shell 集成。
- 现有 PNG、Aseprite、Tiled 与字体素材可直接进入 Cocos Creator 工作流。

下列情况应改选 Unity：

- 首发即包含主机平台。
- 确定采用 Spine/Live2D 大量骨骼动画并需要成熟商业支持。
- 团队已有稳定 Unity/C# 代码库和 CI/CD。
- 需要接入多个国内商业 SDK，且供应商只提供 Unity 插件。

下列情况可以改回 Godot：

- 团队更熟悉 GDScript 和 Godot 场景节点。
- Web 只是短期 Demo，正式版明确转向 PC/Steam 单机。
- 不需要按地图远程分包，也不接浏览器账号、支付或网页容器。

本项目的“Web”指标准浏览器 Web Mobile 构建，不等同于微信小游戏。不过选用 Cocos Creator 后，未来转小游戏平台的迁移成本也低于 Godot。

参考：[Cocos Creator 3.8 LTS 文档](https://docs.cocos.com/creator/3.8/manual/zh/)、[发布到 Web](https://docs.cocos.com/creator/3.8/manual/en/editor/publish/publish-web.html)、[Asset Bundle](https://docs.cocos.com/creator/3.8/manual/zh/asset/bundle.html)、[Godot Web 导出](https://docs.godotengine.org/en/stable/tutorials/export/exporting_for_web.html)、[Unity 2D 文档](https://docs.unity3d.com/Manual/Unity2D.html)

## 3. 平台与画面技术规格

### 3.1 平台顺序

1. Demo：桌面 Chrome/Edge Web。
2. 1.0：响应式Web + PWA，兼容桌面与Android浏览器。
3. 商业版：视运营需求封装 Windows/Android；Steam 和 iOS另行评估。
4. 微信小游戏、主机和原生支付不纳入1.0。

### 3.2 显示规格

- UI设计稿与Web外层可视窗口：`375×817`，是唯一玩家可见设计基准；大厅全景仍是可横滑的宽内容。
- `1080×1920`只作为旧Cocos工程坐标与适配验收尺寸，不作为新UI布局来源。
- 像素游戏内部参考画布：360×640，最终整数倍放大 3 倍。
- 桌面浏览器：游戏容器保持375:817并在窗口中居中，高度优先占满，宽度按比例计算，视窗外使用纯黑页面背景。
- 移动浏览器：同样保持375:817并在可用视口内等比缩放；继续处理刘海、底部手势区和地址栏伸缩。
- Web Shell 只改变外层可视窗口，不得缩放大厅全景内容宽度；大厅、地图等超宽内容由 Cocos 内部 ScrollView/横滑逻辑展示。
- 世界 Tile：16×16 像素；逻辑显示为 48×48 屏幕像素。
- 探索人物：32×32 或 48×48 源帧。
- 战斗人物：48×48 至 96×96 源帧。
- 纹理过滤：Nearest；禁止对 Pixel Art 开启线性过滤。
- UI 安全区：左右各 36 px，上下各 54 px，并适配刘海屏。
- 默认 60 FPS；后台或低电模式允许 30 FPS。

### 3.3 Web 构建约束

- 构建目标使用 Cocos Creator `Web Mobile`，方向设为 Portrait。
- 渲染后端使用成熟的 WebGL 路径；不把实验性 WebGPU 作为首发依赖。
- 逻辑默认运行在主线程；大规模数值模拟确有性能问题时再引入 Web Worker，不预先增加跨线程复杂度。
- 正式地址必须使用 HTTPS。
- 浏览器切换后台时暂停渲染和战斗表现，但领域计时使用可信时间补算。
- 页面缩放、横竖屏变化和浏览器地址栏伸缩不得改变游戏逻辑坐标。
- Web 容器显示加载进度、版本号、静音按钮、全屏按钮、存档导入/导出。
- 不直接修改每次构建生成的 HTML；使用自定义构建模板管理 Web Shell。

### 3.4 Web 发布产物

```text
dist/web/
├─ index.html
├─ application.js
├─ cocos-js/
├─ src/
├─ assets/
├─ remote/
├─ manifest.webmanifest / service worker（项目自建 PWA）
└─ legal/
```

静态托管需设置正确 MIME 类型、缓存策略和 HTTPS。带哈希的版本资源使用长缓存，`index.html` 和版本清单不使用长缓存。

### 3.5 Asset Bundle 分包

```text
start-scene：启动页、加载 UI、最小字体
shared：通用 UI、资源图标、四人战斗公共组件
camp：山外营地与七座建筑
map_01：地图 1 的 Tile、敌人、音乐、事件
map_02 ... map_04：各自独立
career_base：四个初始职业
career_tier_1：八个筑基分支
```

- 首屏只包含 `start-scene`、必要的 `shared` 和营地最小资源。
- 进入营地后后台预载 `map_01`。
- 玩家接近有效出口时按`map_01→map_02→map_03→map_04`预载目标地图Bundle；不存在地图2
  直达地图4的隐藏出口，`map_05`锁定预告不得触发地图Bundle预载。
- 后续第 6–30 张地图采用相同规则，无需进入主包。
- Bundle 使用版本 Hash，构建后上传新版本时保留上一版本一段时间，避免 HTML 与远程资源短暂不一致。
- 地图 Bundle 之间不直接引用脚本；共享脚本放入主代码或 `shared`，避免远程脚本加载顺序问题。

## 4. 工程结构

```text
KunWu/
├─ assets/
│  ├─ bundles/
│  │  ├─ camp/
│  │  ├─ map_01/
│  │  ├─ map_02/
│  │  └─ shared/
│  ├─ scripts/
│  │  ├─ domain/
│  │  ├─ services/
│  │  ├─ repositories/
│  │  ├─ presentation/
│  │  └─ tests/
│  ├─ data/
│  │  ├─ localization/
│  │  ├─ balance/
│  │  ├─ careers/
│  │  ├─ maps/
│  │  └─ schemas/
│  ├─ art/
│  ├─ audio/
│  ├─ fonts/
│  ├─ vfx/
│  └─ third_party/
├─ build-templates/
│  └─ web-mobile/
├─ settings/
├─ Docs/
└─ ThirdParty/
```

`library/`、`temp/`、`local/`、`build/` 为生成目录，不提交 Git。

### 4.1 持久服务节点

| 单例 | 职责 | 禁止承担的职责 |
|---|---|---|
| `AppRoot` | 持久根节点、场景切换、全局生命周期 | 存放所有游戏数据 |
| `GameState` | 当前存档的聚合状态 | 直接操作 UI |
| `DataRegistry` | 加载静态表并提供只读查询 | 修改玩家存档 |
| `SaveService` | 原子写入、备份、版本迁移 | 业务计算 |
| `EventBus` | 跨场景领域事件 | 高频逐帧广播 |
| `AudioService` | BGM、环境音、音效通道 | 关卡逻辑 |
| `TimeService` | UTC 时间、离线收益、防回拨策略 | 直接发奖励 |
| `SceneRouter` | Cocos Scene/Prefab 页面堆栈、过渡、返回规则 | 业务状态 |

`AppRoot` 使用 `director.addPersistRootNode()` 保持跨场景存在。业务逻辑尽量写成无引擎依赖的普通 TypeScript 类，由 Component 调用；避免把整个项目堆进静态单例。

## 5. 核心领域模型

```text
Profile
├─ Wallet：灵粮、灵木、玄铁、灵晶、庚精、灵石、魂晶
├─ CampState：建筑等级、工匠分配、生产结算点
├─ Roster：修士实例、等级、境界、装备、伤亡状态
├─ Inventory：道具堆叠、装备实例
├─ StoryState：章节、任务、NPC、设施解锁
├─ ExpeditionState：地图、坐标、补给、负重、迷雾、临时战利品
└─ Settings：语言、音量、振动、画质、辅助选项
```

为兼容既有存档，`Wallet.spiritStone` 的产品显示名为“灵晶”，`Wallet.immortalCoin` 的产品显示名为“灵石”；字段名不随显示名迁移。

静态定义与运行实例分开：

- `HeroDefinition`：职业、成长曲线、技能池、动画集。
- `HeroInstance`：唯一 ID、资质、等级、境界、随机属性、装备。
- `CareerDefinition`：初始职业、七维成长权重、基础三技能。
- `CareerNodeDefinition`：转职条件、父职业、替换后的三技能、后续转职节点。
- `ItemDefinition`：名称键、图标、类型、堆叠上限。
- `EquipmentInstance`：词条、耐久（若启用）、强化级别。
- `MapDefinition`：尺寸、图块、POI、出生点、遭遇表。
- `ExpeditionState`：某次入山中已揭露格、当前补给和临时掉落。

## 6. 数据驱动

策划主表使用 CSV，复杂嵌套配置使用 JSON；构建前转换为带 TypeScript 类型声明的只读数据，并执行 Schema 校验。

建议主表：

| 表 | 主键示例 | 关键字段 |
|---|---|---|
| `heroes.csv` | `hero_sword_001` | 职业、资质权重、成长、动画集 |
| `careers.json` | `career_warrior_guard` | 父职业、阶段、成长、三技能、转职条件 |
| `skills.csv` | `skill_sword_pierce` | 冷却、目标规则、倍率、效果 ID |
| `buildings.csv` | `camp_recruit_hall` | 等级、成本、解锁、功能参数 |
| `items.csv` | `mat_spirit_wood` | 类型、品质、堆叠、售价 |
| `recipes.csv` | `recipe_blade_t1` | 输入、输出、工坊等级 |
| `enemies.csv` | `enemy_corpse_01` | 属性、AI、掉落组 |
| `encounters.csv` | `enc_gate_common` | 敌群、权重、条件 |
| `quests.json` | `quest_ch01_gate` | 状态机、目标、对话、奖励 |
| `maps/*.json` | `map_ch01_gate` | 图层、格子、POI、出口 |
| `exploration_checks.csv` | `check_map02_seal_01` | 属性、聚合方式、阈值、成功/失败结果 |
| `localization.csv` | `ui.camp.depart` | zh-CN、后续语言 |

ID 一律英文小写蛇形，不将显示名称用作逻辑判断。例如：

```json
{
  "location_id": "loc_sealed_main_hall",
  "display_name_key": "location.authorized.kunwu_hall",
  "fallback_name_key": "location.original.celestial_vault_hall"
}
```

职业节点示例：

```json
{
  "career_id": "career_warrior_guard",
  "parent_id": "career_warrior_base",
  "tier": 1,
  "required_level": 10,
  "attributes_growth": {
    "strength": 1.2,
    "magic": 0.2,
    "technique": 0.6,
    "speed": 0.5,
    "constitution": 1.5,
    "armor": 1.4,
    "resistance": 0.7
  },
  "skills": [
    "skill_shield_bash",
    "skill_taunt",
    "skill_counter_roar"
  ],
  "next_nodes": []
}
```

七维字段固定为 `strength`、`magic`、`technique`、`speed`、`constitution`、`armor`、`resistance`。数据校验必须保证每个职业节点恰好配置三个有效主动技能。

## 7. 营地与离线生产

生产只记录状态，不每 30 秒永久运行一个 Timer。

结算公式：

```text
有效秒数 = clamp(当前服务器/本地可信时间 - 上次结算时间, 0, 离线上限)
周期数 = floor(有效秒数 / 当前生产周期)
实际产量 = 周期数 × 岗位人数 × 单周期产量 × 增益
灵粮净变化 = 灵粮产出 - 其他岗位灵粮维护
```

实现要求：

- Demo 使用本地 UTC；检测系统时间倒退，大于 5 分钟时暂停离线收益并提示。
- 1.0无服务器时将灵源院离线上限设为8小时，并检测时间回拨，避免改系统时间无限刷资源。
- 每次进入营地、修改工匠、退出游戏时结算。
- 先结算旧配置，再保存新分配，防止切换岗位吞产量。
- 资源使用 64 位整数；所有乘法在整数域完成。

## 8. 招贤馆

刷新状态包括：

```text
offers[3]
next_free_refresh_at
paid_refresh_serial
refresh_seed
```

- 三个栏位由确定性随机种子生成，重启游戏不能反复读档刷结果。
- 每个自然整点生成新批次；付费刷新不改变`next_free_refresh_at`，也不因整点到达返还费用。
- 招募后对应栏位标记为空，不立刻补位。
- 候选由职业、形象属性模板、灵根和七维随机依次生成；模板与灵根使用独立随机抽取。
- 0.1 Demo保持入口锁定；1.0使用灵石付费刷新和招募，不接充值或高级付费货币。

## 9. 方格地图与迷雾

### 9.1 坐标与移动

- 使用自定义不可变 `GridCoord { x: number; y: number }`，不把世界像素坐标与格子坐标混用。
- 四方向移动，不允许斜走。
- Tile 数据存地形、阻挡、移动消耗、危险度、视觉标签。
- 寻路使用 A*；长按目的地只在已知可行走格中寻路。
- 每走一步按地形扣除灵粮，并检查遭遇、POI、负重和队伍状态。
- 灵粮归零后仍由领域层持久推进四步衰竭计数；第 4 步先保存移动结果，表现结束后
  原子结算全队阵亡，表现层不得自行修改修士存活状态。
- 每张野外地图是一张完整开放大图，不由“主路径/支路”组成；队伍只占其中一个格子。
- 敌人、资源、副本入口、剧情事件、Boss 与跨图出口均有固定坐标。
- 被击败、采集或完成后的固定对象记录自身状态；若允许刷新，也只在原固定坐标刷新。
- 地图可以存在主线前进出口与已解锁的返回捷径；1.0不允许配置地图2直达地图4或地图4
  隐藏返回地图2的越序出口。

### 9.2 迷雾状态

每格三态：

1. `UNKNOWN`：完全黑暗，不显示内容。
2. `DISCOVERED`：曾经看过，显示暗化地形，不显示动态怪物。
3. `VISIBLE`：当前神识/探灵灯范围内，显示完整内容。

视野采用整数圆：

```text
dx² + dy² <= radius²
```

基础半径2，探灵灯升级后为3/4/5。障碍物遮挡视线按1.0地图配置实现；0.1 Demo仅使用半径。

### 9.3 地图存档压缩

- 小地图直接使用位数组保存已探索格。
- 动态对象只保存与初始定义不同的状态，如“宝箱已开”“Boss 已死”。
- 固定对象使用 `map_id + object_id` 保存；不得依赖随机种子重排位置。
- 同一地图只允许一个Boss状态；剧情强敌与高危强敌使用精英对象状态，不能注册为第二Boss。

### 9.4 探索属性检定

七维属性为力量、魔力、技巧、速度、体质、护甲、抗性。探索事件主要检定前三维，但数据结构允许使用任意属性。

聚合方式：

- `SUM`：四人属性总和。
- `MAX`：队内最高单人属性。
- `COUNT`：达到单人门槛的人数。
- `ALL`：每名存活成员都达到门槛。

```json
{
  "check_id": "check_map02_small_ruin_technique",
  "attribute": "technique",
  "aggregate": "SUM",
  "threshold": 30,
  "on_success": [{"type": "open_event", "id": "event_safe_route"}],
  "on_failure": [{"type": "party_hp_percent", "value": -20}]
}
```

检定结果由领域层结算，地图 UI 只展示需求和结果。百分比掉血至少保留 1 点生命，除非事件文字明确说明可能致死。

## 10. 战斗架构

战斗采用固定时间步模拟，显示与模拟分离：

- 模拟 Tick：20 Hz。
- 渲染：60 FPS。
- 战斗单位最多 4 名友方、6 名普通敌人或 1 名大型 Boss + 2 个召唤物。
- 所有技能先生成 `CombatCommand`，再由结算器处理。

结算顺序：

```text
读取行动意图
→ 检查控制与资源
→ 选择目标
→ 命中/暴击
→ 伤害或治疗
→ 附加状态
→ 触发被动与反击
→ 生成表现事件
→ 死亡检查
```

核心接口：

```typescript
export interface CombatCommand {
  actorId: number;
  skillId: string;
  targetIds: number[];
}

export interface CombatEvent {
  type: string;
  payload: Readonly<Record<string, unknown>>;
}
```

动画和特效只消费 `CombatEvent`，不得反向决定伤害结果。这样可以加速战斗、跳过动画、战斗回放和自动化测试。

### 10.1 七维属性结算

```text
生命上限 = 职业基础生命 + 体质 × 生命系数 + 装备生命
物理技能基值 = 力量 × 力量倍率 + 技巧 × 技巧副倍率
法术技能基值 = 魔力 × 魔力倍率 + 技巧 × 技巧副倍率
行动间隔 = clamp(技能基础间隔 × 100 / (100 + 速度), 最小间隔, 最大间隔)
物理减伤率 = 护甲 / (护甲 + 等级常数)
法术减伤率 = 抗性 / (抗性 + 等级常数)
```

每个技能明确声明依赖属性，不能根据职业名称在代码中推断。

### 10.2 转职数据

- 每个职业节点始终提供 3 个主动技能槽。
- 转职不是在旧技能上无限追加，而是进入新职业节点，并由该节点定义新的三技能。
- 1.0不使用自由技能继承；炼气进入筑基时整体替换三个技能，结丹只增强同一批技能数值。
- 转职树按父ID组织，核心代码不限制层数；1.0实装四个初始职业、八条筑基分支和结丹进阶。

## 11. AI 与技能策略

修士使用规则列表：

```text
若最低友方生命 < 35% → 单体治疗
若友方存活数 ≥ 3 且平均生命 < 60% → 群体治疗
若敌方有护盾 → 破法
否则 → 普攻
```

敌人使用轻量状态机：

- Idle
- ChooseAction
- Cast
- Recover
- Disabled
- Dead

Boss 额外使用按生命百分比和回合时间驱动的阶段脚本，不做通用行为树。

### 11.1 无前后排目标选择

- 四名队员在规则上没有前排和后排。
- 普通单体攻击从所有存活目标中按权重选择。
- 嘲讽生效时，遵守嘲讽的单体技能强制选择嘲讽者。
- 群体技能、随机多段技能以及标记为 `ignore_taunt` 的技能不受嘲讽影响。
- 若同时存在多个嘲讽者，则选择嘲讽强度最高者；相同强度再随机。
- 美术上的左右站位只用于防止重叠，不参与命中概率和承伤。

## 12. UI 架构

- 页面层：营地、建筑、地图、战斗。
- 模态层：入山整备（编队/装载/地图选择）、确认、掉落、角色详情、教程。
- 全局层：资源栏、Toast、加载遮罩、网络/存档错误。
- 所有页面采用 Presenter/ViewModel，把格式化逻辑与领域模型隔离。
- 按钮最小触控尺寸 48×48 dp。
- 关键操作必须二次确认：解雇高资质修士、消耗高级货币、覆盖存档。
- 颜色不是唯一信息载体；灵根资质同时使用边框形状和文字。

### 12.1 API First 与前后端边界

新需求和被修改的旧页面统一按未来存在独立服务端设计，客户端固定调用链为：

```text
Presenter → Application Service → API Port
          → Local Adapter（当前）/ HTTP Adapter（未来）
          → Response → Service 更新 GameState、持久化并发出应用事件
          → Presenter 刷新 ViewModel
```

- Presenter 只管理页面生命周期、用户意图和 ViewModel 渲染，不读写 `GameState`、
  不拼装服务端 DTO，不直接调用 `fetch`。
- API Port、请求/响应 DTO 和错误类型独立于 HTTP；Local Adapter 与 HTTP Adapter
  实现同一 Port，通过 Composition Root 注入。
- Local Adapter 保持异步 `Promise` 语义，并可模拟业务失败、超时、断网和冲突；
  它不操作 Cocos 节点，也不绕过 Application Service 修改客户端权威镜像。
- 账号、付费货币、交易、排行榜和联网战斗结算默认服务端权威；页面开关、
  动画、音效和本地展示偏好由客户端负责。
- 进入实现前必须同步产出并互相链接：客户端技术设计、服务端技术设计、
  API 契约、Local Adapter 实现与正常/超时/断网/重复提交/冲突/失败恢复验收清单。

顶部 HUD、底部 HUD 与灵源院的首个完整实例见：

- [Demo 客户端技术设计](Demo/Tech/P1_HALL_ECO_客户端技术设计.md)
- [Demo 服务端技术设计](Demo/Tech/P1_HALL_ECO_服务端技术设计.md)
- [Demo API 契约](Demo/API/P1_CAMP_HUD_LING_PU_API.md)
- [Demo 本地适配器与验收](Demo/Tech/P1_HALL_ECO_本地适配器与验收.md)

## 13. 存档与版本迁移

存档使用浏览器 IndexedDB：

```text
Database: kunwu_game
Object stores:
├─ profiles
├─ backups
├─ settings
└─ telemetry_local
```

用户清理网站数据、无痕模式退出或浏览器回收存储空间都可能导致存档丢失，因此必须提供手动导出/导入：

- 导出：生成带版本与校验的 `.kwsave` 文件并由浏览器下载。
- 导入：文件选择器读取、校验、预览档案时间后覆盖。
- PWA 首次安装后请求持久存储权限；不能假设浏览器一定批准。

写入流程：

1. 序列化并计算 SHA-256 或 HMAC 校验。
2. 将旧主档内容写入 `.bak`。
3. 在同一个 IndexedDB transaction 中写入新主档。
4. 加载失败时自动尝试备份并记录错误。

顶层字段：

```json
{
  "schema_version": 3,
  "game_version": "1.0.0",
  "saved_at_utc": 1785330000,
  "checksum": "...",
  "payload": {}
}
```

每次改变 Schema 都新增迁移函数，不直接修改历史迁移。

## 14. 音频、动画与特效接入

- 音频总线：Master、Music、Ambient、SFX、UI。
- 动画通过 Cocos `Animation`、`AnimationClip` 和 SpriteFrame 事件，技能命中帧发出表现信号。
- 粒子与屏幕震动由 `VfxService` 统一创建并池化。
- 不允许技能脚本直接加载路径；从 `EffectDefinition` 查询表现资源。
- 低画质关闭环境粒子、热扭曲和部分动态光。

## 15. 性能预算

| 指标 | 0.1 Demo / 1.0预算 |
|---|---:|
| Web 常驻内存 | 桌面 < 450 MB；移动浏览器 < 300 MB |
| 首次压缩下载 | 0.1 Demo <25MB；1.0 <60MB |
| 首次可交互 | 宽带环境 < 5 秒 |
| 同屏粒子 | 高画质 < 1500；低画质 < 500 |
| Draw Call | 地图 < 120；战斗 < 160 |
| 单帧脚本 | 平均 < 5 ms |
| 存档 | < 2 MB |
| 地图加载 | < 2 秒 |
| 战斗对象 | < 64 个活跃场景节点 |

大图集按场景拆分，不建立覆盖全游戏的巨型 Atlas。重复实例使用对象池，但不要过早池化普通 UI。

## 16. 测试策略

### 16.1 单元测试

- 生产结算与灵粮不足。
- 酒馆概率和保底。
- 负重与补给扣除。
- 视野半径。
- 伤害、治疗、暴击、状态叠加。
- 掉落权重。
- 存档迁移。

### 16.2 仿真测试

- 运行 10 万次酒馆刷新，误差符合概率区间。
- 运行 1 万场自动战斗，检查死循环和极端胜率。
- 模拟 30 天离线结算，验证上限与时间回拨。
- 随机生成移动路径，验证灵粮不会扣为负数，断粮四步上限不能被绕过或无限行走。

### 16.3 验收设备

- Windows：Chrome、Edge、Firefox，1080p 集显笔记本。
- macOS：Safari 与 Chrome。
- Android：Chrome，4 GB RAM、720p 和 6–8 GB RAM、1080p。
- iOS：Safari，至少验证当前与前一主版本。
- 验证普通标签页、PWA、后台恢复、清理缓存后的存档导入。

## 17. Git 与内容协作

- Git LFS 跟踪 `.aseprite`、`.psd`、`.wav`、大 PNG。
- 禁止提交 Cocos Creator 的 `library/`、`temp/`、`local/` 和构建输出。
- 分支：`main`、`develop`、`feature/*`、`content/*`。
- 数据表修改必须经过 Schema 校验和数值负责人审核。
- 第三方素材不得混入原创目录，始终保留来源、许可快照和哈希。

## 18. 研发排期建议

| 阶段 | 时间 | 技术交付 |
|---|---:|---|
| 第 1–2 周 | 2 周 | 工程骨架、Web Shell、数据加载、IndexedDB 存档、竖屏 UI |
| 第 3–4 周 | 2 周 | 营地生产、建筑、招募、背包 |
| 第 5–6 周 | 2 周 | 方格地图、迷雾、寻路、补给与负重 |
| 第 7–8 周 | 2 周 | 四人战斗、技能、敌人、掉落 |
| 第 9 周 | 1 周 | 剧情/任务、Boss、完整闭环 |
| 第 10 周 | 1 周 | 性能、存档兼容、打包、Demo 验收 |

## 19. 技术 Definition of Done

- 新修士、新技能、新物品、新地图可只改数据和资源，不改核心代码。
- 强制退出后主档可恢复。
- 所有核心数值有自动测试。
- Web Demo 可在桌面主流浏览器完成 30–45 分钟垂直切片。
- 不联网也能完成全部1.0内容。
- 首次联网加载后，PWA 模式可在缓存未被清理时离线启动。
- 授权名与原创名通过本地化表切换，不影响存档 ID。
- Credits 包含 Cocos Creator/引擎要求的声明与全部第三方素材许可。
