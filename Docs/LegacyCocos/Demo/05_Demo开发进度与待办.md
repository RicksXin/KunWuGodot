# 《昆吾禁地》Demo 开发进度与待办

最后更新：2026-08-07
当前目标：先完成 D0——可从 localhost 实际操作的 15–20 分钟核心闭环。

> 本文件是 Demo 当前进度的唯一事实源。正式项目阶段进度见
> [`Docs/08_开发进度与待办.md`](../08_开发进度与待办.md)，Demo 范围与停止条件见
> [Demo 范围与体验闭环](01_Demo范围与体验闭环.md)。

本文按玩家页面组织需求。技术层、领域层和单元测试是页面交付的前置，不再单独视为“功能已完成”。

---

## 0. 当前结论

当前代码流程已接通到首张 D0 地图，等待用户完成 Cocos 与浏览器人工验收：

```text
Boot 启动页 → Camp 营地大厅 → 入山整备 → map_01 破禁山麓
→ 固定遭遇战灰盒 → 返回地图或战败返回营地
```

营地当前为三层横滑大厅：顶部真实资源与主线、中部建筑/NPC、底部系统入口和灵石余额均已接线。灵源院已接入岗位分配、在线生产、杂役招募、资源储量升级与存档；其余建筑入口继续显示锁定或未开放反馈。入山整备、地图探索与固定遭遇战灰盒已完成代码交接，等待用户刷新 Cocos 资源并进行浏览器人工验收。

最近一次已记录的工程健康状态（本轮 Schema v5 与 Camp Prefab 拆分后待用户复验）：

- TypeScript 类型检查通过。
- 678 个单元测试通过。
- Boot、数据与 Bundle 基础可运行；Camp 模块拆分已完成，场景门禁等待用户复验。
- P0 自动门禁 11/11 通过。
- Web 构建产物约 8.2 MB，低于 25 MB 预算。
- P0 技术条目 20 项完成 18 项，剩真机适配和人工评审。

### 页面状态口径

| 状态 | 含义 |
|---|---|
| ✅ 可体验 | 能从 localhost 进入、操作、产生状态变化并正常返回 |
| 🟡 已有基础 | 已有实现，但尚未完成用户人工验收或仍有阶段缺口 |
| ⬜ 未开始 | 页面及其核心逻辑均未实现 |

### 页面总览

| 页面模块 | 当前状态 | 已有内容 | 主要缺口 |
|---|---|---|---|
| 启动与全局框架 | ✅ | Boot、Bundle、新档与存档、全局路由、加载遮罩、错误兜底 | 后续页面复用全局能力 |
| 营地大厅 | ✅ | 三层横滑、真实资源与主线、建筑/NPC、底部系统入口、灵石余额及独立 Prefab | 场景门禁与视觉回归复验 |
| 建筑与生产 | ✅ | 灵源院生产弹窗、调岗、30 秒在线结算、招募、储量升级与存档 | 后续建筑与 P2 离线收益 |
| 入山整备与编队 | 🟡 | 主面板已按 Approved Figma 交付；四槽位、石岩自然呼吸动画与其余静态立绘、选人、队伍切换/解锁、灵息、负重与三类装载物已接线 | 用户在 Cocos 中导入新图集并人工确认动画、视觉、触控与二级面板返回层级 |
| 背包与装备 | ⬜ | 只有导航占位 | 页面、物品数据、装备交互 |
| 任务 | ⬜ | 只有导航占位 | 页面、任务数据和状态流 |
| 地图探索 | 🟡 | Map 场景、CC0 Tile、动画移动、迷雾、事件、宝箱、遭遇、战斗交接、返营与刷新恢复均已接线 | 用户人工验收 |
| 战斗 | 🟡 | 固定遭遇、20 Hz 时间轴、手动/自动选技、单位灰盒表现、逃生与胜负结算均已接线 | Cocos 资源导入和用户人工验收；D1 正式美术 |
| 归营结算与转场 | 🟡 | 原子入库、魂晶奖励、纯黑缓动切回大厅 | 用户人工验收 |
| 设置与存档 | 🟡 | IndexedDB、备份、迁移、浏览器生命周期 | 设置页面、导入导出入口 |

---

## 1. 当前最高优先级：localhost 最小可玩闭环

目标流程：

```text
新档进入营地
→ 分配杂役生产
→ 在入山整备面板中查看并编成修士
→ 携带灵粮入山
→ 地图移动并揭开迷雾
→ 触发一场固定战斗
→ 获胜并回城结算
→ 刷新页面后进度仍在
```

完成这个闭环前，不优先增加新的独立系统、职业、地图或测试规模。

### 1.1 启动与全局框架

- [x] Boot 场景可启动并加载 shared、camp Bundle。
- [x] IndexedDB 主档、备份、校验和迁移基础。
- [x] 启动失败、WebGL、断网和浏览器生命周期反馈。
- [x] 无主档时创建默认 Profile：初始资源、营地状态、四名修士。
- [x] 将默认 Profile 写入 GameState 并立即刷新营地资源栏。
- [x] 创建唯一的全局 CocosSceneRouter 实例。
- [x] 把场景加载中状态接到全局遮罩，防止重复点击。
- [x] 尚未开放的入口显示明确提示，禁止出现“点了没反应”。

完成判定：首次打开和刷新页面都能进入有效存档，所有可见入口都有反馈。

验收记录（2026-07-30）：localhost 首次进入显示新档 120 灵粮，刷新后数值与营地状态恢复；七座建筑和五个导航共 12 个入口均有可见反馈，控制台无 warning/error。

### 1.2 营地大厅

目标布局与交互以 PRD-01 v0.2 为准。现有“七座建筑平铺 + 底部营地/修士/背包/任务/入山五导航”仅是旧版灰盒，需要替换，不能在旧结构上继续补入口。

已有 API First 改造以 [客户端技术设计](Tech/P1_HALL_ECO_客户端技术设计.md)、
[服务端技术设计](Tech/P1_HALL_ECO_服务端技术设计.md)、
[API 契约](API/P1_CAMP_HUD_LING_PU_API.md) 和
[本地适配器与验收](Tech/P1_HALL_ECO_本地适配器与验收.md) 为准；Demo 后续模块不强制
复制这套 API First 结构。

#### 1.2.1 现有可复用基础

- [x] Camp 场景、资源栏、建筑节点、按钮反馈和红点逻辑基础。
- [x] Ark Pixel 字体、灰盒面板、本地化和提示图标。
- [x] Wallet 变化事件和大厅 Presenter 基础接线。
- [x] 清理旧的底部五主导航布局；保留可复用的 Button、反馈、路由与最小触控区逻辑。

#### 1.2.2 三层页面骨架

- [x] 将大厅拆成顶部固定 HUD、中央横滑全景、底部固定 HUD 三层；上下 HUD 不随全景移动。
- [x] 顶部和底部进入浏览器安全区，适配 720×1280 至 1080×1920 以及地址栏伸缩。
- [x] 中央全景资源未加载时显示灰盒/加载占位，不能露出空白或阻塞设置和错误反馈。
- [x] 所有新增可点击入口的有效触控区至少 48×48dp。

验收记录（2026-07-30）：`Camp.scene` 已切换为 `WorldViewport + SafeAreaRoot`。中央预留背景/建筑/前景三层全景容器，上下 HUD 固定在安全区根节点，缺少正式美术时显示“营地全景灰盒”。浏览器构建可正常加载，旧五导航无残留，控制台无 warning/error；场景校验覆盖层级引用、安全区接线和 48×48dp 触控下限。横向连续滑动属于 1.2.4，本阶段仅完成全景骨架。

#### 1.2.3 顶部头像、资源与主线

- [x] 左上显示可点击头像；具体行为未定义前点击显示“功能待定”。
- [x] 头像右侧按固定顺序显示五种真实 Wallet 资源：灵粮、灵木、玄铁、灵晶、庚精。
- [x] 更新本地化显示：`resource.spirit_stone=灵晶`、`resource.immortal_coin=灵石`；存档字段名不迁移。
- [x] 删除场景默认假数值；数据未就绪时显示占位符，加载后由状态事件刷新。
- [x] 头像下方显示当前主线任务图标和单行目标；无主线时显示“暂无主线任务”。
- [x] 主线提示可点击；具体行为未定义前点击显示“功能待定”，不默认跳转。
- [x] 从其他页面返回或回城结算关闭后，刷新资源和主线提示。

验收记录（2026-07-30）：顶部 HUD 已接入头像、真实 Wallet 五资源和主线目标。新档主线为“前往议事殿，与岑守一交谈”；当前任务进度由 `storyFlags` 解析，后续接入完整任务服务时可替换数据源而不改 HUD 结构。未加载数值显示 `--`，头像和主线点击统一反馈“功能待定”。

#### 1.2.4 横滑全景

- [x] 创建横向 ScrollView/等效容器，顶部与底部 HUD 不放入滚动内容。
- [x] 首次进入、新档进入和普通回城默认定位中部；从建筑子页面返回时恢复离开前位置。
- [x] 支持触控与鼠标按住横向连续拖动、惯性减速和左右边界限制，不做分页吸附。
- [x] 全景按背景、建筑交互、前景遮挡至少三层搭建；建筑、标签、状态和热区保持独立。
- [x] **修复横滑误触回归：位移超过 12dp 后取消建筑与传送阵点击。**
      快速连续操作时同一入口仍只允许响应一次。
- [x] 左右边界不露空白；内容不足一屏时自动居中并禁用横滑。
- [x] 中部默认视口完整显示中上方议事殿和中下方传送阵，能识别左侧招贤馆、右侧交易行，并露出竞技场和炼器坊的局部轮廓。
- [x] 接入正式横向营地背景；背景使用 SIMPLE Sprite，铺满全景并延伸到固定 HUD 后方。

验收记录（2026-07-30）：全景宽度为屏幕的 `2.8` 倍（1080 设计宽下为 3024），默认中部 `x=0`，连续拖动不吸附。表现层接入触摸/鼠标模拟触摸、惯性、立即反向刹停、边界夹取、12dp 误触取消和 350ms 重复激活保护。位置记忆只在进入建筑子页面前标记一次，普通进入/回城不误恢复。

背景验收记录（2026-07-31）：用户已在 Cocos 预览确认正式横向背景显示、全景横滑、固定 HUD 和建筑点击均正常。`Camp.scene` 引用的 SpriteFrame UUID 与背景 `.meta` 一致；正式场景改为由 Cocos Creator 与后续 Prefab 维护，停止完整重生成。

#### 1.2.5 建筑与传送阵布局

- [x] 左侧按招贤馆、百宝库、还魂殿向左下依次排布；外围布置圣迹、教会。
- [x] 中部布置中上方议事殿和中下方传送阵。
- [x] 右侧按交易行、炼器坊、灵源院向右下依次排布。
- [x] 中部右缘露出部分竞技场和工坊（炼器坊），引导玩家继续右滑。
- [x] 右部完整布置竞技场、工坊（炼器坊）、庄园（灵源院）和遗迹。
- [x] 为七座成长建筑绑定入口或基于状态的明确反馈；根据新手进度应用 LOCKED、AVAILABLE、UNLOCKED 等状态。
- [x] 城堡（议事殿）点击进入营地人物列表（NPC List）；内容页未完成前至少接通页面壳和返回大厅路径。
- [x] 新档人物列表默认只显示岑守一；点击岑守一直接进入对话，结束后返回列表并刷新任务/建筑状态。
- [x] 传送阵点击后在大厅上方唤起入山整备面板，不再进入独立编队/准备页面。
- [x] 竞技场、遗迹、圣迹、教会只作不可交互场景锚点，不显示红点，不计入可用入口；当前不实现 PvP 或单机竞技玩法。
- [x] 建筑标签、锁定态和红点随全景移动；返回大厅时刷新建筑状态和红点。

验收记录（2026-07-30）：议事殿位于中上方，传送阵位于中下方；七座成长建筑使用 320×240 灰盒。左右建筑分别沿左下、右下方向展开，传送阵为 360×190。七建筑状态标签、传送阵入口和四个未开放锚点已写入 `Camp.scene`；全景可视区增加矩形裁剪。议事殿已接通全屏人物列表和返回大厅路径，新档仅显示留守管事岑守一。首次三段对话结束会写入 `met_cen_shou_yi`、`tutorial_started`，人物状态由“有任务”刷新为“可交谈”，主线刷新为“整备营地，准备首次入山”并保存存档。浏览器已实际走通上述链路及入山未开放反馈，控制台无 warning/error。

#### 1.2.6 底部左右入口

- [x] 左侧从左到右显示设置、成就、排行榜、邮件、日常进度五个小图标。
- [x] 设置绑定设置页；成就、排行榜、邮件、日常进度统一显示未开放态，不创建页面且不产生红点。
- [x] 右侧只显示灵石图标和余额，读取 `Wallet.immortalCoin`；字段名保持稳定，产品显示名由仙铢改为灵石。
- [x] 顶部灵晶读取 `Wallet.spiritStone`；不得与右下角灵石串用余额。
- [x] 隐藏天符玉、充值、礼包、商店等全部付费入口，不保留可见占位。
- [x] 当前不接广告、支付 SDK、真实订单或任何付费货币发放与消耗。

验收记录（2026-07-30）：底部左侧已按设置、成就、排行、邮件、日常顺序接入五个 112×96dp 入口；设置打开带返回路径的全屏页面壳，其余四项统一给出“尚未开放”反馈且无红点。右下角仅保留灵石图标与余额，顶部灵晶和底部灵石使用独立字段。2026-08-02 改造后由 `CampHudApplicationService` 统一获取 API 快照，两个 Presenter 只渲染 ViewModel。

#### 1.2.7 验收与测试

- [x] 单测覆盖默认/恢复横滑位置、12dp 点击取消阈值、边界限制和重复点击保护的纯逻辑。
- [x] 场景校验覆盖三层节点归属、入口顺序、节点引用数量和 48×48dp 最小触控区。
- [x] 修复后重新人工确认“拖动经过建筑”不误触，轻点建筑均能响应。
- [x] 无引导从中部滑到左右边界，能找到仓库、墓园、工坊和庄园，并能识别竞技场为未开放场景。
- [x] 所有可见入口要么进入正确页面，要么说明不可用原因；暂不实现锚点不得伪装成按钮。
- [x] 顶部、底部和入山入口在 720×1280、1080×1920 及带安全区设备上无遮挡。

完成判定：大厅按 PRD-01 v0.2 成为可横滑的三层页面；顶部显示真实资源和主线，全部建筑与入山位置正确，底部左右入口顺序正确，所有可见可点击项有路由或明确反馈，横滑无误触且返回后状态正确刷新。

人工验收记录（2026-08-02）：横滑门禁改为保持到下一次触摸开始，建筑与传送阵统一检查；
用户已确认拖动不再误触，营地大厅恢复“✅ 可体验”。

#### 1.2.8 拆分为 Prefab 与共享契约（已完成，待用户复验）

- [x] 单体 `CampPresenter`（734 行）拆为六个 Presenter；原脚本缩为兼容安装器并保留 UUID，已从 `Camp.scene` 的 Canvas 移除；已落地的一次性补丁 `patch-camp-bottom-hud.mjs` 删除。
- [x] 新增 `domain/CampSceneContract.ts` 作为逻辑 id、节点名与 Presenter 访问路径的唯一事实源；表现层与 `tools/` 共用同一份。
- [x] `camp-layout-config.mjs` 只保留尺寸坐标与显示名，按 id 建表；`validate-scene.mjs` 交叉核对两边键一致，并逐条验证 `presenterPaths` 在场景与 Prefab 中真实存在。
- [x] 生成器改为按契约把六个 Presenter 挂到各自模块根节点，不再有跨节点 `@property` 接线。
- [x] 在 Cocos Creator 中新建 `NpcPage`/`SettingsPage` 页面壳、挂载六个 Presenter，另存
      `CampPanorama`、`CampTopHud`、`CampBottomHud`、`CampNpcPage`、`CampSettingsPage`
      五个 Prefab，并由编辑器生成对应 `.meta`。

执行记录（2026-08-02）：`WorldViewport` 与 `BuildingLayer` 归入全景 Prefab；顶部、底部、
NPC、设置各自成为独立 Prefab。场景中的旧兼容安装器已移除，五个 Prefab 根节点与
`CampSceneContract` 保持一致。按工程约定，本轮未由 Codex 运行场景校验、类型检查、
单测、构建或预览，需由用户刷新 Cocos 资源后复验。

待接（不阻塞 1.2）：PRD-01 P1-HALL-015「从子页面返回恢复横向位置」的域层逻辑与单测已就绪
（`PanoramaPositionMemory`、`CampPanoramaController.rememberForChildPage()`），但七座建筑子页面
尚未开放，暂无调用点；`camp.panorama.reset` 事件同理暂无发出方。接入首个建筑子页面时一并接线。

### 1.3 建筑与生产页面

- [x] 三种 P1 资源、杂役分配、30 秒结算和灵粮停工逻辑。
- [x] 调岗前先结算旧配置，避免切岗刷收益。

本节只实现 P1 核心闭环中的灵源院。使用大厅上方的纵向生产弹窗，不切换独立建筑
场景；弹窗框架可供后续建筑复用。本轮不为
招贤馆、百宝库、炼器坊、交易行和还魂坛制作功能页；这些建筑继续显示锁定或
明确的未开放反馈。Demo 裁剪范围以
[Demo 范围与体验闭环 §2](01_Demo范围与体验闭环.md#2-d0-可玩样机) 为准；灵源院阶段素材与页面输入读取
[Demo 灵源院美术资料](ArtAssets/03_灵源院生产弹窗.md)，不再引用正式 PRD 作为 Demo 进度事实源。
客户端、服务端、API 与 Local Adapter 使用与 1.2 相同的四份技术文档。

#### 1.3.1 页面与入口

- [x] 点击已解锁的灵源院，在当前大厅上方打开纵向生产弹窗和半透明遮罩。
- [x] 完整静态布局保存于 `CampLingPuPage.prefab`，并以关联 Prefab 实例挂在
      `Camp.scene/SafeAreaRoot` 下；标题、五条资源行、底部按钮和两个确认弹窗均可
      在 Cocos Creator 中直接选中调整，`CampLingPuPresenter` 不再运行时创建节点。
- [x] 弹窗显示“灵源院”和三条已开放资源行：灵粮/灵木/玄铁可操作，
      灵晶/庚精节点保留在 Prefab 中但 P1 整行隐藏；面板保持定高，周期区及
      “杂役招募/关闭”两个按钮位置不因隐藏行变化。
- [x] 三条开放资源行均显示“当前库存/最大储量”，右侧提供“升级”按钮。
- [x] 关闭弹窗后直接回到原大厅位置，并刷新顶部资源栏。
- [x] 灵晶、庚精岗位 P1 整行隐藏，不允许分配、升级或参与生产维护。
- [x] P1 不显示产量升级、整座灵源院升级、双倍收益、广告或付费入口。

#### 1.3.2 杂役分配

P1 新档初始提供 6 名杂役且全部空闲；招募后总数可以增长。当前总数必须进入存档。
页面不再显示分配总结，每行使用 `x/y` 表达当前投入和该资源可达投入上限。

| 岗位 | 单人每 30 秒产出 | 单人每 30 秒灵粮维护 | P1 状态 |
|---|---:|---:|---|
| 灵粮 | 灵粮 +1 | 0 | 开放 |
| 灵木 | 灵木 +1 | 灵粮 -2 | 开放 |
| 玄铁 | 玄铁 +1 | 灵粮 -3 | 开放 |
| 灵晶 | 灵晶 +1 | 灵粮 -4 | P2 开放 |
| 庚精 | 庚精 +1 | 灵粮 -9 | P2 开放 |

- [x] 每个开放岗位显示当前库存/最大储量、当前人数、当前产量、储量升级和 `-`/`+`
      按钮；不显示储量等级、单人产出或维护拆分。
- [x] Prefab 中预留的五条资源行使用相同静态节点结构，并统一使用
      `LingPuResourceRowComponent` 接收渲染状态；单行逻辑尺寸为 `312×64`、间距 `5`，
      图标与文字不重叠，`375×817` 下无需滚动即可容纳未来五行。
- [x] 数据配置定义初始杂役 6、每次招募 5 和单次灵粮消耗；具体消耗不得在 UI
      或业务代码中硬编码。
- [x] 存档记录当前杂役总数，旧存档迁移时补为初始值 6。
- [x] 单次点击只增减 1 人；人数为 0 时禁用 `-`，空闲人数为 0 时禁用全部 `+`。
- [x] 岗位人数只能是非负整数，三个开放岗位人数之和不得超过当前杂役总数。
- [x] 每次点击 `-`/`+` 都先按旧配置结算，再立即应用新人数、刷新预览并保存；
      页面不存在“确认分配”按钮和未保存草稿。
- [x] 快速连续点击按顺序处理，不重复结算、不吞在线周期、不突破总人数。

资源行当前产量统一使用 30 秒口径：

```text
灵粮产出 = 灵粮岗人数
灵木产出 = 灵木岗人数
玄铁产出 = 玄铁岗人数
灵粮维护 = 灵木岗人数 × 2 + 玄铁岗人数 × 3
预计灵粮净变化 = 灵粮产出 - 灵粮维护
```

- [x] 灵粮行直接显示净产量；给灵木、玄铁分配杂役时，灵粮产量实时扣除对应维护。
- [x] 人数值兜底为 `0`，不使用 `--`；P1 隐藏的灵晶、庚精行不渲染兜底文案。
- [x] 不显示“下周期”汇总和“全部岗位可正常生产”等全局状态文案；倒计时不追加
      “/ 30秒”。
- [x] 加减按钮视觉尺寸缩至逻辑 `24×24`，但触控区仍保持 `48×48`；升级按钮放大至
      逻辑 `84×34`。减号、加号按顺序并排放在资源行右下方，人数显示在按钮左侧。

预计灵粮净变化为负时显示明确警告文字，并配合赭黄/朱红行描边，不能只靠颜色表达。

#### 1.3.3 杂役招募

- [x] 点击主弹窗底部“杂役招募”，在主弹窗上方打开二次确认弹窗并加深遮罩。
- [x] 主弹窗“杂役招募”始终可点击；灵粮不足也能进入二次弹窗查看所需消耗。
- [x] 二次弹窗显示灵粮图标、配置消耗、当前灵粮、“招募 5 名杂役”和底部
      “招募/取消”两个按钮。
- [x] 灵粮充足时招募按钮可用；点击后再次校验库存，原子扣粮、总数 `+5`、
      立即保存，关闭二次弹窗并刷新主弹窗。
- [x] 灵粮不足时显示明确文字，招募按钮禁用；取消按钮始终可用。
- [x] 取消只关闭二次弹窗，不扣粮、不增加杂役、不改变岗位分配。
- [x] 招募成功只增加空闲人数，不自动把新杂役分配到任何岗位。
- [x] 二次弹窗打开时主弹窗不可操作；系统返回键优先关闭二次弹窗。

#### 1.3.4 资源储量升级

灵粮、灵木、玄铁分别拥有独立的存储等级和最大储量。这里的升级仅提高对应资源的
最大库存，不提高产量、不缩短周期、不改变岗位人数，也不等同于后续整座建筑升级。

- [x] 点击任一资源行右侧“升级”，在主弹窗上方打开储量升级确认弹窗并加深遮罩。
- [x] 升级弹窗显示目标资源、灵木图标、配置消耗、当前灵木、当前最大储量、
      升级后最大储量，以及底部“升级/取消”两个按钮。
- [x] 数据配置按资源和等级定义初始存储等级、当前上限、下一级上限、灵木消耗和
      当前版本最高等级；UI 与业务代码不得硬编码具体数值。
- [x] 行内升级按钮在未满级时始终可点击；灵木不足也能打开弹窗查看消耗与收益。
- [x] 灵木充足时弹窗升级按钮可用；点击后先按旧上限结算，再次校验库存与等级，
      原子扣除灵木、目标资源存储等级 `+1`、更新上限并立即保存。
- [x] 升级灵木自身储量时，同样从当前灵木库存扣除升级费用。
- [x] 灵木不足时显示明确文字，弹窗升级按钮禁用；取消按钮始终可用。
- [x] 取消只关闭弹窗，不扣灵木、不改变等级、上限和岗位分配。
- [x] 已达当前版本最高等级时行内显示“已满级”，升级按钮禁用且不打开弹窗。
- [x] 生产结算后库存不得超过当前最大储量；超出部分不进入库存，行内显示“已满仓”。
- [x] 快速重复点击只允许一个升级事务成功，失败时不得部分扣款或提高上限。
- [x] 存档只保存各资源存储等级；最大储量由等级和配置表恢复，避免两份数值漂移。
- [x] 升级弹窗打开时主弹窗不可操作；系统返回键优先关闭升级弹窗。

#### 1.3.5 在线结算、停工与保存

- [x] 每次点击岗位 `-`/`+` 时，先按旧岗位结算已完成周期，再应用新分配。
- [x] 调岗、招募或储量升级生效后立即保存杂役总数、`workerAssignments`、
      资源库存、各资源存储等级和结算时间。
- [x] 灵源院在前台时每 30 秒结算一个完整周期，并同步刷新页面库存与大厅资源栏。
- [x] 游戏保持前台时，离开灵源院进入编队、地图等页面不暂停在线生产。
- [x] 进入灵源院、每次调岗、招募成功、储量升级成功和关闭灵源院时触发结算与保存；
      快速重复操作不得重复结算。
- [x] P1 不结算关闭页面或浏览器后台期间的收益；恢复前台时重置在线结算起点，
      P2 再开放最多 4 小时离线收益。
- [x] 可用灵粮不足时按“玄铁 → 灵木”停工；停工岗位本周期整体不产出，不做部分开工。
- [x] 停工时显示具体岗位、原因“灵粮不足”和恢复条件，不允许资源库存变为负数。
- [x] 刷新页面后恢复最新杂役总数、岗位分配、资源库存、三种资源存储等级和正确的结算起点。
- [x] 系统时间倒退超过 5 分钟时暂停结算并给出提示，不修改现有资源和岗位。

#### 1.3.6 美术素材现状与缺口

当前可复用素材：

- [x] 灵源院大厅入口图已有正常态与锁定态：
      `env_camp_building_ling_pu.png`、`env_camp_building_ling_pu_locked.png`。
- [x] 大厅顶部已有可复用的灵粮、灵木、玄铁、灵晶、庚精 `72×72` 资源图标；
      灵源院五行直接复用，未另做第二套。
- [x] Ark Pixel 中文字体及 OFL 许可已接入。
- [x] `ART-UI-014 灵源院生产弹窗视觉套件` 已制作、导入并在 Cocos 预览确认加载。

当前运行时使用 Figma 主面板素材、2 张调岗图标与 5 张共享资源图标；资源行、操作按钮由
Cocos 原生节点绘制，旧占位 PNG 已于 2026-08-08 退役：

| 素材 | 状态 | 实际文件 | 交付尺寸 |
|---|---|---|---:|
| 生产主面板 | 已接入 | `ui_ling_pu_panel_body.png`＋通用上下装饰 | `1005×1509 px`＋复用 `@3x` 装饰 |
| 二次确认弹窗背景 | 已接线，待 Creator 导入 | `ui_ling_pu_recruit_panel.png` | `981×798 px (@3x)` |
| 五条资源行底板 | 已接入 | Cocos `Graphics` 原生绘制 | 无 PNG |
| 加/减调岗按钮 | 已接入 | `icon_action_plus.png`、`icon_action_minus.png` | 各 `120×120 px` |
| 升级/招募/取消/关闭 | 已接入 | `Button/Resizable/Inline`、`Button/Resizable/Footer` 原生绘制 | 无 PNG |
| 旧周期条底/填充 | 已退役 | 页面当前不展示旧进度条图片 | 无运行时文件 |
| 五种资源图标 | 已复用 | `ui/top/icon_resource_{spirit_grain,spirit_wood,dark_iron,spirit_crystal,geng_jing}.png` | 各 `72×72 px` |

按钮的按下、悬停和禁用状态由 Cocos `Button` 颜色过渡生成，不再要求额外状态 PNG；
二次弹窗使用 Figma `95:553` 固定背景与通用按钮，文字和数值仍保持动态渲染。素材加载失败时
仍保留纯色灰盒兜底，避免阻塞操作。

交付约束：

- [x] UI 图片使用 Nearest 过滤并保持整数倍缩放；九宫格已按安全区拉伸。
- 图标本体可为 24×24/32×32，但按钮有效触控区不得小于 48×48dp。
- 正式素材使用原创资产；若 Demo 临时使用 CC0 素材，必须登记来源和许可。
- [x] 新增图片均已由 Cocos Creator 导入并生成 `.meta`，未手写 UUID。
- 文字、数值和资源名称由 UI Label/本地化表渲染，不得烘焙进背景或按钮图片。

P1 不要求制作灵源院室内场景、顶部横幅、杂役头像、生产状态图标、杂役逐帧动画、
作物/水车动画、离线收益宝箱或整座建筑升级特效；这些不阻塞本节功能与灰盒验收。
杂役招募与资源储量升级弹窗复用生产弹窗九宫格、对应资源图标和通用按钮普通态，
不新增专用弹窗、容量箭头或文字 PNG。

完整文件名、截图要求、GPT Image 提示词、制作顺序和美术验收见
[《昆吾禁地》灵源院生产弹窗美术分册](../ArtAssets/03_灵源院生产弹窗.md)。

#### 1.3.7 P1 验收路径

1. 新档进入灵源院，看到三个开放岗位的库存/上限、`0/6`、产量和升级按钮；
   灵晶、庚精两行不显示，三行均没有图文重叠。
2. 连续点击 `+` 将 6 人分配到灵粮岗；每次点击立即生效，等待一个周期后库存真实增加 6。
3. 改为灵粮 3 人、灵木 2 人、玄铁 1 人，三行产量分别显示 `-4`、`+2`、`+1`。
4. 灵粮充足时打开杂役招募二次弹窗，核对配置消耗后招募；库存正确扣除，杂役总数与空闲人数增加 5，原岗位人数不变。
5. 灵粮不足时再次打开二次弹窗，招募按钮禁用；点击取消后所有数据不变。
6. 灵木充足时打开灵粮储量升级弹窗；确认升级后灵木正确扣除，灵粮最大储量提高，
   灵粮产量和三个岗位人数均不变。
7. 灵木不足时再次打开储量升级弹窗，升级按钮禁用；取消后等级与库存不变。
8. 灵粮不足生产时玄铁先停工；仍不足时灵木停工，页面说明原因且库存不为负。
9. 点击关闭回到大厅，资源栏已刷新；刷新浏览器再次进入，杂役总数、岗位、资源
   与三个存储等级保持一致。

完成判定：玩家能直接调岗并预测每 30 秒的产出与维护，能通过二次确认消耗灵粮
招募 5 名杂役，也能消耗灵木提高任一基础资源的最大储量；调岗、招募、储量升级、
停工、关闭、刷新和存档行为均有明确且一致的结果。

早期验收记录（2026-07-31）：在 `375×817` 浏览器视窗中从大厅横滑进入灵源院，当时已验证
五条组件化资源行完整显示、图文无重叠、P2 行不可操作、正式素材加载、连续 6 次
调岗、完整 30 秒在线结算、招募成功、灵粮不足时禁用招募、
灵木不足时禁用储量升级、二次弹窗/主弹窗的分层返回、关闭后大厅资源刷新，以及
刷新浏览器后恢复杂役总数、岗位和资源。储量升级成功、满仓、满级、停工顺序、
后台恢复与时钟倒退由领域/服务单测覆盖。预览控制台无灵源院 warning/error。

API First 改造记录（2026-08-02）：顶部 HUD、底部 HUD 和灵源院已改为
`Presenter → Application Service → CampApiPort → Local Adapter` 调用链，并补齐前后端与
API 文档。当前产品口径已改为 P1 只显示三行、灵晶/庚精整行隐藏且面板定高。
按工程约定，本轮未由 Codex 运行类型检查、单测、场景校验、构建或预览，
需由用户刷新 Cocos 资源后按本节与本地适配器验收清单复验。

### 1.4 入山整备面板（含修士与编队）

本模块没有独立 Party 页面。玩家点击营地传送阵后，在大厅上方唤起入山整备面板；
“编辑队伍”和“传送”分别在其上方打开选人面板、地图选择面板。

#### 1.4.1 已有领域与存档能力

- [x] 七维成长、六档灵根资质、八境界稳定 ID 和一转等级规则；Schema v5 已完成
      v4 旧档的灵根/境界迁移，当前可玩上限仍为炼虚 Lv60。
- [x] 最多四人编队、死亡限制、跨队互斥和入山校验。
- [x] 六基础职业、18 个基础技能和四名初始修士数据。
- [x] HeroFactory 可把数据实例化为修士和战斗单位。
- [x] 修士灵息、队伍预设、当前队伍、三类携带物与恢复结算锚点进入 v4 存档；
      v3 旧档迁移为满灵息、第 1 队和默认 60 灵粮。
- [x] 灵息上限/自然恢复、负重系数、物品重量、队伍解锁费用和地图门槛统一由
      `shared/expedition_preparation.json` 配置，UI 不硬编码数值。

#### 1.4.2 入山整备主面板

- [x] 按 `375×817` 逻辑尺寸运行时创建模态面板，并等比挂到迁移中的旧 Cocos 画布；
      不把 `1080×1920` 旧坐标扩散为新的 UI 事实源。
- [x] 顶部横向显示 4 个修士槽位。空位显示人物黑影与“人选未定”；已上阵卡显示
      灵息、姓名、职业·等级和境界，不显示灵根资质文字与旧品级星星。
- [x] 六档灵根资质边框由 `SpiritualRootFrame` 在通用卡框上运行时绘制，通过轮廓、灵纹
      数量与颜色共同区分，是修士卡内灵根资质的唯一展示；异灵根使用青白不对称灵纹，
      不使用暗红或紫黑。境界继续使用文字徽记。当前四名初始修士只覆盖杂灵根与伪灵根，
      其余四档随未来修士数据直接生效。
- [x] 四名初始修士的静态像素立绘已接入修士卡背景；石岩在主面板优先播放 8 帧自然呼吸
      Sprite Sheet，其余修士和编辑队伍列表仍使用静态立绘；动画图集缺失时回退静态立绘，
      所有立绘均缺失时保留剪影降级；
      文件名、尺寸和素材说明见
      [入山整备、编辑队伍与地图选择美术分册](../ArtAssets/04_入山整备编辑队伍与地图选择.md)。
- [x] 操作栏依次为“编辑队伍”、队伍栏、“调息”。调息当前为禁用态；
      修士不在野外地图时每 5 分钟自然恢复 1 点，具体值由配置读取。
- [x] 新档只开放 1 队；第 2、3 队按顺序消耗配置的灵石解锁，切队与解锁立即保存。
- [x] 显示当前/最大负重 `x/y`；最大值由当前队伍力道、肉身与配置系数计算。
- [x] 灵粮、开山镐、探灵镜三行均显示已携带/当前拥有与 `- / +`，单件重量仍由配置参与负重计算；增减后
      立即校验负重并保存。新档提供 3 把开山镐和 1 个探灵镜供灰盒交互。
- [x] 打开面板、切换/解锁队伍或编辑成员时，灵粮默认重算为库存与当前剩余负重
      共同允许的最大整数数量；玩家仍可手动减少。
- [x] 底部按钮收敛为“传送/返回”：移除旧“历练”禁用入口；传送打开地图选择；返回
      无损关闭面板。
- [x] Approved Figma `83:238`（一级面板组合 `106:555`）已落到 `CampExpeditionPage.prefab`：
      面板为 `359×607`，修士卡 `71×163`，操作按钮 `72×28`，队伍序号 `26×26`，
      装载行 `204×32`，底部按钮 `132×44`；四张 `@3x` PNG 已通过一次 TinyPNG 入库，
      待用户在 Creator 中完成人工视觉与操作验收。

#### 1.4.3 编辑队伍面板

- [x] 纵向滚动显示全部已拥有修士；每行包含头像剪影、境界·灵根文字、姓名、职业、等级、
      灵息、七维合计战力和选择/取消按钮。
- [x] 选中态同时使用描边、底色和“取消 + 槽位编号”，不只依赖颜色。
- [x] 支持加入与移除；满队、阵亡、跨队占用、不存在修士均显示明确原因。
- [x] 每次选择或取消立即写入当前队伍并排队保存；完成后返回入山整备主面板。

#### 1.4.4 地图选择面板

- [x] 显示 5 张产品地图；每项包含地图编号、名称、所需灵息、每步灵粮和最低携带
      灵粮，未解锁地图显示锁定态。
- [x] 点击已解锁地图时一次校验队伍、阵亡、每名修士灵息、最低灵粮和负重，并用
      文字显示首个阻断原因。
- [x] 校验通过后发出稳定的 `expedition.mapSelected` 交接事件；场景与地图数据加载成功前
      只保存内存 pending departure，不提前扣灵息或物资。
- [x] Map 场景接入后，由应用服务原子创建 ExpeditionState、扣除队伍灵息和携带灵粮并
      直接传送到所选地图；开山镐与探灵镜在 D0 没有消耗点，只参与整备校验。
- [x] 已由 Cocos Creator 导入地图脚本、数据、CC0 素材和 `Map.scene`，并生成真实 `.meta`。
- [ ] 由用户亲自完成 `375×817` 预览、触控、返回层级、刷新存档和控制台验收。

完成判定：玩家能从传送阵在同一模态流程中完成选人、切换队伍、调整三类携带物、
看懂灵息与负重阻断原因并选定地图；关闭面板不损失状态。实际进入野外地图随 1.5
的 Map 场景交接完成。

### 1.5 地图探索页面

本轮实现细节与恢复口径见 [D0 地图探索客户端设计](Tech/D0_MAP_客户端技术设计.md)。

- [x] 四方向移动、地形成本、Dijkstra 最短路径。
- [x] UNKNOWN、DISCOVERED、VISIBLE 三态迷雾。
- [x] 探灵灯范围、灵粮扣除、负重警告和返程安全判断。
- [x] Tiled 坐标转换与固定对象导入。
- [x] 创建 `375×817` Map 场景壳和正式 `48×64` 边界下的 `15×15` D0 开放区数据。
- [x] 接入 Puny Dungeon CC0 地形，并提供素材未导入时仍可操作的像素灰盒。
- [x] 渲染玩家位置、可行走格、镜头跟随和三态迷雾。
- [x] 支持点击相邻格、底部方向按钮或键盘方向键四向移动。
- [x] 业务移动保存成功后，角色圆点用代码 Tween 过渡到相邻格，再平滑跟随镜头；动画期间锁定输入。
- [x] 每步真实扣除灵粮、保存进度并刷新剩余量/坐标/返程估算 HUD。
- [x] 增加一次性剧情事件「残碑留痕」和宝箱「封禁木匣」；探明后显示代码标记，踏入后弹窗。
- [x] 地图对象完成状态进入 v6 存档；宝箱奖励先作为临时战利品，安全归营后入库。
- [x] 将临时遭遇提示升级为统一地图事件面板；信息区展示故事/描述，行动区按
      对象数据显示迎战、探灵、交谈、开采/开启/进入、闲谈和离开。
- [x] 固定坐标「残禁石傀」踏入时先打开面板；只有点“迎战”才进入 D0 战斗逻辑页，
      “探灵”按本次携带的探灵镜显示敌方特性。
- [x] 「封禁木匣」改为点击“开启”后再原子结算；只点“离开”不会提前发放奖励。
- [x] 旧的“返回入口才能返营”已替换为归营符直接归营；从其他格走回入口仍可免符归营。
- [x] Boot 检测到 active ExpeditionState 时直接恢复地图，不重复扣除出发成本。
- [x] 修复 Camp → Map 切场景时，营地组件在 `onDestroy` 重复解绑已销毁节点事件导致的报错。
- [x] 地图顶部改为左侧“地图名称（坐标）/负重/灵粮”、右侧“休整/归营/队伍/背包/设置”，
      下方复用营地主线任务摘要组件。
- [x] 野外休整状态、次数、食材补充灵粮、每次休整一次运功疗伤和刷新恢复进入 v7 存档。
- [x] 归营符可在任意坐标安全归营；无符时不提供绕过断粮阵亡的紧急撤离。
- [x] 从其他格返回入口传送阵时，移动动画结束后弹出免归营符的安全归营二次确认；刚启程时不弹。
- [x] 地图背包只展示本次获得的临时战利品，改为五列方形物品格平铺，格内显示物品图标与右下角数量；
      队伍和设置统一给出功能待定反馈。
- [x] 灵粮归零后显示“灵粮已尽”警示并持久保存四步衰竭状态；顶部依次显示
      “断粮 / 气血亏空 / 步履维艰 / 生机将绝”，补粮成功重置衰竭步数。
- [x] 第 4 个衰竭步移动动画结束后原子结算全队阵亡；本次入山修士生命归零、退出
      所有队伍槽位并进入还魂殿待处理状态，可损失物按当前 D0 范围向下取半返还。
- [x] 断粮衰竭步数进入 v8 存档；刷新恢复到第 4 步时优先重试阵亡结算，保存失败不重复返还。
- [ ] 由用户刷新 Cocos 资源并完成场景、触控、刷新恢复和控制台人工验收。

完成判定：玩家能连续移动、看到迷雾变化、消耗灵粮并进入固定战斗。

### 1.6 战斗页面

页面信息层级、静态立绘底部覆盖规则、缺失素材表、所需截图和 GPT Image 提示词统一见
[战斗页面与静态单位美术分册](../ArtAssets/05_战斗页面与动态单位.md)。Demo 战斗单位固定使用
静态立绘，不接入 Live2D，也不为角色制作逐帧动作；技能及通用战斗特效使用 Sprite Sheet
与 Cocos AnimationClip。
客户端宿主、状态流、失败恢复和人工验收见
[D0 固定遭遇战客户端设计](Tech/D0_COMBAT_客户端技术设计.md)。

- [x] 20 Hz 时间轴结算和可复现事件序列。
- [x] 七维伤害、治疗、护盾、破甲、控制和持续伤害。
- [x] 九种目标类型、嘲讽、群体免疫和 ignoreTaunt。
- [x] 以独立 `combat` 逻辑路由复用 Map 场景宿主，运行时创建 `375×817` 战斗灰盒；
      正式 Combat 场景资源留到 D1 美术替换时创建。
- [x] 用四名初始修士和一个固定敌人创建 CombatSnapshot。
- [x] 将 tick 推进接到游戏循环，支持浏览器切后台暂停和恢复。
- [x] 将 CombatEvent 显示为血条、行动条、伤害数字和技能文字。
- [x] 敌我单位按“名称 → 族类图标 + 生命条 → 行动条 → 状态图标”渲染，以上信息全部
      覆盖在静态立绘底部；我方名称可切换自动施法。
- [x] 页面只提供正常速度；达到配置条件后才在右上角显示“逃生”。
- [x] 实现手动技能 FIFO 等待队列；当前修士未施法时保持技能面板，后续就绪修士排队且
      行动计时冻结为 `0`，不再使用超时自动选技；切换自动后仍可按策略继续行动。
- [x] 固定遭遇战胜利后打开战利品二级弹窗，上半区管理当前野外背包，下半区显示本场掉落；
      支持整批负重校验、全部拾取、离开放弃和每次点击丢弃 1 个背包物品。
- [x] 胜利成立时先保存野怪完成态与修士战后状态；战利品选择单独结算，弹窗阶段刷新或
      同一次入山内重新进入地图不会再次刷新已击败野怪。
- [x] 创建新的入山进度时按地图配置清除普通野外敌人完成态；宝箱、一次性剧情和 Boss 不随之重置。
- [x] 胜负结束后进入原子结算；保存失败保留结果面板并允许重试，不停留在死循环。
- [ ] 由用户刷新 Cocos 资源，完成 FIFO 技能选择、自动施法、逃生、战利品拾取/超重/丢弃、
      胜负、返回和刷新恢复人工验收。

完成判定：玩家能看见一场有过程、有结果的自动战斗，而不只是后台计算。

### 1.7 归营结算与转场

- [x] 归营不展示独立结算面板；确认归营并原子保存成功后，使用持久全局遮罩缓动至
      纯黑，在黑屏期间切换营地，再缓动恢复画面。
- [x] 安全归营返还剩余灵粮与携带物，将临时战利品合并进 Inventory，清空本次
      ExpeditionState 并保存；保存失败完整回滚且不开始转场。
- [x] 残禁石傀胜利时从遭遇配置发放 `200` 魂晶，与修士战后状态、敌人完成态在同一
      事务保存；同一次入山不能重复领取。
- [x] 战利品弹窗显示“魂晶 +200（已获得）”；魂晶不进入背包、不占负重，放弃普通
      战利品不影响魂晶入账。
- [x] 战斗胜利后更新地图固定敌人状态，避免重复首杀并隐藏地图标记。
- [x] 保存后返回营地并通过 Wallet/Inventory 事件刷新大厅状态。
- [x] 战败复用全队阵亡原子结算并以黑屏缓动返回营地；保存失败回滚状态并允许重试。
- [ ] 由用户完成魂晶只发一次、普通战利品拾取/放弃、两种安全归营、断粮与战败返回的
      黑屏转场及大厅状态刷新人工验收。

完成判定：战斗结果真正影响营地状态，刷新页面后结果仍然存在。

### 1.8 核心闭环验收

- [ ] 新玩家无需控制台或编辑器即可完成整个流程。
- [ ] 从新档到首次回城控制在 15–20 分钟以内。
- [ ] 所有可见按钮都有响应。
- [ ] 浏览器刷新后资源、队伍和探索状态可恢复。
- [ ] 无 Sev-1、Sev-2 问题。
- [ ] localhost 实际录屏或逐步验收记录留档。

---

## 2. 核心闭环后再做的页面

### 2.1 背包与装备

- [ ] 创建 Inventory 页面。
- [ ] 定义最小物品和装备实例数据。
- [ ] 展示材料、装备和临时战利品。
- [ ] 支持查看与装备，P1 暂不做复杂筛选、重铸和强化。

### 2.2 任务

- [ ] 创建 Quest 页面。
- [ ] 实现三个灰盒目标：首次调岗、首次入山、首次战斗。
- [ ] 显示当前目标、完成状态和奖励领取。
- [ ] 用任务驱动建筑与地图解锁。

### 2.3 招募与培养

- [ ] 招贤馆三候选页面（0.1 Demo保持锁定，不进入制作）。
- [ ] 确定性刷新与新手固定四人组。
- [ ] 修士升级、转职和死亡/还魂页面。

### 2.4 设置与存档

- [ ] 音量、画质和辅助设置。
- [ ] 存档导出、导入、覆盖确认和错误提示。
- [ ] 浏览器数据被清理后的恢复指引。

### 2.5 营地美术资源压缩

首批 GPT Image 产出直接进了 `assets/`，尺寸是模型原生输出，未按交付口径裁切：

| 资源 | 当前 | 文档口径 |
|---|---|---|
| `env_camp_panorama_bg.png` | 1422×1106，2.7MB | 1152×896（中央有效区 1050×817）|
| `buildings/*.png` | 1536×1024，各约 3.1MB | 逻辑框 320×240 |

- [ ] 按口径裁切缩放，七座建筑全部产出后一并处理（当前 4/7）。
- [ ] 决定这批 MB 级 PNG 是否纳入 Git LFS。`.gitattributes` 现有注释假设「像素成品只有几 KB」，与实际不符。
- [ ] 复核 Web 首发分包体积：七座建筑按当前尺寸约 22MB 未压缩。

---

## 3. 平台与阶段门禁

这些任务可与页面开发并行，不阻塞软件层接线：

- [x] P0 自动门禁 11/11。
- [x] 桌面浏览器启动、Boot/Camp 场景和 Bundle 构建验证。
- [ ] iOS Safari 刘海安全区验证。
- [ ] 720×1280 与 1080×1920 像素缩放验证。
- [ ] 移动端低画质 ≥ 30 FPS。
- [ ] 浏览器后台 30 分钟恢复。
- [ ] 清理网站数据后通过导入恢复。
- [ ] 断网时已加载内容仍可运行。
- [ ] 给出 P0 PASS / CONDITIONAL PASS / FAIL 结论。

---

## 4. 已实现的基础能力索引

| 页面/能力 | 已有实现 |
|---|---|
| 启动 | GameBootstrap、BootSequence、BootSplash |
| 全局状态 | AppRoot、GameState、EventBus、TimeService |
| 存档 | SaveRepository、IndexedDbSaveBackend、备份与迁移 |
| 资源加载 | BundleManifest、BundleLoader、CocosBundleHost |
| 适配 | ViewportLayout、ViewportAdapter、Web Shell |
| 营地 | Camp.scene、CampSceneContract、六个 Camp*Presenter、ResourceBar、HallBadges |
| 生产 | Production、CampEconomy |
| 修士 | HeroGrowth、HeroFactory、Party、职业/技能/初始修士数据 |
| 地图 | TiledImport、GridCoord、Movement、FogOfWar、BurdenAndGrain |
| 战斗 | CombatState、CombatResolver、CombatFormulas、SkillTargeting |
| 内容校验 | DataValidator、ValidationReport、本地化完整性测试 |

---

## 5. Todo 维护规则

- Todo 以页面为主，技术任务写在所属页面下面。
- “逻辑有测试”只代表前置完成，不代表页面完成。
- 页面只有在 localhost 可进入、可操作、可返回时才能标记完成。
- 涉及资源或队伍状态的页面还必须通过刷新恢复验证。
- 不允许保留无反馈的按钮；未开放功能必须明确显示锁定或提示。
- 测试数量是健康指标，不作为产品进度百分比。
- 每完成一个页面，同步更新“页面总览”和对应验收结果。
