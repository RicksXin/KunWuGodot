# Demo 美术 03：灵源院生产弹窗

版本：0.3\
日期：2026-08-03\
状态：0.1 Demo P1/P2 美术制作资料\
上级索引：[0.1 Demo 美术制作资料](README.md)

> 本分册只服务 0.1 Demo 的 P1/P2 制作裁剪。正式 1.0 页面读取
> [正式灵源院素材分册](../../ArtAssets/03_灵源院生产弹窗.md)，两者互不充当范围事实源。

本节参考用户提供的第三方游戏截图，只借鉴“大厅上方打开纵向生产面板、操作后叠加
二次确认弹窗”、两级弹窗的框体继承关系，以及普通列表项与选中列表项的层级差异，
不复制其具体视觉。

本项目的框体关系冻结为：主面板与二次确认弹窗使用同一张基础九宫格，宽度、材质、
描边粗细和四角结构完全一致，只通过运行时高度区分；主面板再额外叠加独立的顶部、
底部装饰。二次确认弹窗只使用基础九宫格，不生成另一套边框。顶部装饰中央必须保留
标题安全区，标题仍由 Cocos Label 渲染。

参考图中的双龙/双兽、银色金属浮雕、哥特尖刺、资源图标、升级按钮、工匠招募按钮、
双倍收益、货币栏和具体间距均不得进入《昆吾禁地》素材。

```text
大厅（保持原位置并变暗）
└─ 灵源院生产弹窗
   ├─ 标题“灵源院”
   ├─ 灵粮岗位：图标、库存/上限、净产量、升级、- / x/y / +
   ├─ 灵木岗位：图标、库存/上限、当前产量、升级、- / x/y / +
   ├─ 玄铁岗位：图标、库存/上限、当前产量、升级、- / x/y / +
   ├─ 任一资源升级：灵木消耗 + 当前上限 → 新上限 + 升级 / 取消
   └─ 结算倒计时 + 30 秒周期条 + 压在面板背景下边缘的杂役招募 / 关闭
      └─ 杂役招募：灵粮消耗 + 杂役 +5 + 招募 / 取消
```

1.0 后续章节开放灵晶和庚精时启用 Prefab 中预留的两个资源行节点，不新增页面，也不改变为
滚动列表；尚未开放的资源仍不显示对应行。

## 5.1 精简后的素材清单

UI 使用 `375×817` 玩家可见窗口作为设计基准，PNG 按 `@3x` 交付。灵源院专属
素材进入 `assets/bundles/camp/ui/ling_pu/`；按钮与资源行使用 Approved Figma 对应的 Cocos
原生组件；共享资源图标进入 `camp/ui/top/`。
新图片必须由 Cocos Creator 导入并生成 `.meta`。

| 类型   | 素材               | 数量 |     逻辑尺寸 |          最终 PNG 尺寸 | 文件名                                            |
| ---- | ---------------- | -: | -------: | -----------------: | ---------------------------------------------- |
| 弹窗   | 全建筑共用基础九宫格       |  1 |  `96×96` | `288×288 px (@3x)` | `ui_common_modal_frame.png`                   |
| 确认弹窗 | 杂役招募/储量升级固定背景 | 1 | `327×266` | `981×798 px (@3x)` | `ui_ling_pu_recruit_panel.png` |
| 主面板装饰 | 顶部三切片装饰          |  1 | `128×48` | `384×144 px (@3x)` | `ui_common_panel_top_ornament.png`            |
| 主面板装饰 | 底部三切片装饰          |  1 | `128×36` | `384×108 px (@3x)` | `ui_common_panel_bottom_ornament.png`         |
| 列表项  | 全建筑共用普通态九宫格底板   |  1 |  `64×32` |  `192×96 px (@3x)` | `ui_common_list_item_normal.png`               |
| 列表项状态 | 全建筑共用选中态完整底板     |  1 |  `64×32` |  `192×96 px (@3x)` | `ui_common_list_item_selected.png`             |
| 小按钮  | 增加/减少按钮（含符号）     |  2 |  `40×40` | `120×120 px (@3x)` | `icon_action_plus.png`、`icon_action_minus.png` |
| 通用按钮 | 资源行内紧凑操作 | 0 | `72×28` | 无 PNG | `Button/Resizable/Inline` 原生绘制 |
| 通用按钮 | 面板底部/确认弹窗操作 | 0 | `132×44` | 无 PNG | `Button/Resizable/Footer` 原生绘制 |
| 周期 | 旧倒计时条底/填充 | 0 | — | 无 PNG | 当前页面不展示，旧文件已退役 |

当前运行时主面板使用 Figma Approved 的 `ui_ling_pu_panel_body.png` 与通用上下装饰；
杂役招募与储量升级确认框使用 `ui_ling_pu_recruit_panel.png` 固定背景。
`ui_ling_pu_panel_frame.png` 继续保留给其他可拉伸 Level 2 面板复用，不得删除。

灵源院资源行已改为 Cocos `Graphics` 原生绘制；出征编队行使用 Figma 页面专用
`ui_expedition_hero_selection_row_<state>.png`，不再依赖旧资源行九宫格。

旧资源行、旧周期条、旧灵源院横向按钮以及两张通用按钮占位 PNG 已完成 Prefab 解绑并从
运行时目录移除。按钮按下、选中、禁用状态由原生组件绘制和代码驱动，不再保留图片兜底。

按钮按下使用运行时 `100ms` 缩放回弹，悬停与禁用由 Cocos 原生颜色和层级表达。
灵粮、灵木、玄铁、灵晶和庚精
直接复用大厅顶部 `72×72` 运行时资源图标；五张图标均已整理至
`assets/bundles/camp/ui/top/`。灵源院当前无按钮 PNG 美术缺口。

以下内容由 Cocos 节点、Label 和代码完成，不需要美术图片：

- 全屏半透明黑色遮罩、分隔线、滚动区域和按钮热区。
- 标题、库存/最大储量、岗位 `x/y`、当前产量和倒计时数字。
- 灵粮/灵木不足、满仓、满级、停工原因和恢复条件；使用文字与行底板 Tint 表达。
- 周期结算时的资源增减数字动画。
- 杂役招募和储量升级二次弹窗复用 `ui_ling_pu_recruit_panel.png`、对应资源图标和通用
  底部按钮；不叠加主面板上下装饰。
- 资源行右上角的“升级”使用通用行内按钮；储量升级确认弹窗底部的
  “升级/取消”使用通用底部按钮。按钮分类由使用位置决定，不由文案决定。

明确删除此前规划的灵源院横幅、室内背景、杂役头像、返回/信息图标、生产状态图标、
专用警告面板和周期时钟图标。本轮不制作产量升级、整座建筑升级特效、双倍收益、
广告按钮或离线收益宝箱。资源储量升级与杂役招募都复用通用弹窗，并按按钮的使用位置选择行内或底部样式；参考图中
的升级、工匠招募与二次弹窗具体造型均不复刻。

## 5.2 需要提供的截图

#### 5.2.1 主参考截图

| 截图                               | 尺寸与本地路径                                                                               | 只参考                                                                   |
| -------------------------------- | ------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `交易行面板.jpg` | `1256×2808`；`/Users/zhangxiaoen/Desktop/个人资料/referPng/交易行面板.jpg` | 只参考高主面板在基础框体上增加顶部、底部装饰，顶部中央为标题留空，以及内容区、底部操作区的纵向层级 |
| `升级建筑确认弹窗.jpg` | `1256×2808`；`/Users/zhangxiaoen/Desktop/个人资料/referPng/升级建筑确认弹窗.jpg` | 只参考二次确认弹窗沿用主面板的基础材质、描边和四角语言，但取消上下华丽装饰并降低高度；同时参考进一步压暗下层的层级关系 |

两张截图都是第三方游戏内部设计参考，不得进入运行时 Bundle、公开构建或对外
素材包。

禁止参考或复制：双龙/双兽边框、银色金属浮雕、资源图标画法、“升级”按钮、工匠
招募按钮造型、双倍收益、广告图标、字体、数值、具体间距和底部导航。允许参考的只有
“共用基础框 + 主面板上下装饰 + 二次确认只保留基础框”的组件关系。

#### 5.2.2 项目截图

制作和后续回归时可归档以下五张本项目截图。五张使用同一 `375×817` 布局，只改变
弹窗层级、目标资源和资源是否足够；本轮已完成浏览器预览验收，但截图文件尚未归档：

| 截图                                                              | 必须显示                                                                                           | 状态        |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------- |
| `assets/refer/ling_pu_panel_layout_375x817.png`                 | 大厅停在灵源院位置并变暗；P1 弹窗只显示灵粮、灵木、玄铁三条资源行，包含库存/上限、当前产量、升级按钮和 3/2/1 分配；不显示灵晶、庚精占位；底部显示结算倒计时、周期条、杂役招募和关闭 | 待用户预览并归档  |
| `assets/refer/ling_pu_recruit_confirm_375x817.png`              | 二次弹窗显示灵粮图标、配置消耗、当前灵粮、“杂役 +5”、可用的招募和取消按钮                                                        | 预览已验收，待归档 |
| `assets/refer/ling_pu_recruit_insufficient_375x817.png`         | 与上一张布局一致，但当前灵粮小于配置消耗，显示“灵粮不足”，招募禁用、取消可用                                                        | 预览已验收，待归档 |
| `assets/refer/ling_pu_storage_upgrade_375x817.png`              | 从灵粮行打开升级弹窗；显示灵木图标、配置消耗、当前灵木、灵粮当前上限 → 新上限，以及可用的升级/取消按钮                                          | 逻辑已测，待归档  |
| `assets/refer/ling_pu_storage_upgrade_insufficient_375x817.png` | 与上一张布局一致，但当前灵木小于配置消耗，显示“灵木不足”，升级禁用、取消可用                                                        | 预览已验收，待归档 |

截图必须来自 Cocos 预览的 `375×817` 可见区域，不包含浏览器地址栏、开发者工具
或桌面。使用实际 Ark Pixel 字体和最终节点布局；美术尚未完成时可使用 Kenney
面板、纯色节点以及文字 `-`/`+` 占位。

生成时按顺序上传，不要让 GPT 一次生成三张素材：

1. 生成基础九宫格时上传 `交易行面板.jpg`、`升级建筑确认弹窗.jpg`、本项目对应层级
   灰盒截图、`env_camp_panorama_bg.png` 和当前 `ui_ling_pu_panel_frame.png`。两张第三方
   截图只解释组件关系；营地背景只提供色盘；当前面板只提供像素密度和项目材质比例，
   不锁定其现有标题牌和边角造型。
2. 基础九宫格通过后，生成顶部装饰时必须重新上传已确认的
   `ui_common_modal_frame.png` 作为第一风格锚点，再上传 `交易行面板.jpg` 解释“装饰围绕
   标题安全区展开”的结构。不得只凭文字重新猜测基础框样式。
3. 顶部装饰通过后，生成底部装饰时同时上传已确认的基础九宫格和顶部装饰，锁定材质、
   描边、对称轴、像素密度和装饰繁简层级。
4. 三张素材分别生成、分别验收；不得生成一张完整高面板后切出上下装饰，也不得在同一
   张图里输出素材拼版。
5. 加减按钮、通用行内按钮和通用底部按钮都只生成普通态；按下/禁用状态由运行时代码处理。

重做通用底部按钮时，不得把已退役的 `ui_common_button_footer_normal.png` 历史图作为正向风格图上传。
如需让 GPT Image 理解退回原因，只能将它标注为“反例：禁止复制阶梯形外轮廓、外扩侧翼和底部基座”；
正向风格参考使用已确认的新基础框、资源行底板和已生成的通用行内按钮。

## 5.3 共用输出要求与提示词

> 2026-08-08 退役说明：本节旧按钮与周期条提示词只作历史追溯，不得重新生成或放回
> 运行时目录；现行按钮以 Approved Figma 原生组件为准。

- 每次只生成一张可拆分资产，不生成完整页面后再切图。
- 每张素材单独输出真实 Alpha 透明 PNG；面板和行底板必须标明九宫格安全拉伸区。
- 按钮只交付普通态 PNG；按下时只缩放可见子节点，用 `40ms` 缩小到 `94%`、
  `60ms` 回到原大小，按钮根节点和热区不缩放；悬停和禁用状态由 Cocos `Button`
  Tint/透明度表达。
- GPT Image 无法直接输出精确尺寸时，可先生成高清母版，再裁切、补透明边和缩放；
  最终尺寸必须与 5.1 表格一致。
- 运行时使用 Nearest 过滤和整数倍缩放；按钮图片放入至少 `48×48dp` 的节点内。

每条具体提示词前添加：

```text
为原创竖屏2D像素修仙游戏《昆吾禁地》制作灵源院生产弹窗的单个独立UI资产。参考的第三方游戏截图只用于理解“高主面板与矮二次确认弹窗共用基础框，高主面板额外叠加上下装饰，操作时二次弹窗覆盖并压暗主面板”的组件关系和信息层级，不得复制其双龙/双兽边框、银色浮雕、图标、按钮、字体或具体构图。

整体视觉延续《昆吾禁地》山外营地：低饱和黛青、暖褐、深木、旧青铜、黑玉、灰绿和少量朱砂；2D pixel art，清晰像素簇，硬边缘，无抗锯齿，小尺寸清楚。中国修仙题材，古拙、肃穆、实用。基础框保持克制；只有主面板的独立顶部、底部装饰允许提高精致度，但仍不得变成宫殿、牌坊或动物雕像。

只输出下文指定的单个UI资产，透明背景，主体居中，不展示手机界面、完整游戏截图、使用示意图或素材拼版。
```

每条具体提示词末尾添加：

```text
最终交付PNG尺寸必须严格保持为具体提示词指定的@3x尺寸。不要任何汉字、英文字母、数字、资源数量、倒计时、红点、水印、Logo或棋盘格背景；不要双龙、双兽、骷髅、尖刺、银色欧式浮雕、哥特花边、金币、宝石、双倍收益、广告、充值、礼包或购买入口；不要复制参考截图的资源图标、升级/招募按钮造型、二次弹窗边框和其他按钮外形；不要平滑矢量、3D渲染、写实照片、油画、模糊渐变、科幻霓虹或日式神社风格。
```

`icon_action_plus.png` 是调岗增加符号，不是购买入口，具体提示词明确允许生成。

## 5.4 具体提示词

#### 5.4.1 共用基础框与主面板上下装饰

本组必须按“基础框 → 顶部装饰 → 底部装饰”的顺序生成。三张图不是三套弹窗：

```text
主建筑面板 = 同一张基础九宫格（较高） + 顶部装饰 + 底部装饰
二次确认弹窗 = 同一张基础九宫格（较矮）
```

禁止让 GPT 分别自由生成“主面板边框”和“确认弹窗边框”，否则材质、边宽、四角和
像素密度必然漂移。上下装饰都是覆盖层，不包含大面积面板内底。

##### A. 全建筑共用基础九宫格

上传图片角色：

- 图片 1 `升级建筑确认弹窗.jpg`：只参考矮弹窗使用简洁基础框的结构。
- 图片 2 `交易行面板.jpg`：只参考高主面板与矮弹窗属于同一家族，不参考外部装饰造型。
- 图片 3 当前 `ui_ling_pu_panel_frame.png`：只参考《昆吾禁地》现有深木、黑玉、旧青铜
  的材质比例和像素密度；它是待替换旧稿，不要求保留现有标题牌或边角。
- 图片 4 `env_camp_panorama_bg.png`：只参考营地色盘和光照温度。

把 §5.3 共用提示词放在前面，再追加：

```text
制作《昆吾禁地》所有营地建筑主面板与二次确认弹窗共同使用的基础九宫格边框，文件名 ui_common_modal_frame.png。这是两级弹窗唯一的基础框体，不是灵源院专属边框，也不是完整页面截图。

外轮廓为端正、连续的竖向矩形，只允许四角各有一次小斜切。使用低反光深色风化木内板、纤细旧青铜外线和黑玉内沿；四角用极简昆吾山石卡扣收口。上下左右边框的宽度、材质和亮度完全一致，整体克制、清楚、可长期复用。中央内底保持大面积低对比和低纹理，允许放置图标、资源行、说明文字与按钮。

这张基础框不得包含标题牌、标题凹槽、顶部冠饰、底部冠饰、悬挂物、侧柱、脚座、按钮、图标或任何页面专属符号。高主面板与矮二次确认弹窗必须仅通过九宫格拉伸高度形成，不能依靠不同边框区分层级。

基础逻辑尺寸严格为96×96，最终交付严格为288×288像素@3x RGBA透明PNG。上下左右各16个逻辑像素为不可拉伸边框区，对应最终PNG各48像素；中央64×64逻辑像素为安全九宫格拉伸区。要求分别拉伸到约343×650主面板和319×280二次确认弹窗时，四角、边宽、木纹尺度和像素密度保持一致。

不要文字、标题、图标、资源、按钮或装饰覆盖层；不要双龙、双兽、银色浮雕、尖刺、哥特花边、牌坊、城门、建筑横梁或厚重底座。
```

文件：`ui_common_modal_frame.png`

##### B. 主面板顶部三切片装饰

只有基础九宫格通过后才生成本图。把已确认的 `ui_common_modal_frame.png` 作为图片 1，
把 `交易行面板.jpg` 作为图片 2；图片 2 只解释“顶部左右装饰围绕中央标题留空”的结构。

把 §5.3 共用提示词放在前面，再追加：

```text
基于图片1已经确认的《昆吾禁地》基础弹窗框，制作主建筑面板专用的独立顶部三切片装饰，文件名 ui_common_panel_top_ornament.png。它将作为透明覆盖层压在同一张基础九宫格的上边框，二次确认弹窗不使用本装饰。

装饰沿水平中轴左右对称，使用与基础框完全一致的旧青铜、黑玉、深木明暗和像素密度。左右两端各使用克制但精致的昆吾山脊剪影、断裂阵纹、古铜云雷纹和小块黑玉嵌片构成向中央收拢的边饰；装饰层级可以明显高于基础框，但不得出现动物、人物、建筑或武器。

顶部中央必须保留连续、平整、完全无遮挡的标题安全区。源图中央至少64×24逻辑像素只保留低对比黑玉或深木横梁，不画符纹、尖角、玉片或发光；运行时横向拉伸后用于放置“灵源院”“交易行”“百宝库”等Cocos Label标题，但PNG中不得出现任何文字。标题安全区两侧的装饰重量、最高点和留白必须严格对称。

逻辑尺寸严格为128×48，最终交付严格为384×144像素@3x RGBA透明PNG。左右各32个逻辑像素为不可拉伸装饰端，中间64个逻辑像素为可横向拉伸标题区；除装饰和上边框接触区域外全部透明。适合三切片拉伸到343–359逻辑像素宽，不能包含面板内底或左右完整侧边。

不要双龙、双兽、鸟翼、兽首、骷髅、刀剑交叉、银色欧式浮雕、哥特尖刺、宫殿飞檐、牌坊、巨大宝石、文字或Logo；不要复制图片2的具体轮廓和构图。
```

文件：`ui_common_panel_top_ornament.png`

##### C. 主面板底部三切片装饰

只有顶部装饰通过后才生成本图。把已确认的基础九宫格作为图片 1、顶部装饰作为图片 2，
用它们锁定材质、边宽、对称轴和像素密度；第三方截图不再作为主要风格输入。

把 §5.3 共用提示词放在前面，再追加：

```text
基于图片1的基础弹窗框和图片2已确认的顶部装饰，制作同组主建筑面板专用的独立底部三切片装饰，文件名 ui_common_panel_bottom_ornament.png。它作为透明覆盖层压在基础九宫格的下边框，二次确认弹窗不使用本装饰。

底部装饰与顶部装饰使用完全相同的旧青铜、黑玉、深木、描边粗细、像素密度和左右对称轴，但整体高度更低、视觉重量更轻。左右两端使用收束的昆吾山石折线、古铜阵纹和小块黑玉卡扣向中央延伸，中部保持连续平直，不遮挡底部按钮、倒计时或内容区，也不形成承重底座。

逻辑尺寸严格为128×36，最终交付严格为384×108像素@3x RGBA透明PNG。左右各32个逻辑像素为不可拉伸装饰端，中间64个逻辑像素为可横向拉伸的低纹理边线；除装饰和下边框接触区域外全部透明。适合三切片拉伸到343–359逻辑像素宽，不能包含面板内底或左右完整侧边。

不要脚座、底座、台阶、托架、侧柱、建筑横梁、牌匾、动物、骷髅、刀剑、银色欧式浮雕、哥特尖刺、巨大宝石、按钮、文字或Logo。底部装饰不能比顶部装饰更华丽、更高或更亮。
```

文件：`ui_common_panel_bottom_ornament.png`

导入时基础框使用九宫格，四边不可拉伸区统一为 `48 px`；顶部、底部装饰使用横向
三切片，左右不可拉伸区各 `96 px`。主面板先渲染基础框，再叠加上下装饰；二次确认
弹窗只渲染基础框。当前 Prefab 仍处于旧 `1080×1920` 工程坐标，接入时仍需明确
区分 `375×817` 逻辑尺寸与旧工程坐标，不得直接把 `@3x` 源图像素当作逻辑尺寸。

#### 5.4.2 全建筑共用列表项普通态与选中态完整底板

本组服务灵源院资源行、入山装载行、修士列表、地图列表、交易条目、建筑升级项和后续
其他纵向列表。固定组合关系为：

```text
普通项 = ui_common_list_item_normal.png
选中项 = ui_common_list_item_selected.png
禁用/锁定项 = 普通项 + 运行时降亮度 + 锁图标 + 原因文字
```

普通态与选中态都是完整、可见、不透明的无框底板，运行时在同一个 Sprite 节点上切换
SpriteFrame，不再依赖透明叠层。选中态必须在普通态通过后，以普通态原图作为编辑目标
生成；只改变整行底色、明度与极轻材质层级，不加入固定选中图标、边框或页面装饰。
悬停、按下、禁用和锁定状态不增加近似 PNG。

##### A. 通用列表项普通态无框九宫格底色层

上传图片角色：

- 图片 1 已确认的 `ui_common_modal_frame.png`：只锁定新面板系统的黛青、黑玉、深木
  材质比例和像素密度；不得继承它的外框、内沿、包角或斜切轮廓。
- 图片 2 历史已退役的 `ui_ling_pu_resource_row.png`：只参考列表项低对比、让位于文字和图标的
  视觉权重；它是待替换旧稿，不要求保留其现有边角和尺寸。
- 图片 3 `交易行面板.jpg`：只参考纵向列表的信息密度，以及每项都具有独立背景的层级；
  不参考外部边框、按钮、图标、色盘或具体构图。
- 图片 4 `/Users/zhangxiaoen/Desktop/个人资料/referPng/微信图片_20260801145431_50_209.jpg`，
  `1256×2808`：按图面实际呈现，前四项是选中态，底部两项是普通未选中态。只参考
  “普通态接近面板内底、选中态整行切换为较亮暖灰褐/灰橄榄实色”的状态关系；不得
  复制人物、头像框、星级、名称、数值、序号飘带、选择/取消按钮、银色面板框或布局。

本条使用下面的完整提示词，不再拼接 §5.3 的灵源院限定开头：

```text
Use case: stylized-concept
Asset type: 原创竖屏2D像素修仙游戏《昆吾禁地》的全建筑、全列表共用列表项普通态九宫格底色层
Input images: 图片1是已确认基础弹窗框，只锁定低饱和黛青、黑玉、深木、灰绿色盘和硬边像素密度，不继承外框、内沿、包角或斜切轮廓；图片2是待替换旧列表项，只参考低视觉权重；图片3只参考纵向列表信息密度；图片4是状态关系参考，图面前四项为选中态、底部两项为普通未选中态，本次只参考底部两项接近面板内底的低对比关系。所有图片均不是编辑目标，不得复制人物、图标、文字、按钮、飘带、面板边框或具体布局
Primary request: 生成一个单独的空白普通态列表项底板，文件名 ui_common_list_item_normal.png。它将用于资源行、装载行、修士列表、地图列表、交易条目、建筑升级项和后续纵向列表，不得出现任何页面、资源、职业、品质、阵营或功能专属符号
Style/medium: 2D pixel art，硬边缘，清晰像素簇，无抗锯齿，无平滑矢量感；视觉层级明显低于外层弹窗、标题、按钮、头像和物品图标
Shape: 最普通的连续横向矩形无框底色层，不是卡片、按钮或缩小版面板。不画任何可见外框、内框、连续描边、包角、端盖、侧柱、脚座、浮雕、标题牌或四角斜切；四条画布边界只是图片裁切边，不形成描边
Color and material: 使用完整、连续、低反光的深黛青黑玉与极少量深木灰褐色，整体接近弹窗内底，只比弹窗内底略亮、略暖半个克制层级。参考图片4底部普通项“安静融入内容区”的关系，但不直接复制黑色透明效果。表面只允许极轻、均匀、无方向性的像素颗粒，不画可辨认污渍、纸张破口、云纹、符纹或固定装饰
Content layout: 底板中央保持平整、低纹理、低对比，允许运行时自由放置左侧图标或修士头像、中部一至三行文字、右侧状态或行内按钮；不画固定分栏、分隔线、边缘高光、暗边或阴影。多条上下排列时应读作同一内容区内自然分行，不形成一排独立卡片
Size and slicing: 375×817 UI基准下的逻辑源图尺寸严格为64×32，最终交付PNG严格为192×96像素@3x。左右各12逻辑像素、上下各8逻辑像素为不可拉伸区，对应最终PNG左右各36像素、上下各24像素；中心40×16逻辑像素为安全九宫格拉伸区。拉伸到约280–327逻辑像素宽、50–106逻辑像素高时，底色、颗粒和像素密度必须保持均匀，不出现接缝、边框或角部装饰
Opacity invariants: 矩形画布从左上角到右下角必须被普通态实色底面完整填满，每个像素完全不透明，Alpha必须为100%。严禁透明或半透明像素、透明背景、透明留白、棋盘格背景、只有纹理没有底色，不能把它做成需要叠在其他底图上的透明遮罩
Output constraints: 只输出一个空白普通态列表项底色层，不展示完整页面、列表拼版、普通/选中对比图或使用示意；不要文字、数字、图标、人物、头像、按钮、复选框、对勾、锁、价格、资源、进度条、红点、序号飘带、水印或Logo
Avoid: 任何外框、内框、描边、包角、角标、四角斜切、边缘高光、投影、银色边框、双龙、双兽、哥特尖刺、霓虹、厚重卡牌感、强高光、大面积发光、透明玻璃、羊皮纸卡片、现代扁平白卡、3D渲染、写实照片、油画笔触
```

文件：`ui_common_list_item_normal.png`

##### B. 通用列表项选中态完整底板

只有普通态底板通过后才生成本图。把已确认的 `ui_common_list_item_normal.png` 作为
图片 1 并明确标注为“编辑目标”；把本次
`微信图片_20260801145431_50_209.jpg` 作为图片 2 并明确标注为“状态关系参考”。不再
上传其他第三方截图或弹窗框，避免重新引入外框、装饰和材质漂移。

本条使用下面的完整提示词：

```text
Use case: precise-object-edit
Asset type: 《昆吾禁地》全建筑、全列表共用列表项选中态完整九宫格底板
Input images: 图片1是唯一编辑目标，即已经确认的 ui_common_list_item_normal.png；图片2只作为状态关系参考，图面前四项是选中态、底部两项是普通未选中态，只参考“选中后整行由深色切换成低饱和暖灰褐/灰橄榄实色”的层级关系，不复制人物、头像框、星级、文字、数值、红色序号飘带、选择/取消按钮、银色面板框、污渍位置或布局
Primary request: 只把图片1改成选中态完整底板，文件名 ui_common_list_item_selected.png。它必须是一张单独显示时就完整可见的实色背景，不是透明叠层、透明蒙版、发光覆盖层、选中描边或使用示意图
Preserve invariants: 严格保持图片1的64×32逻辑尺寸、192×96像素@3x画布、连续矩形外形、无框结构、四边覆盖范围、像素密度、受光、内容安全区和九宫格切线不变；四条画布边界和每个像素的覆盖范围与图片1逐像素对齐。不要新增或删除任何透明区域，不改变外轮廓，不移动九宫格切线
State change only: 只改变完整底面的色相、明度和极轻材质层级。将整行从普通态深黛青黑玉均匀切换为低饱和暖灰褐、灰橄榄与少量旧木褐混合的实色底面，整体提高约一个半克制明度层级，使它在纵向列表中一眼可与普通态区分，但仍低于弹窗标题、主操作按钮和危险警告。参考图片2前四项的暖色整行选中关系，但降低黄度、饱和度和脏污强度，使其符合《昆吾禁地》的黛青、黑玉、深木、旧青铜色盘
Material: 中央继续保持完整、连续、低反光的实色底面，只加入极轻、均匀、无方向性的旧石或旧纸纤维像素斑驳，用于避免纯色僵硬；斑驳不能形成可辨认图案、单侧大黑斑、烧焦边、破口、裂纹、云纹或符纹，也不能降低上层米白文字、资源图标和修士头像的可读性
Selection semantics: 不在图片中加入固定选择符号。选中序号、已选择/取消文字、队伍槽位编号、按钮和状态图标全部由运行时独立节点表达；底图只负责整行底色变化。不得加入朱砂竖条、对勾、圆点、飘带、数字、角标或发光边
Size and slicing: 九宫格不可拉伸区与图片1完全一致，左右各12逻辑像素、上下各8逻辑像素，对应最终PNG左右各36像素、上下各24像素；中心40×16逻辑像素保持均匀实色并可安全拉伸。拉伸到约280–327逻辑像素宽、50–106逻辑像素高时，不出现接缝、边框、重复污渍或材质拉花
Opacity invariants: 矩形画布从左上角到右下角必须被选中态实色底面完整填满，每个像素完全不透明，Alpha必须为100%。严禁透明或半透明像素、透明背景、透明留白、棋盘格背景以及只有色光、标记或纹理而没有完整底色；绝对不能输出一张透明选中覆盖层
Output constraints: 只输出一个选中态完整底板，不并排展示普通态，不展示完整页面、列表拼版或使用示意；不要文字、汉字、字母、数字、图标、人物、头像、按钮、复选框、对勾、锁、红点、序号飘带、水印或Logo
Avoid: 任何外框、内框、连续描边、包角、四角括线、角标结构、边缘高光、发光边、整行发光、大面积朱红、蓝色霓虹、呼吸光、粒子、银色边框、厚重卡牌、羊皮纸卡片、页面专属装饰、突出侧翼、标签页、飘带、底座、3D渲染、写实照片、油画笔触
```

文件：`ui_common_list_item_selected.png`

导入时两张 SpriteFrame 使用完全相同的九宫格切线：左右 `36 px`、上下 `24 px`。
普通态与选中态复用同一个 Sprite 节点，选中时只替换其 SpriteFrame，不叠加第二个背景
节点。悬停、按下、禁用使用 Cocos Tint/Tween，锁定继续叠加通用锁图标和原因 Label。
选中态还必须保留“已选择/取消/槽位编号”等运行时文字之一，不能只依赖底图色差。

#### 5.4.3 增加与减少按钮

两张小按钮分别包含完整底板和增加/减少符号，不再拆分底板或制作按下/禁用 PNG：

| 按钮 | 具体提示词                                                                                                                   | 文件名                     |
| -- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 增加 | 制作灵源院分配杂役使用的方形增加按钮。深木外框、旧青铜包角、低反光黑玉内底，中央用两根短白玉筹条横竖交叠成清楚的增加符号；这是调岗按钮，不是购买、充值或货币入口。逻辑尺寸严格为40×40，最终透明PNG严格为120×120 px（@3x）。 | `icon_action_plus.png`  |
| 减少 | 制作与增加按钮同组的方形减少按钮。保持相同画布、外框、材质、体量和透明边界，中央只保留一根横向短白玉筹条。逻辑尺寸严格为40×40，最终透明PNG严格为120×120 px（@3x）。                            | `icon_action_minus.png` |

#### 5.4.4 通用行内按钮与通用底部按钮

这两张是全局通用组件，不是灵源院专属素材。生成时不使用 §5.3 中“灵源院生产弹窗”的
限定性开头，改用下面这段通用开头：

```text
为原创竖屏2D像素修仙游戏《昆吾禁地》制作单个全局通用横向按钮组件。按钮将在多个生产、编队、地图、确认与系统面板里复用，不得出现灵田、药草、农具、职业、地图或其他页面专属符号。

整体延续《昆吾禁地》低饱和黛青、暖褐、深木、旧青铜、黑玉和灰绿色盘；2D pixel art，硬边缘，清晰像素簇，无抗锯齿。只输出指定的单个按钮底图，真实 Alpha 透明背景，不含文字、图标或展示场景。
```

通用行内按钮：

```text
制作列表行、资源行和信息卡片内部使用的通用行内按钮普通态。它可承载“升级”“选择”“取消”“调息”等紧凑操作，文案全部由 Cocos Label 渲染。

造型是薄而平的黛青黑玉签牌：只使用一层极细旧青铜或灰绿描边，四角做克制的小斜切，内底低反光、中央干净。只允许一条极弱的上沿亮线表示可点击，不要厚重的立体台阶、外伸托架、左右支柱、脚座、大块灰白玉、强高光或浮雕。它在信息行中的视觉权重必须低于行文字和资源图标。

九宫格源图逻辑尺寸必须严格为72×28，最终交付透明PNG尺寸必须严格为216×84 px（@3x）。四角、斜切和描边全部放在左右各约10个逻辑像素、上下各约6个逻辑像素的不可拉伸区，中心保持低纹理，可横向拉伸至约56–120×28。可见按钮可小于48dp高，但运行时必须放入至少48dp高的独立交互节点。不要文字、图标、对勾、价格、资源、宝石或页面专属纹样。
```

历史退役文件名：`ui_common_button_inline_normal.png`

通用底部按钮：

```text
制作面板底部固定操作区与二次确认弹窗使用的通用底部按钮普通态。它可承载“招募”“关闭”“升级”“取消”“完成”“返回”“启程”“离开”等面板级操作，文案全部由 Cocos Label 渲染。同一底图同时支持主操作和次操作，两者只通过运行时 Tint、Label 颜色和明度区分，不再制作第二张近似底图。

外轮廓必须是一个标准、连续、低调的横向长方形 UI 按钮：上边和下边是两条连续平直的水平线，左右边是等高的垂直线；四角最多只允许 `1–2` 个逻辑像素的单次小斜切，不得出现连续多层退让。整个按钮必须能被一个规整长方形紧密包住，边框、高光、暗角和材质纹理全部放在这个长方形内部。

材质使用低反光深黛玉或暗木内底，外围只有一层细旧青铜描边和一条极弱的上沿亮线。与行内按钮的区别来自更大的高度、更宽的文字区和稍暖的内底，不来自异形外轮廓或建筑式装饰。造型必须明确读作现代游戏 UI 中的平面按钮，不能读作木制牌匾、石制牌坊、建筑横梁或面板基座。

严格禁止：阶梯形、台阶形、多层退台、梯形外扩、左右扩展翼、侧边柱、突出端盖、下方基座、底座、脚座、托架、横梁、牌匾、牌坊、城门、建筑构件感；禁止任何装饰突破长方形外轮廓，禁止用透明缺口做出层层缩进的边缘。

在 `375×817` UI 基准中的逻辑显示尺寸必须严格为 `132×44`，重做后最终交付透明 PNG 尺寸必须严格为 `396×132 px (@3x)`。使用九宫格横向拉伸时，只允许中央内底变宽，四角、描边和按钮高度完全不变。不要文字、图标、对勾、价格、资源、宝石、奖励光芒或页面专属纹样。
```

历史退役文件名：`ui_common_button_footer_normal.png`

两张文件输出至 `assets/bundles/camp/ui/common/`。按下、悬停和禁用态由 Cocos
Tween/Tint 生成，不需要额外提示词或 PNG。

#### 5.4.5 资源图标

灵源院直接复用大厅顶部 HUD 的资源图标与提示词，见
[正式营地 HUD 分册 §4.4 五种顶部资源图标](../../ArtAssets/02_营地HUD.md#44-顶部-ui-提示词)。
当前灵粮、灵木、玄铁运行时图标均已存在并由 Cocos 导入；灵源院直接复用，不再生成
另一套图标。

#### 5.4.6 生产周期条（历史方案，已退役）

进度条底：

```text
制作灵源院30秒生产周期的横向像素进度条底。深木与旧青铜组成细长凹槽，内部为低反光黑玉，左右端对称，整体克制。逻辑尺寸严格为160×10，最终透明PNG严格为480×30 px（@3x）。不要填充、图标、刻度、数字或文字。
```

历史退役文件名：`ui_production_progress_track.png`

进度条填充：

```text
制作叠加在生产周期条底上的独立填充条。逻辑尺寸严格为160×10，最终透明PNG严格为480×30 px（@3x）；填充是一条清晰的暗青绿至暖金像素灵液，左端完整、右端平直，运行时通过裁切改变长度。不要外框、数字、文字、固定百分比或大面积柔光。
```

历史退役文件名：`ui_production_progress_fill.png`

## 5.5 制作顺序与验收

1. 先完成灵源院主弹窗、杂役招募弹窗和储量升级弹窗灰盒，截取 §5.2.2 的五张项目截图。
2. 先只生成共用基础九宫格，同时合成一张 `343×650` 主面板和一张 `319×280`
   二次确认弹窗，确认两者的边宽、四角、材质和像素密度完全一致。
3. 基础框通过后依次生成顶部、底部装饰；只给主面板叠加，在标题区放入实际 Label
   检查留白，二次确认弹窗继续只显示基础框。
4. 先生成通用列表项普通底板，并分别拉伸到灵源院资源行、入山装载行、修士列表和
   地图列表的实际尺寸检查；普通态通过后，再以它为编辑目标生成选中态完整底板。
5. 在灰盒截图中同时合成普通项、选中项和锁定项，检查 P1 三行无重叠，且库存/上限、
   升级、分配和选中状态清楚。
6. 保留已通过方向确认的加减按钮和通用行内按钮，按新提示词重做通用底部按钮；
   重点验收连续矩形外轮廓与运行时状态。
7. 复用或补齐资源图标，最后生成周期条。
8. 所有素材导入 Cocos 后设置 Nearest；基础框和列表项使用九宫格，上下装饰使用横向三切片，
   并按表格中的逻辑尺寸显示。

验收：

- 点击灵源院后只出现一个纵向生产弹窗，大厅在后方变暗且不发生场景切换。
- P1 主弹窗只显示灵粮、灵木、玄铁三条可操作资源行；未开放的灵晶、庚精不显示，
  且没有产量升级、双倍收益、广告或付费入口。
- 玩家能在 3 秒内看到各岗位 `x/y`、库存/上限、当前产量、升级和 `-`/`+`。
- 灵粮行产量随灵粮岗人数增加，并随灵木/玄铁岗位维护实时减少；页面不显示储量等级、
  “下周期”汇总或“全部岗位可正常生产”文案。
- 主弹窗底部只有“杂役招募/关闭”，不存在“确认分配”按钮。
- 主面板与招募/储量升级二次弹窗使用同一张 `ui_common_modal_frame.png`；二次确认仅
  改变高度，不存在另一张确认弹窗边框。
- 只有主面板叠加顶部、底部装饰；二次确认弹窗不得出现装饰残片、标题冠饰或底部冠饰。
- 主面板顶部装饰中央能稳定放入不同长度的建筑标题，文字四周留白充足，装饰不穿过
  标题、不压住首行内容。
- 上下装饰的材质、描边和像素密度与基础框一致；底部装饰明显低于、轻于顶部装饰，
  不读作承重底座。
- 普通列表项只靠低对比底色和轻微材质变化形成清楚但低调的分行，不抢正文、图标、
  头像和按钮；多行连续排列时不会形成独立卡片感，也没有可见外框、内框或包角。
- 普通态和选中态底板的尺寸、外轮廓、九宫格切线及画布边界逐像素一致；选中时没有
  跳动、缩放或内容位移。
- 选中态同时具有整行轻微明度变化、左侧朱砂短标及运行时文字，不只依赖颜色；不得
  出现外框、内框、包角、角标结构或发光边，朱砂短标不会被误认成红点、警告或付费入口。
- 选中底板位于头像、资源图标、文字、按钮和进度信息下层；禁用与锁定项继续使用
  降亮度、锁图标和原因文字，不新增第三张背景。
- 灵粮不足时二次弹窗的招募按钮禁用，取消按钮仍可辨识且可用。
- 灵木不足时储量升级弹窗的升级按钮禁用；灵木充足时明确显示升级后的最大储量。
- 灵粮、灵木、玄铁与大厅顶部使用相同图标文件。
- 资源行内的“升级”只使用通用行内按钮，轮廓薄、对比低，不再出现旧按钮的厚木桩和灰白玉牌。
- 主弹窗和确认弹窗底部只使用通用底部按钮；必须是连续规整的横向长方形，不得有阶梯形、外扩侧翼、突出端盖或底部基座。
- 弹窗、资源行和按钮缩放后像素清楚；基础九宫格和上下三切片均没有拉伸破损。
- 按钮按下时在 `100ms` 内完成 `94%` 缩小回弹，不改变热区；悬停和禁用 Tint 能清楚区分可用状态。
- 灵粮不足与停工通过文字说明，不只依赖颜色。
- 所有文案和数值由 Label 渲染，PNG 中没有烘焙文字或数字。
- 美术只借鉴参考截图的组件继承和信息层级，没有复制双龙/双兽边框、银色浮雕、
  外部图标或按钮。
- 所有按钮节点至少 `48×48dp`，在目标分辨率和安全区内无遮挡。
