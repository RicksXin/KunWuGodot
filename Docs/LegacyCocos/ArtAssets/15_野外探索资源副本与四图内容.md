# 《昆吾禁地》野外探索、资源副本与四图内容素材

版本：1.0-art-brief.9  
日期：2026-08-07  
状态：地图生产流程已统一为“上游规则 → Figma 灰盒 → GPT Image 视觉母版 → Tile Set → 人工清稿与拼装”；`map_01` 的 Tile Set 需求图与 A–F 批次输入映射已补齐，灰盒和素材待制作  
对应 PRD：[入山整备](../PRD/04_出征探索副本与战斗_PRD.md#page-05)、
[野外探索](../PRD/04_出征探索副本与战斗_PRD.md#page-06)、
[资源副本](../PRD/04_出征探索副本与战斗_PRD.md#page-07)、
[战斗](../PRD/04_出征探索副本与战斗_PRD.md#page-08)

## 1. 范围、事实源与交付总表

### 1.1 事实源

四图轮廓、坐标、地标、对象、敌群与遮挡以 [地图分册](../1.0策划案/地图/README.md) 为准；敌人和
Boss 以 [敌人与 Boss](../1.0策划案/数值/19_1.0敌人与Boss战斗数值_草案.md) 为准；两座资源副本以
[资源副本策划](../1.0策划案/地图/32_可重复资源副本与新修士追赶机制_草案.md) 为准。

美术清单必须从最终地图对象表和敌人内容表生成，不以截图里“看起来有几个”估算。每项记录
`content_id / map_id / state / sprite_path / animation / source / license / status`。

### 1.2 四图与资产范围

| 地图 | 地形/建筑核心 | 必需地标 | 敌人/Boss 核心 | 状态 |
|---|---|---|---|---|
| `map_01` 破禁山麓·万修之门 | 青灰山道、废营、断裂阵灯、山腹暗道 | 旧营阵、万修之门残碑、三盏阵灯、石阶捷径 | 石灵、尸傀、守门石灵 | 首图试制规范已整理；灰盒待制作 |
| `map_02` 白玉广场 | 白玉庭、四方阵眼、灵木侵蚀 | 四阵眼、封坛、庭院/废营 | 尸傀、木魅、封坛尸将 | 等待独立灰盒 |
| `map_03` 灵宝遗址 | 外苑、灵宝阁残层、铸灵机关 | 铸灵堂、机关区、回火矿窟入口 | 铸灵傀儡、邪修、器灵、四臂铸灵傀 | 等待独立灰盒 |
| `map_04` 镇魔塔外层 | 黑石塔基、封链、裂隙、魔雾 | 封魂间、封链枢纽、镇魂残响入口 | 高阶尸傀、魔气孽物、残魂、银翅夜叉化身 | 等待独立灰盒 |

每图至少交付：Figma 灰盒与对象标注、地形 Tile Set、边缘/转角、阻挡体、可淡出前景、独立地标与状态、
地图缩略图、区域卡图、交互对象普通/高亮/已处理态、普通/精英敌人世界棋子、Boss 世界棋子和对应战斗背景。

### 1.3 生产职责边界

| 参与方 | 负责 | 不负责 |
|---|---|---|
| 上游策划/PRD | 地图尺寸、路线职责、对象数量、坐标、状态、移动与消耗语义 | 像素风格和 Tile 绘制 |
| Figma 灰盒 | 规则网格、可走区、阻挡、地形消耗、进出口、地标占位、对象静区和视窗测试 | 正式地图美术 |
| GPT Image | 风格探索、地图视觉母版、分组素材母版 | 决定路线、碰撞、对象数量、未冻结坐标、无缝 Tile 和最终地图 |
| 人工像素清稿 | 统一像素密度、真实 Alpha、无缝拼接、正式 `16×16` Tile、状态与锚点 | 修改上游地图规则 |
| Tiled/Cocos | 按灰盒拼图、碰撞、遮挡、对象、迷雾、路径与运行时覆盖标记 | 使用 GPT 整图覆盖结构 |
| 用户验收 | 俯视感、路线可读性、标记清晰度、Tile 拼接和最终风格 | 由 Codex 代替执行预览或编辑器验收 |

## 2. 共用像素、图层与视觉红线

### 2.1 世界像素基线

- 世界 Tile 源像素 `16×16`，逻辑显示 `48×48`；Pixel Art 使用 nearest。
- 地图 UI 仍以 `375×817` 视窗设计，Tile 规格不反推 UI 布局。
- 地形、阻挡、前景遮挡、交互高亮和碰撞语义分层交付，不把整张大地图烘焙成一张图片。
- 屋顶、树冠、拱门、前景山石、封链等遮挡层必须有可淡出区域和锚点说明。

### 2.2 最终图层顺序

```text
低细节地形底层
→ 道路/地形消耗/阻挡的平面大轮廓
→ 关键地标与可淡出前景
→ 世界对象棋子
→ 队伍、资源、敌群、Boss、入口等 UI Overlay 标记
→ 迷雾、选中、锁定与路径预览
```

- 队伍、资源点、敌群、Boss、副本入口、出生点和跨图出口不得烘焙进地形或地图视觉母版。
- 交互对象、状态变体和可淡出前景必须与基础地形分层。
- 迷雾使用运行时 Shader/Mask/Tile 状态，原则上不导出未知/已发现/可见三张完整地图。

### 2.3 导航底图优先

- 可玩区域约 `75%–85%` 使用低饱和、低对比、大面积连续平面。
- 普通可走底面只使用 `3–5` 个接近明度的基础色。
- 普通地面不允许每格随机散布碎石、草叶、裂纹、灵光、铜屑、尸骸、落叶或独立阴影。
- 细节预算只留给一、二级地标、进出口、Boss 区、封链/阵眼等导航结构。
- 不为每个 Tile 画黑色闭合外轮廓，正式地图不显示棋盘格或网格边界。
- 灰盒已确认的对象格中央至少保留约 `32×32` 逻辑像素标记静区，不放高光点、裂纹交点、小植物、尖锐黑影或近似标记颜色的点状装饰。
- 普通背景不得依赖更亮外发光、白色贴纸边或黑色厚底盘补救标记可读性。

### 2.4 纯俯视平面 2D 红线

- 摄像机固定为接近 `90°` 垂直向下的正交投影；无地平线、消失点、近大远小、透视收窄或斜向缩短。
- 所有可走区都是同一层完全平整的 2D 色块；不显示地面厚度、台阶立面、平台侧壁、崖壁侧面、深坑内壁或向下纵深。
- 高差与阻挡只用平面轮廓、边界、局部遮挡和大色块表达，不使用立体台地、悬浮岛或立体沟壑。
- 建筑、山门、阵眼、封链和机关只显示平面图形或正上方顶部剪影，不显示大面积正立面、侧面、厚度或内部纵深。
- 光影只允许极弱的平面明暗分区；禁止体积光、环境遮蔽、边缘光、法线高光、物体投影和用阴影塑造体积。
- 任意普通行走区都必须像平铺的 2D 地图，不像高处斜看的场景、沙盘或微缩模型。

### 2.5 Alpha 与禁止背景

- 地面 Tile 使用实际地表色铺满边缘；阻挡、地标、贴花、前景、图标和标记使用真实 RGBA Alpha。
- 绝对禁止 `#00FF00`、任何绿色键色、白底、黑底、棋盘格或投影展示板伪装透明。
- GPT 未返回真实 Alpha 时，只能作为人工清稿母版，不能直接导入游戏。

## 3. 地图素材统一生产工作流

本节是四张地图唯一的正式生产入口。每张地图都必须先有上游规则和 Figma 灰盒，之后才允许使用 GPT Image。
`3.3`、`3.4` 的提示词只生成视觉母版；`3.6` 的提示词只生成分组素材母版。任何 GPT 输出都不能绕过
Tile Set 拆分、人工像素清稿和 Tiled/Cocos 重拼。

### 3.1 阶段、输入、输出与回退

| 阶段 | 主要输入 | GPT Image | 阶段输出 | 通过门槛 | 失败回退 |
|---|---|---|---|---|---|
| 1. 上游冻结 | 地图分册、PRD、数值中的地图尺寸/消耗/对象量 | 不参与 | 地图规范表、对象清单、状态清单 | 无来源冲突；未冻结项明确标记 | 回上游分册 |
| 2. Figma 灰盒 | 地图规范表、候选对象锚点、375×817 视窗 | 不参与 | 标注灰盒、碰撞图、GPT clean guide、标记测试 | 路线可达、地形消耗清楚、对象静区成立 | 改灰盒 |
| 3. 地图视觉母版 | 灰盒四类导出、视角参考、失败反例 | 参与 | 只含地形/大轮廓/关键地标的视觉母版 | 纯俯视、低噪点、不改路 | 改提示词或重做母版 |
| 4. Tile Set 需求表 | 灰盒与视觉母版 | 不参与 | terrain/连接/边缘/转角/阻挡/地标清单 | 灰盒每个格与状态都有资产去向 | 回灰盒或拆分表 |
| 5. 分组素材母版 | 已确认视觉母版、Tile Set 需求表、像素参考 | 参与 | 地面、道路、阻挡、前景、地标、贴花母版 | 同批比例/色盘/投影一致 | 单批重生，不重做整图 |
| 6. 人工像素清稿 | 分组母版 | 不参与 | 正式 `16×16` Tile、透明对象、状态变体 | 无缝、硬边、真实 Alpha、统一像素密度 | 回对应素材批次 |
| 7. Tiled/Cocos 拼装 | 灰盒、正式 Tile、对象表 | 不参与 | 分层地图、碰撞、遮挡、对象与迷雾 | 结构与灰盒一致，不引用 GPT 整图 | 回 Tile 或拼装 |
| 8. 合成验收 | 375×817 探索页、七种标记、迷雾与路径 | 不参与 | 用户确认的地图试制视窗 | 俯视感、导航、标记和拼接全部通过 | 回底图/标记/图层 |

### 3.2 GPT Image 地图输入包

每张地图单独制作并按下列顺序上传；文件名中的 `map##_` 替换为实际地图 ID：

| 顺序 | 建议文件名 | 必须展示什么 | 用途 |
|---|---|---|---|
| 图片1 | `map##_gpt_clean_guide.png` | 无网格文字的纯色可玩轮廓、道路、危险地形、阻挡、地标占位和静区 | 最高结构事实源 |
| 图片2 | `map##_layout_collision.png` | 网格、可走区、地形消耗、阻挡、进出口、对象锚点和图例 | 解释图片1的语义，不作为美术内容 |
| 图片3 | `map##_marker_test.png` | 七种标记或同轮廓占位符以实际逻辑尺寸叠加到典型区域 | 前期控制底图噪点；正式标记完成后必须替换占位符回归，不允许临时放大或加底盘 |
| 图片4 | `screenshot_exploration_viewport_375x817.png` | 最终探索视窗、HUD、迷雾、队伍和多个点位 | 检查最终缩放和 UI 压力 |
| 图片5 | `screenshot_map_topdown_2d_reference.png` | 已确认的正交纯俯视、平面 2D 地图裁切 | 只锁定视角和无侧面处理 |
| 图片6（可选） | `map##_current_overdetailed.png` | 当前过度精细或带 2.5D 感的失败图 | 只作反例，不继承结构和纹理密度 |

不得用 GPT 根据策划文字直接猜路线。图片1、2缺失时停止地图母版生成，先完成 Figma 灰盒；图片6不能取代
图片1、2。除图片5外，不上传其他游戏高精度地图、写实风景、绿色键色底或带水印图片。

### 3.3 GPT Image 地图视觉母版共用提示词

复制本段后追加对应地图在第 4 或第 5 节的 `Specific map`。这是视觉母版提示词，不是最终地图提示词。

```text
Use case: stylized-map-concept
Asset type: 原创竖屏2D像素修仙游戏《昆吾禁地》的纯俯视平面野外地图视觉母版；它用于确认地形、色盘、视角、细节密度和关键地标视觉，不是可直接导入游戏的完整地图PNG、3D场景、战斗背景或完整UI页面
Input priority: 图片1是必须严格保持的clean guide与最高结构事实源；图片2只解释可走区、地形消耗、阻挡、进出口、地标占位和对象静区；图片3只用于控制底图噪点，绝对不放大、加白边、加黑底或重画标记；图片4是375×817最终视窗参考；图片5只锁定正交纯俯视和平面感；若提供图片6，它只是过度精细和2.5D的失败反例。所有输入图都不是可以自由重构的布局灵感图
Primary request: 严格保留图片1、2已确认的画布比例、可玩轮廓、主支路、地形消耗区域、阻挡、高差语义边界、地标占位、进出口和对象静区，只把灰盒转译为低细节纯俯视2D视觉母版。未冻结对象不推断坐标、不新增点位，普通地面的空旷是有意设计
Projection/camera: 固定正交投影，摄像机接近90度垂直向下。无地平线、消失点、近大远小、透视收窄、斜向缩短或斜45度等距视角
Ground-plane rule: 所有可走区位于同一张完全平整的2D平面，只用轮廓和色块区分；不显示地面厚度、台阶立面、平台侧壁、崖壁侧面、深坑内壁、悬浮岛底部或向下纵深
Landmark projection: 建筑、山门、阵眼、封链和机关只使用平面图形或正上方顶部剪影，不显示大面积正立面、侧面、拱门纵深、立柱体积或建筑厚度
Lighting/shadow: 使用平涂本地色和极弱平面明暗分区，不用投影、环境遮蔽、边缘光、法线高光、体积光、倒角明暗或阴影塑造体积
Visual hierarchy: 约75%–85%画面使用低饱和、低对比、大面积连续平面；约10%–20%用于道路边、阻挡与高差语义大轮廓；只有极少区域用于一、二级关键地标和功能色
Terrain treatment: 普通可走底面只使用3–5个接近明度的基础色和稀疏大色面。不要每格散布碎石、裂纹、草丛、落叶、灵光点、碎骨、密集高光或随机噪点，不为普通区域设置独立视觉中心
Marker quiet zones: 所有灰盒已确认对象静区、交叉口和长距离路径中心保留稳定低对比底色；每个48×48逻辑格的标记落点中央至少32×32范围不安排裂纹交点、小物件、尖锐影子、点状高光或强边缘
Style/medium: 中国修仙题材纯俯视平面2D pixel art map，大块硬边像素簇、平涂色面、稀疏材质变化和大尺度俯视轮廓；源Tile为16×16像素、逻辑显示48×48，视觉语言必须可拆为Tile、边缘、转角、阻挡、地标和前景
Specific map: [替换为第4或第5节对应地图的主题追加段]
Layer constraints: 只表达地形底层、道路、危险地形、阻挡、高差大轮廓与关键地标；不烘焙队伍、玩家、资源、敌群、Boss、宝箱、NPC、副本入口、出生点、跨图出口、地图Pin、文字、数字、路径、网格、迷雾、选中边或HUD
Output constraints: 保持输入灰盒结构，不裁切、不改路、不新增建筑或支路；输出只作为视觉母版，不声称已经具备无缝Tile能力。不要#00FF00、绿色键色、棋盘格、边框、水印或Logo
Avoid: 2.5D, 3D, three-quarter view, oblique top-down, 45-degree isometric, perspective, horizon, vanishing point, foreshortening, visible platform thickness, visible cliff sides, stair risers, pit walls, building facades, extruded terrain, miniature diorama, ambient occlusion, cast shadows, volumetric lighting, rim light, highly detailed pixel landscape, busy texture, high-frequency noise, scattered pebbles, dense grass, cracks everywhere, glowing dots, sparkles, debris, oversaturated fantasy map, icons baked into the map
```

### 3.4 当前失败地图的视觉母版编辑提示词

只有在图片1、2灰盒同时存在时，才允许把当前失败图作为图片6上传。使用本段替换 `3.3` 中的
`Primary request`，其余共用约束和对应地图 `Specific map` 保持不变。

```text
Use case: edit-visual-mother
Primary request: 以图片1、2灰盒为唯一结构事实源编辑图片6。保留灰盒已确认的画布比例、可玩轮廓、主支路、地形消耗、阻挡、高差语义边界、地标占位、进出口和对象静区；图片6只提供需要被简化的色盘与失败视觉参考。删除它的2.5D/3D投影、平台厚度、崖壁侧面、立体光影、随机微纹理和未经灰盒确认的点位，不从图片6恢复、移动或推断对象
Required edit: 把大部分可走区改为低饱和、低对比、大面积连续平面；将剩余质感合并成少量大而稀疏的色面与连续材质带，只保留一、二级地标的纯俯视轮廓和局部功能色
Output constraints: 输出与灰盒保持相同结构，不裁切、不改路、不新增地标、建筑、植被区或装饰区；未冻结对象不新增、不移动、不推断。禁止#00FF00、绿色键色、棋盘格、边框、水印或Logo
```

### 3.5 从灰盒确认 Tile Set

Tile Set 不能从 GPT 母图目测裁切。先给灰盒每个格记录 `terrain_id`、地形消耗、阻挡/可走语义和
上下左右连接，再建立“灰盒使用拓扑 → 正式资产”的覆盖表。

道路四方向至少检查单点、水平/垂直直线、`4` 个端点、`4` 个转角、`4` 个 T 形和十字，共 `16` 种。
若最终 Tiled 自动地形采用八邻域 Blob/Wang Tile，只能在 Tiled 规则冻结后升级，不让 GPT 猜 `47` Tile。

Tile Set 需求表必须分开记录：

- 基础地面与低差异变体；
- 普通道路、危险地形及其连接拓扑；
- 上下左右边、外转角、内转角与斜向边；
- 大型阻挡的占格和碰撞底座；
- 可淡出前景及渐隐区域；
- 独立地标、交互锚点和状态变体；
- 低频装饰贴花；
- 运行时覆盖标记，后者不属于地形 Tile。

进入正式清稿前必须满足：

1. 灰盒每个格和每个对象状态都有明确资产去向。
2. 所有实际使用的道路与边缘拓扑都有对应 Tile。
3. 不依赖 GPT 整图像素补洞。
4. `10×10` 连续铺设无裂缝、明显重复焦点或棋盘噪点。
5. 普通/危险地形在灰度下可分，但不会抢过覆盖标记。
6. 地标、状态对象和可淡出前景均与基础地形分层。

### 3.6 GPT Image 分组素材母版共用模板

地图视觉母版通过后，按“基础地面 → 道路/边缘 → 阻挡/前景 → 单独地标 → 低频贴花”分批生成。
一次只生成一个资产家族；大型地标和状态对象逐个生成。这里的“三张输入图”是每一次 GPT 调用的三个固定
槽位，不是 Figma 中的地形、连接、对象三张需求总表：

| 输入槽位 | 每次上传什么 | 是否随批次变化 | 来源 |
|---|---|---|---|
| 图片1 | 当前地图已确认的视觉母版，如 `map01_visual_mother.png` | 固定 | 第 3.3 阶段 GPT 输出，经用户确认后使用 |
| 图片2 | 当前批次专用需求图 | 变化 | Figma 从 Tile Set 总表按 A–F 批次单独导出 |
| 图片3 | `screenshot_project_pixel_style_reference.png` | 固定 | 已通过的项目像素素材人工合成，不使用未确认 GPT 图充当标准 |

不得把 `tileset_terrain / tileset_connections / tileset_objects` 三张分类总表依次当作图片1、2、3。
分类总表用于人工盘点和生成批次专用图；真正上传到图片2的文件必须只包含当前批次需要的信息。

```text
Use case: game-asset-concept-sheet
Project: 原创中国修仙题材纯俯视2D像素游戏《昆吾禁地》
Map: [替换地图ID与名称]
Input images: 图片1固定为已确认的对应地图视觉母版，只用于统一地表、功能色和像素语言；图片2必须替换为当前批次专用需求图，只包含本轮资产的数量、连接方向、占格、状态和透明规则；图片3固定为项目已确认的像素密度、硬边轮廓和受限色盘参考。不要把地形、连接、对象三张分类总表分别误当成图片1、2、3
Projection: 严格90度垂直俯视、正交投影、完全平面的2D像素画；所有地形和物件只显示顶部轮廓，不显示正立面、侧面、厚度、透视、投影或环境遮蔽
Pixel treatment: 目标源Tile为16×16像素。GPT输出只是人工清稿母版，不声称已满足真实16×16、无缝拼接和精确网格；使用大块硬边像素簇、受限色盘、无抗锯齿、无柔边和无线性模糊
Visual density: 低饱和、低对比、低频材质；普通地面必须比队伍、敌群、资源和入口标记安静，不让每个素材拥有独立高光、裂纹中心或大量小装饰
Layout: 同类元素按规则行列分开摆放，每个元素之间至少保留一个元素宽度的空白，不重叠、不连接、不被裁切；不要文字标签、编号、水印、边框或可见网格
Transparency: 地面Tile使用实际不透明地表色并铺满方块边缘；阻挡、地标、贴花和前景使用真实RGBA透明背景；禁止绿色键色、#00FF00、白底、黑底、棋盘格和抠图残边
Specific request: [替换为当前批次要求]
Avoid: 2.5D, 3D, isometric, oblique view, perspective, visible sides, terrain thickness, cast shadow, ambient occlusion, bevel, realistic texture, painterly brush, antialiasing, soft edge, dense noise, scattered pebbles, grass everywhere, cracks everywhere, glowing particles, text, numbers, UI, icon frame, chroma key green, checkerboard background
```

六批调用时只替换图片2：

| 批次 | 图片2文件 | 内容 |
|---|---|---|
| A 基础地面 | `map01_batch_a_base_terrain.png` | 地面家族、数量、不透明规则和重复测试要求 |
| B 道路/边缘/转角 | `map01_batch_b_connections.png` | 道路 `16` 拓扑、四向边缘、内外转角 |
| C 山体阻挡/前景 | `map01_batch_c_blockers_foreground.png` | 两组斜山脊、阻挡占格、碰撞底座、淡出前景 |
| D 阵灯状态 | `map01_batch_d_lamp_states.png` | 三状态、同尺寸/锚点/碰撞和状态差异 |
| E 单独地标 | `map01_batch_e_landmark_[name].png` | 每次只含一个地标的占格、锚点、层级和状态 |
| F 低频贴花 | `map01_batch_f_decals.png` | 允许的贴花类别、数量、透明和低对比限制 |

### 3.7 人工清稿与 Tiled/Cocos 拼装

本流程不使用 Python 脚本处理图片。可使用 Aseprite、Photoshop 或同类像素编辑工具进行人工清稿：

1. 统一像素颗粒、受限色盘和明暗方向。
2. 清除抗锯齿、柔边、半透明脏像素和错误背景。
3. 重建真正无缝的 `16×16` 地面、连接、边缘与转角。
4. 将阻挡视觉、碰撞底座、前景淡出区、地标锚点和状态分别导出。
5. 在 Tiled/Cocos 按 Figma 灰盒重新拼图，不能导入 GPT 整张视觉母版覆盖结构。
6. 后置叠加对象、队伍、资源、敌群、Boss、入口、迷雾、路径和选中状态。
7. 新资源交给 Cocos Creator 导入并生成 `.meta`，禁止手写或脚本伪造 `.meta`。

### 3.8 地图共用验收 Gate

- 第一眼能确认摄像机接近 `90°` 垂直向下，无 2.5D、透视、平台厚度和侧面。
- 可走区是平整 2D 色块；道路、危险地形、阻挡和进出口无需文字即可理解。
- 不叠加标记时仍能靠大轮廓和一、二级地标定向；叠加标记后标记优先被读取。
- 七种标记在最浅、最深和噪点最高区域，以 `100%`、`75%`、`50%` UI 缩放及灰度图检查均可识别。
- 普通区域保持大面积安静色面；移除标记后不应每个小区域都有独立细节中心。
- Tile 无裂缝、错位或线性过滤；前景淡出后路径、对象和预警仍可读。
- 不出现 `#00FF00`、绿色键色、棋盘格、烘焙点位、文字、路径箭头、HUD、水印或 Logo。
- 用户人工确认风格、俯视感、标记可读性和 Tile 拼接后，地图才能进入 Approved。

## 4. `map_01` 首图试制

`map_01` 是四图工作流的唯一首轮样板。其试制通过前，不一次生成另外三张完整地图，也不提前生产四图共用的
大而全 Tile Set。

### 4.1 规范与事实边界

[地图01详细设计](../1.0策划案/地图/28_地图01_破禁山麓与万修之门_详细设计_草案.md) 已冻结
数据边界、区域关系、路线职责和内容数量，但新版固定对象坐标尚未重新冻结。
[旧固定对象表](../1.0策划案/地图/22_1.0四图固定对象与经济清单_草案.md) 中的坐标只作为灰盒
`v0.1` 候选锚点和密度检查输入，不得直接进入正式 Tiled、任务或碰撞数据。

| 项目 | `map_01` 规范 |
|---|---|
| 地图 | 破禁山麓·万修之门 |
| 数据边界 | `48×64` 格，纵向 `3:4`；矩形边界不等于可玩轮廓 |
| Figma 灰盒 | `768×1024`，每格 `16×16` |
| 完整逻辑地图 | `2304×3072`，不表示导出同尺寸整图 |
| 移动 | 只允许四方向移动 |
| 地形消耗 | 普通地形 `1/步`；碎石危险地形 `2/步` |
| 空间骨架 | 南侧宽入口 → 废营 → 残碑 → 中段双路绕脊 → 北坡汇流 → 门前展开 |
| 内容量 | 普通敌群 `10`、精英 `2`、Boss `1`、生产资源 `8`、局部副本 `1`、永久捷径 `1` |
| 资源 | 灵木 `3`、玄铁 `2`、灵粮 `2`、低阶灵晶 `1` |
| 一级地标 | 万修之门 |
| 二级地标 | 灰白斜山脊、三盏阵灯 |
| 三级地标 | 废营旗、暗道洞口/炉火、东壁阶梯、尸旗 |
| 地表语言 | 低饱和灰青山岩、枯黄旧路、深灰阻挡、克制暖白古阵和少量叶家暗红 |

区域关系必须保持：

- A 入山坡：南侧安全落点和基础操作教学。
- B 废营灯坪：第一阵灯、叶家尸首、拖痕和早期灵粮。
- C 万修门外坡：残碑、粮册小环、灰白斜山脊与双路预览。
- D 山腹暗道：西侧短路、顾南炉救援和局部副本。
- E 东壁阵灯台：第二阵灯、裂甲石卫、资源和永久回程阶梯。
- F 北坡残阵带：D/E 汇流、第三阵灯、尸煞执旗者和最后休整。
- G 万修之门：Boss 整备石坪、守门石灵和 `map_02` 出口。

灰盒 `v0.1` 可先使用：出生点 `(24,61)`、第一灯 `(21,52)`、残碑 `(24,42)`、第二灯
`(38,39)`、暗道入口 `(8,28)`、东壁精英/捷径 `(39,31)`、第三灯 `(15,20)`、尸煞执旗者
`(16,15)`、守门石灵 `(24,8)`、出口 `(24,3)`。旧版约 `58` 步主路径、约 `44` 步最短 Boss
路径和“连续 `12` 步内出现内容”只用于密度检查，不写入最终验收。

### 4.2 Figma MCP 灰盒提示词

使用时提供目标 Figma 文件或空白页面的精确链接；如果目标页面已经存在，必须先确认只编辑指定页面，不覆盖
其他地图、HUD 或组件页面。

```text
请在我提供的Figma文件中创建新页面“MAP_01_Greybox”，不要修改其他页面。

目标：为《昆吾禁地》map_01“破禁山麓·万修之门”制作严格规则网格的纯俯视棋盘灰盒。它是GPT Image的结构参考，不是正式地图美术，也不是已冻结的Tiled碰撞和任务坐标。

基础尺寸：
创建主Frame“01_MAP01_MASTER_ANNOTATED”，尺寸768×1024px，背景#14171B。
地图为48列×64行，每格严格16×16px；坐标原点在左上角，x=0–47、y=0–63。
所有形状吸附16px网格，不允许小数坐标、任意旋转或透视。

图层组：
00_DATA_BOUNDARY
01_WALKABLE_MASK
02_ROAD_AND_TERRAIN_COST
03_BLOCKED_MASK
04_ROUTE_STRUCTURE
05_LANDMARK_FOOTPRINT
06_OBJECT_ANCHOR
07_MARKER_QUIET_ZONE
08_OCCLUDER
09_GRID_AND_COORDINATES
10_LEGEND

灰盒语义色：
普通可走地面#858B91
普通道路、1灵粮/步#B3AA90
碎石危险地形、2灵粮/步#746F68
不可通行山体#343B43
灰白斜山脊#555E67
可淡出前景#20272F、60%透明
主要地标#C5A35B
普通敌群#A85B5B
精英#C66B53
Boss#8C445B
资源#5E7F9C
剧情/事件#786B91
出生点和出口#D9D1B8
标记静区无填充、#EEE8DA描边、55%透明
不要使用绿色、绿色键色或#00FF00。

可玩轮廓：
1. 纵向山谷，矩形四角用不可通行山体挖空。
2. 南侧A区为宽入口；B区包含废营、第一阵灯和早期教学。
3. C区可看见残碑、灰白斜山脊和北侧万修之门方向。
4. 中段斜山脊形成西侧D暗道短路和东侧E开阔绕行。
5. D、E必须在F区汇合，不形成长距离死胡同。
6. F区后收窄并经过第三阵灯和尸煞执旗者。
7. 最北G区展开为门前半圆整备石坪，连接Boss和map_02出口。
8. 不制作多层桥梁、视觉传送、等距地形、立体平台或崖壁侧面。

灰盒v0.1候选锚点：
出生点(24,61)
第一阵灯(21,52)
残碑(24,42)
第二阵灯(38,39)
山腹暗道入口(8,28)
东壁精英与捷径(39,31)
第三阵灯(15,20)
尸煞执旗者(16,15)
守门石灵(24,8)
map_02出口(24,3)

普通敌群候选锚点：
(22,56)、(15,51)、(31,49)、(10,42)、(37,41)、(18,34)、(34,30)、(12,21)、(31,18)、(23,12)

资源候选锚点：
灵木(9,55)、(35,53)、(6,35)
玄铁(40,46)、(13,25)
灵粮(18,47)、(38,23)
灵晶(5,18)

其他事件候选锚点：
废营尸首在(31,49)附近
废营粮册(17,36)
一次性宝箱(29,27)
山腹暗道入口(8,28)
东壁永久阶梯(39,31)

候选坐标来自旧版迁移参考。如发生不可达、重叠或与A–G区域职责冲突，不要静默移动，在旁边增加红色冲突注释。

路线要求：
出生点到第一灯为低风险教学；第一灯、尸首和残碑推进方向清楚；C区同时可读西暗道与东壁；两路都能到F；东壁精英后阶梯返回A/B；西北灵晶是短险路；两个精英可绕开；最高危险普通敌群到Boss之间保留安全整备区；每个对象锚点中心保留一个格子的低噪点静区。

复制导出：
02_MAP01_LAYOUT_COLLISION：保留网格、可走区、地形消耗、阻挡、地标占位和对象静区，只保留简短图例。
03_MAP01_GPT_CLEAN_GUIDE：768×1024；隐藏网格、坐标、文字、图例和对象图标；保留可玩轮廓、道路、危险区、阻挡、斜山脊、地标占位和静区。
04_MAP01_MARKER_TEST：从B、C、E、F、G截取典型区；每格按48×48逻辑像素显示，放入实际24×24或32×32覆盖标记，检查32×32静区。

导出PNG：
map01_master_annotated.png
map01_layout_collision.png
map01_gpt_clean_guide.png
map01_marker_test.png
```

### 4.3 GPT Image `map_01` 主题追加段

将 `3.3` 共用提示词中的 `[Specific map]` 替换为下段；不得单独使用本段纯文生图。

```text
Specific map: map_01“破禁山麓·万修之门”。主体使用低饱和灰青山岩平面、少量枯黄旧路大色带和深灰不可走山体轮廓。南侧是宽入口和废营，中段灰白斜山脊形成西侧山腹暗道与东侧阵灯台两路，两路在北坡重新汇合，最北为万修之门与守门石灵石坪。普通道路为1灵粮地形；碎石危险区为2灵粮地形，使用略深、略破碎但仍低频的大色块区分，不铺满小石子。万修之门、灰白斜山脊和三盏阵灯是核心导航结构；万修之门只显示纯俯视门址和顶部剪影，不画正面拱门。阵灯暖白与暗红反纹只聚集在灯体附近。废营、暗道和东壁阶梯靠平面顶部轮廓识别，周边走路区保持安静。不要山坡侧面、崖壁侧面、阶梯立面、洞穴纵深、满地白阵纹、枯草、断木和碎骨
```

用户确认后将视觉母版保存为 `map01_visual_mother.png`；它是 `3.6` A–F 六批调用中固定的图片1，
不能用灰盒 clean guide、当前失败图或任一分组素材母版代替。

### 4.4 `map_01` 第一批 Tile Set

| 资产组 | 第一批建议 | 说明 |
|---|---:|---|
| 普通灰青地面 | `4` 个低差异变体 | 无中心主体，可无缝重复 |
| 枯黄普通道路 | `3` 个低差异变体 | 路宽和明度统一 |
| 碎石危险地形 | `3` 个变体 | 灰度可分，但不依赖满地小石子 |
| 道路连接 | 四方向 `16` 种 | 单点、端点、直线、转角、T形、十字 |
| 山体边缘 | 上下左右边、`4` 外角、`4` 内角 | 与灰盒实际使用逐项对照 |
| 斜山脊 | 两个斜向家族 | 西北—东南、东北—西南阶梯式斜边 |
| 大型阻挡物 | `6–8` 个轮廓 | 以 `1×1`、`2×2`、`2×3` 等占格试制 |
| 低频贴花 | `4–6` 个 | 不参与路线和对象识别 |
| 可淡出前景 | 山脊压顶、暗道顶石、万修门顶部 | 与碰撞底座分层 |
| 独立地标 | 阵灯、残碑、废营、暗道入口、阶梯、万修门 | 不烘焙进地面 |
| 状态变体 | 阵灯、阶梯、门等 | 同尺寸、同锚点，只替换状态视觉 |

### 4.5 Figma MCP 制作 `map_01` Tile Set 需求图提示词

使用时同时提供 `01_MAP01_MASTER_ANNOTATED` 与 `02_MAP01_LAYOUT_COLLISION` 的精确 Figma 节点链接。
Figma 必须先读取两者的结构和截图，再创建需求页面；不能只看导出 PNG 猜连接。需求图只用于整理结构和作为
`3.6` 的图片2，不绘制正式像素美术。

```text
请读取我提供的map_01灰盒精确节点链接，基于灰盒中实际出现的地形、道路、阻挡、地标和状态，制作“map_01 Tile Set需求图”。不要修改来源灰盒、HUD、通用组件或其他地图页面。

目标边界：
这不是正式地图美术，也不是让你绘制像素Tile。只使用规则网格、纯色色块、方向标记、占格框和文字说明，供人工盘点以及后续GPT Image A–F批次的图片2使用。无法从灰盒或上游确认的连接、占格和状态统一标记UNVERIFIED，不自行补全。

创建页面：
MAP_01_TileSet_Requirements

页面内创建Frame：
00_REQUIREMENT_OVERVIEW
01_BASE_TERRAIN
02_ROAD_CONNECTIONS
03_EDGES_AND_CORNERS
04_DIAGONAL_RIDGE
05_BLOCKERS_AND_OCCLUDERS
06_LANDMARKS_AND_STATES
07_GRAYBOX_COVERAGE
08_GPT_BATCH_EXPORTS

Frame宽度统一1440px，使用纵向Auto Layout；需求卡之间保持32px。不要使用游戏UI装饰、阴影、渐变、厚重边框或正式像素纹理。

创建组件COMP_TILE_REQUIREMENT_CARD，包含字段：
requirement_id
asset_family
topology_id
footprint
walkable
terrain_cost
source_tile_size
used_in_map01
estimated_count
source_coordinate_examples
state
notes

字段规则：
source_tile_size固定16×16px。
footprint使用1×1、1×2、2×2、2×3等逻辑格。
walkable只用YES/NO。
terrain_cost只用0、1、2或N/A。
used_in_map01只用USED、RESERVED或UNVERIFIED。
state只用DRAFT、NEEDS_ART、NEEDS_CLEANUP或APPROVED。
预览区使用64×64px，即源Tile的4倍结构预览，只画纯色关系，不画正式纹理。

灰盒语义色：
普通灰青地面#858B91
普通道路、1灵粮/步#B3AA90
碎石危险地形、2灵粮/步#746F68
不可通行山体#343B43
灰白斜山脊#555E67
可淡出前景#20272F、60%透明
主要地标#C5A35B
对象静区描边#EEE8DA、55%透明
不要使用绿色、绿色键色或#00FF00。

01_BASE_TERRAIN：
整理TERRAIN_M1_ROCK_FLAT，4个低差异变体，1×1，可走，消耗1，无中心主体。
整理TERRAIN_M1_OLD_ROAD，3个低差异变体，1×1，可走，消耗1，中心安静。
整理TERRAIN_M1_RUBBLE_DANGER，3个变体，1×1，可走，消耗2，灰度下与普通道路可分但不依赖高频碎石。
每个家族增加4×4重复铺设结构测试区。

02_ROAD_CONNECTIONS：
使用四方向编码N=1、E=2、S=4、W=8，建立完整16种连接卡：孤立、四端点、水平/垂直直线、四转角、四T形和十字。每张卡显示N/E/S/W关系、map_01是否使用、使用次数和代表坐标。暂未使用的标记RESERVED，无法确认的标记UNVERIFIED，不删除卡片。

03_EDGES_AND_CORNERS：
建立EDGE_TOP/RIGHT/BOTTOM/LEFT、四个OUTER_CORNER和四个INNER_CORNER。每张卡明确哪侧可走、哪侧阻挡、属于道路还是山体、是否允许旋转复用及map_01使用次数。若旋转会改变受光或像素方向，标记REQUIRES_UNIQUE_DIRECTION。

04_DIAGONAL_RIDGE：
整理RIDGE_NW_TO_SE和RIDGE_NE_TO_SW两组阶梯式斜边。每组展示连续斜边、起点、终点、连接水平边、连接垂直边、外转折和内转折。只使用16px阶梯格，不用平滑矢量斜线；标明可走侧、阻挡侧和可淡出前景区。

05_BLOCKERS_AND_OCCLUDERS：
整理1×1、1×2、2×2、2×3阻挡、不规则大型山体、暗道顶部、山脊压顶和万修门顶部。分别显示视觉占格、碰撞占格、对象锚点、fade区域、fade后保留底座、sorting层级以及是否允许覆盖角色。地图标记、任务目标和战斗预警始终在可淡出前景上方。

06_LANDMARKS_AND_STATES：
阵灯：LAMP_BROKEN、LAMP_REVERSED、LAMP_REPAIRED；三状态尺寸、锚点和碰撞相同。
残碑：STELE_DEFAULT、STELE_INTERACTED、STELE_C07_RESERVED；C07只标预留面。
东壁阶梯：STAIRS_CLOSED、STAIRS_OPEN。
万修之门：GATE_LOCKED、GATE_BOSS_READY、GATE_OPEN。
山腹暗道：TUNNEL_DEFAULT、TUNNEL_DISCOVERED、TUNNEL_CLEARED。
废营：CAMP_DEFAULT、CAMP_CORPSES_PROCESSED、CAMP_LEDGER_READ。
每项显示候选footprint、交互锚点、碰撞/前景是否变化、是否需要透明背景以及属于Tile、独立Sprite还是前景。无上游依据的状态标记UNVERIFIED。

07_GRAYBOX_COVERAGE：
建立覆盖矩阵。行包含基础地面、普通道路、危险地形、16道路拓扑、四向边缘、内外转角、两组斜山脊、阻挡占格、前景和地标状态；列包含requirement_id、map_01_used、use_count、example_coordinate、source_region、asset_required、verification_status和notes。source_region只用A–G。

08_GPT_BATCH_EXPORTS：
分类总表可以保留用于人工盘点，但GPT实际上传的图片2必须按批次单独导出：

map01_batch_a_base_terrain.png
只包含三种地面家族、数量、不透明规则、低噪点要求和4×4重复测试要求。

map01_batch_b_connections.png
只包含道路16拓扑、四向边缘、四外角、四内角、统一路宽和方向规则。

map01_batch_c_blockers_foreground.png
合并两组斜山脊、阻挡占格、碰撞底座、暗道/山脊/万修门前景与fade规则。

map01_batch_d_lamp_states.png
只包含阵灯三状态、相同尺寸/锚点/碰撞和允许变化的视觉字段。

map01_batch_e_landmark_gate.png
map01_batch_e_landmark_stele.png
map01_batch_e_landmark_camp.png
map01_batch_e_landmark_tunnel.png
map01_batch_e_landmark_stairs.png
每张只包含一个地标的占格、锚点、碰撞、前景层、状态和透明裁切范围。

map01_batch_f_decals.png
只包含4–6个允许的低频贴花类别、低对比/透明要求和禁止内容，不画正式贴花。

另外导出map01_tileset_requirement_overview.png供人工审阅和防漏，不作为3.6三张输入中的固定图片。

所有批次导出要求：
保留清晰的requirement_id、方向、占格、状态和数量。
不包含完整地图截图、角色、怪物、资源、Boss或地图Overlay标记。
不使用正式地表纹理、装饰背景、阴影、渐变、绿色键色、#00FF00、水印或Logo。
完成后停止，不生成正式像素Tile。
```

### 4.6 `map_01` 分批素材母版提示词

每次复制 `3.6` 共用模板并替换 `Specific request`，不能把以下批次混成一张大图。

**批次 A：基础地面**

```text
Specific request: 生成map_01基础地面像素素材母版。包含4个低饱和灰青平整山岩地面变体、3个枯黄旧石路地面变体和3个略深的碎石危险地形变体。所有方块边到边填满，不使用透明背景。变体之间只允许大尺度、低对比色面变化，不出现中心石块、中心裂纹、独立草丛、白色阵纹或明显视觉焦点。每个设计都适合人工重绘为可无缝平铺的16×16 Tile
```

**批次 B：道路、边缘与转角**

```text
Specific request: 生成map_01枯黄旧石路与灰青地面的连接结构母版。道路宽度和边缘像素密度统一，分别展示单点、四个端点、水平直线、垂直直线、四个转角、四个T形和十字，共16种四方向拓扑；另展示上下左右边缘、四个外转角和四个内转角。严格纯俯视、完全平面，不显示路肩厚度、台阶立面、投影或黑色粗描边，路面中心保持低噪点
```

**批次 C：山体阻挡与前景**

```text
Specific request: 生成map_01灰青山体阻挡和可淡出前景素材母版。包含6–8个不同占格的大型俯视阻挡轮廓，以及西北—东南、东北—西南两种斜山脊边缘。阻挡依靠深浅色块和顶部轮廓识别，不画崖壁侧面、山坡立面、厚度或投影。大型轮廓内部保持简洁，不堆积小石子。可淡出前景与碰撞底座分开摆放，使用真实透明背景
```

**批次 D：阵灯三状态**

```text
Specific request: 生成map_01通用阵灯的三个状态母版：破损熄灭、暗红反向阵纹运行、修复后的稳定暖白阵光。三个状态使用相同俯视轮廓、尺寸和锚点，只改变局部裂纹、阵纹方向和功能色。阵光限制在灯体附近，不生成大范围光环、粒子、地面法阵或UI底盘，使用真实透明背景
```

**批次 E：单独地标**

```text
Specific request: 生成map_01的[万修之门顶部轮廓／万修门残碑／废营／山腹暗道入口／东壁永久阶梯]纯俯视2D像素地标母版。保持map_01灰青、枯黄、旧金、暗红和暖白受限色盘。只显示正上方顶部轮廓，不显示正立面、侧面、洞穴纵深、台阶立面或投影。地标主体集中、边界清楚，周围不附带大片地面，不附带角色、怪物、资源、文字或地图图标，主体完整并留出透明裁切空间。每次只替换方括号中的一个地标
```

**批次 F：低频贴花**

```text
Specific request: 生成map_01低频地面贴花母版，只包含4–6个克制的俯视元素，例如连续浅色磨损带、低对比枯草边、小块碎石带或极浅旧阵痕。每个贴花使用真实透明背景，轮廓松散、对比很低，不出现独立发光点、白色中心、完整法阵、尸骨、宝物或地图标记。贴花只用于少量打破重复，不能成为路线和对象识别依据
```

### 4.7 最小试制批次

第一轮只完成：Figma 四张灰盒导出 → GPT 地图视觉母版 → 基础地面 → 道路 `16` 连接 →
山体边缘/阻挡 → 一盏阵灯三状态。先人工清稿并拼出一个 `375×817` 典型视窗，不一次生成整套地标。

只有试制视窗通过 `3.8`，并由用户确认风格、俯视感、标记可读性和 Tile 拼接后，才继续万修之门、废营、
暗道、阶梯与整张 `map_01`，随后才迁移到其余地图。

## 5. `map_02–04` 主题输入与后续顺序

本节只保存三张地图的视觉主题，不是生成入口。每张地图必须先根据其详细设计复制 `4.2` 的结构方法建立独立
Figma 灰盒，再使用 `3.3` 共用提示词和本节对应 `Specific map`；视觉母版通过后，复制 `4.5` 的结构方法
制作本地图 Tile Set 需求总表和批次专用图片2，最后使用 `3.6` 分批生成。不得复用 `map_01` 路线、候选坐标或
批次需求内容。

### 5.1 `map_02` 白玉广场

```text
Specific map: 主体使用低饱和暖灰白玉平面、冷灰玉道大色带与灰绿灵木侵蚀区块。广场、玉道、庭院内地和外环全部是纯俯视平面2D处理，不画台地厚度、庭院墙正立面、门楼正面、立柱透视或投影。白玉地面大部分平整安静，不在每块玉砖上画缝、反光、污渍、苔藓和碎玉，不显示密集玉砖网格。中央八角阵盘、封尸坛址和四庭院入口使用俯视平面轮廓形成导航层级；四阵眼差异只集中在入口轮廓、阵眼和少量连续区域色带，不让石压、雷痕、蜃雾和机关纹样铺满走道。木魅树根作为大块平面阻挡，不生成满地小藤蔓和叶片
```

### 5.2 `map_03` 灵宝遗址

```text
Specific map: 主体使用低饱和灰褐平面区块、暗青金属连接带、冷灰山脊阻挡与大块断裂建筑顶部剪影。三个岛状建筑区只是平面多边形可玩轮廓，不是悬浮平台；不画平台侧壁、悬空底部、断崖厚度、斜山坡或链桥纵深。普通遗址地面不满地散布齿轮、铜屑、符纸、碎宝、小灯、烟尘、裂纹和发光法器。铸灵堂吊炉只使用正上方圆形炉口剪影；炉橙、悬匣微光和升降台炉纹只聚集在对应构造物上。十三盏小灯是有意义的固定对象，不得用普通点光造成误认。道路与区块中心保持宽阔、低对比和平面化
```

### 5.3 `map_04` 镇魔塔外层

```text
Specific map: 主体使用低饱和炭黑、深灰紫和冷灰石面组成破碎平面外环。中央深坑只是一块平面深色封闭轮廓，倒置镇魔塔外壁只保留纯俯视边界符号；不画深坑内壁、塔壁正立面、环带厚度、向下透视或深度渐变。黑石路面不铺满紫雾颗粒、魔气点、碎骨、裂缝红光、尸甲片、灰烬或细小锁链。三条封链长臂是平铺带状图形，定界冷白、镇尸金褐、锁魂青银只限制在封链和枢纽内部；不画锁链垂落、近大远小或体积阴影。结丹祭坛、新月断台和残羽只用平面符号与顶部轮廓。魔雾使用大片低对比平面遮罩带，不使用满屏点状粒子或遮住走路中心的高密雾团
```

### 5.4 迁移顺序

按 `map_01 → map_02 → map_03 → map_04` 顺序逐图完成灰盒、母版、Tile Set、人工清稿和视窗验收。
后一张地图可以复用已经通过的像素密度、Alpha、文件命名和通用 Overlay 规范，但不能复用上一张地图的
路线、地形连接表、对象坐标或未核对地标占格。

## 6. 探索 HUD、操作图标与地图覆盖标记

### 6.1 本批制作范围

本批先制作地图页缺失的 `4` 个探索操作图标、`3` 个扎营面板操作图标和 `7` 个
地图覆盖标记，共 `14` 张独立 PNG。
用户口语中的“回城”在正式页面统一称为“归营”；“野怪”统一按普通敌群/敌对遭遇理解。

| ID | 资产 | 逻辑尺寸 | 交付尺寸 | 文件名 | 计划路径 |
|---|---|---:|---:|---|---|
| `EXPLORE-ACTION-001` | 扎营操作图标 | `24×24` | `72×72 @3x` | `icon_explore_camp.png` | `assets/bundles/shared/ui/exploration/` |
| `EXPLORE-ACTION-002` | 归营操作图标 | `24×24` | `72×72 @3x` | `icon_explore_return_to_camp.png` | 同上 |
| `EXPLORE-ACTION-003` | 队伍操作图标 | `24×24` | `72×72 @3x` | `icon_explore_party.png` | 同上 |
| `EXPLORE-ACTION-004` | 背包操作图标 | `24×24` | `72×72 @3x` | `icon_explore_inventory.png` | 同上 |
| `EXPLORE-CAMP-ACTION-001` | 弄点吃的/进食补给图标 | `24×24` | `72×72 @3x` | `icon_explore_camp_food.png` | 同上 |
| `EXPLORE-CAMP-ACTION-002` | 恢复体力/运功疗伤图标 | `24×24` | `72×72 @3x` | `icon_explore_camp_recover.png` | 同上 |
| `EXPLORE-CAMP-ACTION-003` | 继续探险图标 | `24×24` | `72×72 @3x` | `icon_explore_camp_continue.png` | 同上 |
| `EXPLORE-MARKER-001` | 我方队伍地图标记 | `32×32` | `96×96 @3x` | `marker_explore_party.png` | `assets/bundles/shared/world/markers/` |
| `EXPLORE-MARKER-002` | 通用资源点标记 | `24×24` | `72×72 @3x` | `marker_explore_resource.png` | 同上 |
| `EXPLORE-MARKER-003` | 普通敌群标记 | `24×24` | `72×72 @3x` | `marker_explore_enemy.png` | 同上 |
| `EXPLORE-MARKER-004` | 副本入口标记 | `24×24` | `72×72 @3x` | `marker_explore_dungeon.png` | 同上 |
| `EXPLORE-MARKER-005` | 出生/进入落点标记 | `24×24` | `72×72 @3x` | `marker_explore_spawn.png` | 同上 |
| `EXPLORE-MARKER-006` | 前往其他地图的出口标记 | `24×24` | `72×72 @3x` | `marker_explore_map_exit.png` | 同上 |
| `EXPLORE-MARKER-007` | Boss 地图标记 | `32×32` | `96×96 @3x` | `marker_explore_boss.png` | 同上 |

地图覆盖标记是对齐到世界对象坐标上方的 UI Overlay，不属于 `16×16` 地形 Tile，也不替代
最终敌人内容表要求的普通/精英/Boss 世界棋子。队伍与 Boss 标记在一个 `48×48` 逻辑格内居中显示；
其余对象标记以对象坐标底部中央为锚点，主体不得越过相邻格的主要交互区。

### 6.2 组件与状态边界

- 七个操作图标只提供图形主体，不包含按钮底板、按下态、禁用态、文字或次数；按钮状态读取
  Approved 通用组件，操作名称与剩余休整等信息由 Label 渲染。
- 归营图标表示“执行归营动作”，不能直接画成归营符物品。地图入口安全归营不消耗归营符，
  归营符的物品图标继续由 13 分册维护。
- 队伍操作图标是打开队伍摘要的 UI 图标；地图队伍标记表示当前坐标，二者不能使用同一张图。
- “弄点吃的”是扎营内的进食补给入口：选择允许的野外食物并补充本次出征灵粮，食物不恢复生命。
  图标不得画成辟谷饼、干粮或某一件具体物品的物品图标。
- 用户口语“恢复体力”在正式规则中对应“运功疗伤”：每次扎营只对存活修士生效，不消耗食物、药品或资源，
  不能复活阵亡修士。可用/已使用状态由运行时按钮表达，不复制两张图。
- “继续探险”只关闭扎营面板并返回当前地图，不触发归营、跨图、传送或新一次出征。
- 出生点表示本次进入地图的安全落点，不是复活点、存档点、传送收费点或永久回城石。
- 副本入口表示进入当前地图内的局部副本或已发现资源副本；跨图出口表示通往另一张完整地图。
  两者必须在灰度和缩小状态下仍能依靠轮廓区分。
- 通用资源点标记只表示“这里有可采集资源”，不烘焙灵木、玄铁、灵晶等具体产物，也不使用
  装备品质色。具体内容在对象面板中显示。
- 普通敌群标记只表示敌对遭遇，不承担普通/精英/Boss 层级；精英与 Boss 继续使用内容表中的
  世界棋子和独立机制表现。
- Boss 标记只承担“已发现的关键强敌”类别识别，不画具体 Boss 面孔、种族或地图专属造型；
  具体形象、当前阶段与战斗机制仍由对应世界棋子、对象面板和内容表表达。
- `UNKNOWN` 状态不显示任何对象；已发现、可交互、已处理、条件锁定继续通过运行时显隐、Tint、
  外描边、通用锁图标和原因文字组合，不为本批复制状态 PNG。

### 6.3 生成前需要提供给 GPT Image 的截图

按素材类型上传以下项目截图；它们只锁定布局、大小、色盘和像素密度，不作为具体图形
的编辑目标：

| 编号 | 建议文件名 | 必须展示什么 | 用途 |
|---|---|---|---|
| 图片1 | `screenshot_exploration_page_375x817.png` | 375×817 探索页，包含顶部 HUD、世界视窗、底部操作区和安全区 | 锁定操作图标所在层级与触控区，不让 GPT 生成完整按钮或页面 |
| 图片2 | `screenshot_map01_marker_scale.png` | 按第 3、4 节完成并通过试制的 `map_01` 低噪点 Tile 地图裁切，至少显示 `5×5` 个 48×48 逻辑格、迷雾边缘和一个空对象占位框 | 检查中明度底图上的尺寸和轮廓 |
| 图片3 | `screenshot_marker_dark_contrast_board.png`；后续替换为 `screenshot_map04_marker_scale.png` | 前期使用 Figma 制作的深灰/深紫低噪点纯色测试板和同尺寸占位框；`map_04` 完成后替换为其 Tile 地图 `5×5` 裁切 | 先检查深色背景可读性；最终 Approved 前必须用真实 `map_04` 回归 |
| 图片4 | `screenshot_project_icon_style.png` | 已通过或待对齐的 `24×24` 项目图标并排合成，至少含开山镐、探灵镜、通用锁和一个资源图标 | 只继承硬边像素簇、左上受光、深色外轮廓和留白比例，不复制具体物体 |
| 图片5 | `screenshot_exploration_camp_panel_375x817.png` | 375×817 扎营面板成稿或灰盒，包含三个操作的实际按钮框、文字层与可用/已使用占位态 | 锁定扎营面板内的图标占比、操作层级和三图标的并排关系 |

生成地图覆盖标记时使用图片2、3、4。图片2必须来自按第 3 节正式流程重拼的 Tile 地图，不得用 GPT
视觉母版代替；图片3前期只能是明确标注的 Figma 深色对比测试板，不能伪装成 `map_04` 成稿，最终批准前必须替换为
真实 `map_04` Tile 截图。两张图都保留“单格边界和标记占位框”；生成探索操作图标时使用
图片1、4；生成扎营面板三个图标时使用图片4、5。不要上传其他游戏地图、现代地图 App 图标、绿色抠图底、棋盘格或带水印图片作为输入。

### 6.4 本批共用输出与 Alpha 规则

- 十四张图片必须逐张生成，一次只输出一个图标或标记，不生成组图、Sprite Sheet、页面示意图
  或带文字的资产表。
- 全部使用真实 RGBA Alpha：只有图形主体及必要的硬边像素可见，主体外完全透明。
- 绝对不使用 `#00FF00`、绿色键色或任何其他纯色抠图背景；也不使用白底、黑底、棋盘格、
  投影展示板或手机页面作为伪透明背景。
- 如果 GPT Image 没有返回真实 Alpha，该图片只可作为待人工清稿母图，不能导入游戏；不得追加
  绿色背景继续生成。
- 图标使用清晰硬边像素簇，无抗锯齿、无柔边、无模糊渐变和半透明污染。可读性依靠轮廓、
  明暗和离散色块，不依赖大范围柔光。
- 所有图形不含汉字、字母、数字、资源数量、任务红点、品质框、锁、按钮底板、边框、名称、
  水印或 Logo；动态文字和状态由运行时节点表达。
- 正式导入前分别合成到 `map_01` 与 `map_04` 截图，在 `100%`、`75%` 和 `50%` UI 缩放下人工检查。

### 6.5 四个探索操作图标提示词

每次复制下面的共用提示词，再在 `Specific subject` 后追加 6.5.1 至 6.5.4 中的一段。四张图片
分别生成，不能让模型自行拼成一组。

```text
Use case: stylized-concept
Asset type: 原创竖屏2D像素修仙游戏《昆吾禁地》的探索页面操作图标；单个独立透明PNG，不是完整按钮或页面
Input images: 图片1是375×817探索页布局参考，只锁定图标位置、信息层级和安全区；图片4是项目现有小图标的像素密度、深色外轮廓、左上受光与留白参考。两张输入图都不是编辑目标，不复制其中的文字、按钮、物体或具体轮廓
Primary request: 只生成下方 Specific subject 指定的一个操作图标。图标必须在24×24逻辑尺寸下清楚，不能依赖文字解释，也不能包含按钮底板
Style/medium: 中国修仙题材2D pixel art，清晰硬边像素簇，无抗锯齿；低饱和黛青、旧青铜、米白硬高光和少量功能色，最外侧使用稳定的深黛褐轮廓
Composition/framing: 正方形画布，图标主体位于中央，最终逻辑显示尺寸严格为24×24，交付PNG严格为72×72像素@3x；主体占画布约70%–82%，四周保留均匀透明安全边，缩小后不糊成一团
State constraints: 只制作普通态主体；不包含选中、按下、禁用、冷却、次数不足、红点、锁或文字。状态由运行时按钮、Tint、通用锁和Label组合
Output constraints: 真实RGBA透明背景，只输出一个图标；主体外完全透明。不要#00FF00、绿色键色、白底、黑底、棋盘格、纯色展示底、投影展示板、完整按钮、页面截图、文字、数字、水印或Logo
Avoid: 现代App线性图标、平滑矢量、3D渲染、写实照片、西式徽章、霓虹、玻璃、金币、付费符号、广告角标、过度金色浮雕、柔光渐变
```

#### 6.5.1 扎营操作图标

```text
Specific subject: 文件名 icon_explore_camp.png。主体是一处紧凑的修仙远行休整符号：低矮防风布篷、收拢的行囊卷和一块打坐用的方形蒲团形成清楚三角剪影，旁边只有两三个离散暖色火星，不画完整篝火场景。它表示玩家主动扎营、使用有限休整次数并可运功疗伤；不要药瓶、回血十字、食物、床铺、现代帐篷、露营车、房屋或永久营地建筑，也不要把行营阵盘的具体装备图标画进来
```

#### 6.5.2 归营操作图标

```text
Specific subject: 文件名 icon_explore_return_to_camp.png。主体是一座简化的外营门楼/低檐营门，前方有一条由两段硬像素弧线构成、向门内收束的归途阵痕，配一个极小的中性白青阵光点。它表示执行归营动作，不表示某一张归营符物品；不要卷轴符纸、消耗品数量、城市天际线、现代Home图标、传送收费门、逃跑人物、循环刷新箭头或红色撤退警报
```

#### 6.5.3 队伍操作图标

```text
Specific subject: 文件名 icon_explore_party.png。主体恰好由四个简化修士肩头像组成，两前两后、大小接近，通过中央一枚低调旧铜阵印形成一个整体；四人轮廓不能变成单一主角加三个小跟班。它表示打开固定四人队伍摘要，不是地图当前位置标记；不要第五个人、候选空槽、加号招募、宠物、军队旗帜、现代联系人头像、聊天气泡或人物脸部细节
```

#### 6.5.4 背包操作图标

```text
Specific subject: 文件名 icon_explore_inventory.png。主体是一只修仙远行使用的深褐布质储物袋/背囊，束口绳、侧边短绑带和一枚无字旧铜小扣形成清楚轮廓，袋身微鼓但不露出具体战利品。它表示查看本次出征背包、负重与临时战利品；不要宝箱、金币袋、购物袋、礼品盒、现代双肩包、容量数字、红点、装备品质光、掉落物喷出或付费货币
```

### 6.6 三个扎营面板操作图标提示词

每次复制下面的共用提示词，再在 `Specific subject` 后追加 6.6.1 至 6.6.3 中的一段。三张图分别
生成，不要让模型自行生成三联按钮。

```text
Use case: stylized-concept
Asset type: 原创竖屏2D像素修仙游戏《昆吾禁地》的扎营面板操作图标；单个独立透明PNG，不是完整按钮或扎营场景
Input images: 图片4是项目现有小图标的像素密度、深色外轮廓、左上受光和留白参考；图片5是375×817扎营面板布局与三个操作按钮的尺寸参考。两张输入图都不是编辑目标，不复制其中的文字、按钮底板、占位图或具体物体
Primary request: 只生成下方 Specific subject 指定的一个操作图标。它必须依靠剪影表达操作意图，不能画成某个可拾取的具体物品
Style/medium: 中国修仙题材2D pixel art，清晰硬边像素簇，无抗锯齿；低饱和黛青、旧青铜、暖褐、米白硬高光与少量功能色，最外侧使用稳定的深黛褐轮廓
Composition/framing: 正方形画布，图标主体位于中央，最终逻辑显示尺寸严格为24×24，交付PNG严格为72×72像素@3x；主体占画布约72%–82%，四周保留均匀透明安全边，三个图标的视觉重量接近
State constraints: 只制作可用普通态主体；食物不足、本次疗伤已使用、按下、禁用、选中和加载由运行时按钮、Tint和Label表达，不复制状态PNG
Output constraints: 真实RGBA透明背景，只输出一个图标；主体外完全透明。不要#00FF00、绿色键色、白底、黑底、棋盘格、纯色展示底、投影展示板、按钮底板、页面截图、文字、数字、水印或Logo
Avoid: 现代App线性图标、平滑矢量、3D渲染、写实照片、西式露营符号、医疗红十字、绿色回血符号、现代餐具、震荡柔光、霓虹、厚重金属徽章
```

#### 6.6.1 弄点吃的/进食补给图标

```text
Specific subject: 文件名 icon_explore_camp_food.png。主体是一只紧凑的深褐木钵，钵内只露出谷粒、饼块和肉干三种不完整的抽象硬色块，一双细短竹箸斜放在钵沿，上方只有两段离散暖色食气像素。它表示在扎营时挑选野外食物并补充本次出征灵粮，重点是“进食操作”而不是一件食物掉落物。不要单独辟谷饼、单独肉块、物品品质框、数量、血量恢复十字、药碗、烹饪锅、西式刀叉、酒杯、餐馆招牌或大量柔边热气
```

#### 6.6.2 恢复体力/运功疗伤图标

```text
Specific subject: 文件名 icon_explore_camp_recover.png。主体是一名方向中性、盘膝端坐的简化修士剪影，双手在下腹结成稳定小印，胸腹间两枚米白硬光点由一段不闭合青白行气弧连接，背后只有一圈稀疏的深黛护体轮廓。它表示扎营时执行一次免费运功疗伤，只恢复存活修士生命，不消耗食物、药品或资源。不要医疗红十字、绿色回血十字、爱心、药瓶、丹药、床铺、睡眠符号、死者苏醒、魂灯、复活光柱、具体初始修士的脸或职业配饰
```

#### 6.6.3 继续探险图标

```text
Specific subject: 文件名 icon_explore_camp_continue.png。主体是一张已经卷起并用细绳系好的扎营蒲团/防潮布卷，其右上方依次排列两枚向远方缩小的简化草鞋足迹，足迹之间以一段米白石径硬像素连成开放前行轮廓。它表示收起休整物品、关闭扎营面板并继续当前地图探索。不要现代播放三角、粗大向右箭头、快进符号、跑步人、回营门楼、归营阵光、跨图山门、传送圆环、地图Pin、新地图编号或撤退警报
```

### 6.7 七个地图覆盖标记提示词

每次复制下面的共用提示词，再在 `Specific subject` 后追加 6.7.1 至 6.7.7 中的一段。地图标记
必须能同时压在 `map_01` 青灰山地和 `map_04` 黑石魔雾上，不与地形实体混成一体。

```text
Use case: stylized-concept
Asset type: 原创竖屏2D像素修仙游戏《昆吾禁地》的世界地图对象覆盖标记；单个透明PNG，不是地形Tile、物品图标或完整页面
Input images: 图片2是按第3、4节完成并由Tile重拼的map_01低噪点底图、48×48逻辑格与标记占位参考；图片3前期是明确标注的Figma深色低噪点对比测试板，map_04完成后必须替换为其真实Tile截图进行最终回归；两张图都不是GPT地图视觉母版。图片4是项目现有小图标的像素密度、硬边轮廓和受光参考。只继承尺寸、色盘、对比和像素颗粒，不复制输入图中的对象、文字或地图结构
Primary request: 只生成下方 Specific subject 指定的一个地图覆盖标记。标记必须像叠在世界对象坐标上方的可交互类别符号，不画成一块独立地形或完整场景
Style/medium: 中国修仙2D pixel art，硬边像素簇，无抗锯齿；使用深黛黑外轮廓、旧青铜中间层、米白硬高光和少量语义色。轮廓在灰度下也要区分，不能只靠红绿颜色
Composition/framing: 正方形画布，主体居中或底部中央落锚；除我方队伍和Boss标记外，最终逻辑显示尺寸严格为24×24，交付PNG严格为72×72像素@3x。主体占画布约72%–86%，外侧保留透明安全边，不压过相邻地图格主要交互区
State constraints: 只制作已发现普通态；可交互高亮、已处理、条件锁定、选中和危险等级由运行时显隐、Tint、外描边、通用锁和Label组合，不把这些状态烘焙进标记
Output constraints: 真实RGBA透明背景，只输出一个标记；主体外完全透明。不要#00FF00、绿色键色、白底、黑底、棋盘格、投影展示板、地图截图、文字、字母、数字、水印或Logo
Avoid: 现代地图Pin、Google Maps水滴定位针、感叹号气泡、任务问号、标准交通标志、指南针App、平滑矢量、3D、写实照片、柔光渐变、霓虹、厚重品质框
```

#### 6.7.1 我方队伍地图标记

```text
Specific subject: 文件名 marker_explore_party.png，逻辑尺寸改为32×32，交付改为96×96像素@3x。主体是一枚方向中性的四人小队阵形标记：恰好四个米白/青白短人形光点围绕中央旧铜掌印阵核，组成紧凑菱形；外侧以一圈不闭合的深黛硬边保证压在浅色和深色地形上都可见。它表示固定四人队伍当前占据的唯一地图格，不画四张脸、职业武器、第五个光点、宠物、主角箭头、现代定位Pin、阵营旗或大面积发光圆盘
```

#### 6.7.2 通用资源点标记

```text
Specific subject: 文件名 marker_explore_resource.png。主体是一处从地表纹脉向上凝结的可采集灵脉节点：底部是两条短促旧青铜/灰石纹脉，中间形成一枚小型浅青灵光结点，旁边只有一片非常简化的嫩芽形硬像素，组合成“地下资源正在汇聚”的轮廓。它必须能泛指灵木、玄铁、灵晶、庚精和剧情允许的普通采集点，不画具体资源堆、矿车、镐、宝箱、金币、成熟药草、品质光柱、奖励数量或已采空状态
```

#### 6.7.3 普通敌群标记

```text
Specific subject: 文件名 marker_explore_enemy.png。主体是一面破旧敌群警戒幡的简化剪影：短幡杆、向内收束的两枚兵刃缺口和中央一枚暗朱砂警戒阵点，底部有很小的地面接触影。它只表示普通敌对遭遇，不指定尸傀、石灵、木魅、邪修或妖兽类型；不要具体怪物头像、骷髅、西式恶魔角、血滴、Boss皇冠、精英金框、交叉刀剑徽章、感叹号或现代危险三角牌
```

#### 6.7.4 副本入口标记

```text
Specific subject: 文件名 marker_explore_dungeon.png。主体是一座向地图内部/地下延伸的低矮石门洞：上方是厚重封石拱口，内部为深黛暗面，三段短台阶向下收束，门内只有极少冷青阵纹硬高光。它表示进入当前地图内部的局部副本或资源副本，不是通往另一张大地图；不要开放山门、向远方延伸的道路、城市门、传送圆环、宝箱、怪物脸、锁、难度数字或现代地下城入口图标
```

#### 6.7.5 出生/进入落点标记

```text
Specific subject: 文件名 marker_explore_spawn.png。主体是一枚平铺在地面的安全到达阵印：中央为空的旧铜小方印，四周恰好四个米白短落脚点，外沿有两段不闭合青白阵痕，整体扁平、稳定、无垂直门体。它表示本次进入地图时的安全落点和可辨认返回方向，不是复活点、保存点、传送收费点或任务目标；不要墓碑、十字、生命图标、房屋、旗杆、篝火、人物、现代定位Pin、字母S或绿色安全圆
```

#### 6.7.6 前往其他地图的出口标记

```text
Specific subject: 文件名 marker_explore_map_exit.png。主体是两根分开的低矮山门石柱和一条从柱间继续向画布上方延伸的浅色石路，门内使用中性米白/青白天光，顶部轮廓保持开放。它表示前往另一张完整地图的跨图出口，与副本入口的“暗门洞＋向下台阶”形成强烈对比；不要黑暗洞穴、地下台阶、回城房屋、现代Exit文字、字母、道路交通箭头、传送圆环、锁、地图编号或红色撤退警报
```

#### 6.7.7 Boss 地图标记

```text
Specific subject: 文件名 marker_explore_boss.png，逻辑尺寸改为32×32，交付改为96×96像素@3x。主体是一枚遭内部强敌撑裂的厚重镇压阵印：宽阔的六角旧铜印牌形成稳定上重轮廓，中央一道竖向深黛裂口包含小型暗朱砂灵压核，印牌上沿恰好三枚向外崩开的短刺状阵角，底部只保留两段断裂锁环形硬像素。它表示地图中已发现的唯一关键强敌，必须依靠“宽印牌＋中央裂核＋三枚上部阵角”的剪影与普通敌群的窄幅警戒幡立即区分，在灰度和缩小后仍清楚。不画守门石灵、封坛尸将、四臂铸灵傀或银翅夜叉的具体脸和身体；不要骷髅、王冠、西式恶魔角、怪物头像、Boss字样、血滴、感叹号、精英金框、战力数字、红色实心圆盘或大面积柔光
```

### 6.8 本批生成顺序与验收

1. 先生成扎营、归营、队伍、背包四个操作图标，放入 Approved 操作按钮检查尺寸和风格。
2. 再生成弄点吃的、运功疗伤、继续探险三个扎营面板图标，并排放入图片5的实际按钮框检查操作语义与视觉重量。
3. 然后生成我方队伍、资源点、普通敌群和 Boss 四个高频地图标记，同时合成到 `map_01` 和
   `map_04`；普通敌群与 Boss 必须并排检查剪影层级。
4. 最后生成副本入口、出生点和跨图出口，三者放在同一地图截图中做灰度轮廓对照，但仍逐张生成。
5. 十四张图都通过单独查看与对应页面合成后，才可进入 Cocos 导入；不得用绿色键色母图占位通过。

验收必须满足：

- 扎营图标不会被误认为回血药、食物或永久营地；归营图标不会被误认为归营符物品。
- 进食补给图标是“木钵与食气”操作，不是某一件食物物品，也不暗示恢复生命。
- 运功疗伤图标是“盘膝行气”，不是用药或复活；继续探险图标是“收卷休整物与前行足迹”，不是归营或跨图。
- 队伍操作图标明确是固定四人；地图队伍标记在一个 48×48 逻辑格内清楚且不遮挡相邻对象。
- 资源点与背包/宝箱不同；普通敌群的窄幅警戒幡与 Boss 的宽阔裂印在剪影、占比和灰度下都能立即区分。
- 副本入口是向内/向下的暗门，跨图出口是向外延伸的开放山门，出生点是扁平地面阵印。
- 所有标记在 `map_01`、`map_04` 和灰度截图中均可识别，不只依赖红、绿或发光颜色。
- 未知区不会提前显示对象；锁定、已处理和高亮状态没有烘焙进普通态 PNG。
- 十四张 PNG 均为真实 Alpha，图形外没有绿色、白色、黑色、棋盘格或抠图残边。

### 6.9 其余探索素材组

| ID | 资产组 | 规格 |
|---|---|---|
| `EXPLORE-UI-001` | 灵粮、负重、休整、归营 HUD | Approved Figma 后按 `@3x` 切片 |
| `EXPLORE-ICON-001` | 采集、宝箱、NPC、传讯、剧情、捷径等后续对象图标 | `24×24 / 72×72 @3x` |
| `EXPLORE-ICON-002` | 力道/法力/神识/遁速/肉身/护体/定力检定 | 复用 11 分册七维图标 |
| `EXPLORE-STATE-001` | 未知、已发现、可交互、已处理、条件锁定 | 图形＋运行时 Tint/Mask |
| `EXPLORE-PATH-001` | 路径点、终点、阻挡、中断 | 源像素按 Tile 对齐 |
| `CAMP-ILL-001` | 野外扎营小场景 | `280×180 / 840×540 @3x` |
| `RETURN-ILL-001` | 归营阵光表现 | 序列帧，逻辑 `160×160` |

迷雾由运行时 Shader/Mask/Tile 状态实现，原则上不为每格状态导出三张整图；若需纹理，只交付可平铺
噪点与边缘，不烘焙地图对象。

## 7. 两座资源副本

### 7.1 回火矿窟

- 入口/副本卡图、回火炉、真假矿脉、运输阵/炉心、过热提示、矿傀/夺宝者、短机制首领。
- 至少一张专属战斗背景，主色与 map_03 一致但突出回火橙光和炉压，不画普通金矿洞。
- 三难度通过徽记/环境层级表达，不为每个难度复制整套无差别背景。

### 7.2 镇魂残响

- 入口/副本卡图、魂印、命灯/封魂节点、真假残响、记忆完整度、阵法投影首领。
- 至少一张专属战斗背景，表现封魂阵与残响投影，不画反复囚禁真实 NPC 灵魂。
- 三难度沿同一阵法逐层增强，区别不只换颜色。

两本都需：基础/中/高三难度徽记、首通标记、重复挑战标记、机制对象 normal/damaged/failed/success
状态和结算卡图。不得出现门票、体力、每日次数、扫荡或挑战词条图标。

## 8. 敌人、精英与 Boss

- 每个最终 `enemy_id` 至少交付世界棋子与静态战斗立绘；行动、受击和阵亡由 Tween/Tint 与
  Sprite Sheet 特效表达。
- 精英不能只在普通敌人头顶加金框；至少有轮廓、部件或武器差异。
- 四个主线 Boss 各需世界态、战斗多阶段变体、关键机制对象、技能特效、受击和阵亡/退场。
- 第 2、4 Boss 的魂魄与魂器由 14 分册交付，本分册只负责战斗掉落表现和首杀标记。
- 两座资源副本投影不能复用已超度 NPC 的完整肖像作为可刷敌人。

## 9. 最终输入与交付验收

### 9.1 制作输入清单

美术制作前必须提供：

1. 当前制作地图的 `master_annotated / layout_collision / gpt_clean_guide / marker_test` 四类 Figma 导出和 `375×817` 视窗；四图按第 5.4 节顺序逐图补齐，不要求在 `map_01` 试制前伪造其余三图；
2. 地图缩略图、迷雾、对象面板、扎营、归营与失败报告 Approved 稿；
3. 两座副本选择、三难度、战斗机制与结算 Approved 稿；
4. 每种敌人/Boss 的尺寸对照、锚点和战场占位截图；
5. 前景淡出、Tile 拼接、碰撞和动画状态示意。

### 9.2 总验收

- 四图在无文字时也能通过地形、建筑、色盘和地标区分。
- Tile 无裂缝、错位或线性过滤；前景淡出后路径和对象仍可读。
- 未知迷雾不泄露 Boss、宝箱、敌人和副本入口。
- 敌人/Boss 尺寸、锚点、朝向和碰撞与灰盒一致，动画不越出关键信息安全区。
- 两座副本世界解释、机制对象和奖励视觉互相区分，且没有扫荡/门票/日常语义。
- 最终导出表覆盖地图对象与敌人内容表中的每个 1.0 稳定 ID，无孤儿素材和缺图 ID。
