# 4. 营地顶部与底部 HUD

版本：1.0-art-brief.1  
日期：2026-08-06  
状态：美术制作唯一事实源分册\
上级索引：[《昆吾禁地》美术素材制作总清单](README.md)


## 4.1 第二批：顶部与底部 UI 素材范围

#### 4.1.1 顶部固定 HUD

顶部首批建议制作 10 张：

| 类型 | 素材        | 数量 | 375 基准逻辑尺寸 |          最终 PNG 尺寸 | 文件名                                |
| -- | --------- | -: | ---------: | -----------------: | ---------------------------------- |
| 头像 | 默认玩家占位头像  |  1 |    `50×50` | `150×150 px (@3x)` | `portrait_player_placeholder.png`  |
| 头像 | 头像装饰框     |  1 |    `50×50` | `150×150 px (@3x)` | `ui_camp_avatar_frame.png`         |
| 资源 | 五项资源共用格底板 |  1 |    `58×51` | `174×153 px (@3x)` | `ui_camp_resource_slot.png`        |
| 资源 | 灵粮图标      |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_resource_spirit_grain.png`   |
| 资源 | 灵木图标      |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_resource_spirit_wood.png`    |
| 资源 | 玄铁图标      |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_resource_dark_iron.png`      |
| 资源 | 灵晶图标      |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_resource_spirit_crystal.png` |
| 资源 | 庚精图标      |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_resource_geng_jing.png`      |
| 主线 | 主线任务提示底板  |  1 |   `250×25` |  `750×75 px (@3x)` | `ui_camp_main_task_panel.png`      |
| 主线 | 主线任务图标    |  1 |    `18×18` |   `54×54 px (@3x)` | `icon_camp_main_task.png`          |

说明：

- 顶部资源顺序固定为：灵粮、灵木、玄铁、灵晶、庚精。
- 五项资源使用同一张格底板，名称和数量由 Cocos Label 渲染。
- 玩家形象尚未确定时只制作可替换占位头像，不把其当作最终主角设定。
- 主线任务的目标文字不能画进底板。
- 主线任务底板可见尺寸为 `250×25`，应放在至少 `250×48` 的透明交互节点中；扩大的触控热区不烘焙进图片。

当前已生成 8 张：玩家占位头像、头像框、五种资源图标、主线任务图标。仍待生成
`ui_camp_resource_slot.png` 与 `ui_camp_main_task_panel.png`；在这两张完成前继续使用
Cocos 节点底色，不得为其他页面制作同功能替代图。

#### 4.1.2 底部固定 HUD

底部首批建议制作 11 张：

| 类型 | 素材         | 数量 | 375 基准逻辑尺寸 |          最终 PNG 尺寸 | 文件名                                  |
| -- | ---------- | -: | ---------: | -----------------: | ------------------------------------ |
| 按钮 | 通用系统按钮普通态  |  1 |    `40×40` | `120×120 px (@3x)` | `ui_camp_system_button_normal.png`   |
| 按钮 | 通用系统按钮按下态  |  1 |    `40×40` | `120×120 px (@3x)` | `ui_camp_system_button_pressed.png`  |
| 按钮 | 通用系统按钮禁用态  |  1 |    `40×40` | `120×120 px (@3x)` | `ui_camp_system_button_disabled.png` |
| 图标 | 设置         |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_camp_settings.png`             |
| 图标 | 成就         |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_camp_achievements.png`         |
| 图标 | 排行榜        |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_camp_leaderboard.png`          |
| 图标 | 邮件         |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_camp_mail.png`                 |
| 图标 | 日常进度       |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_camp_daily_progress.png`       |
| 状态 | 通用暂未开放封印遮罩 |  1 |    `40×40` | `120×120 px (@3x)` | `ui_camp_unavailable_seal.png`       |
| 货币 | 灵石余额底板     |  1 |   `112×48` | `336×144 px (@3x)` | `ui_camp_spirit_stone_panel.png`     |
| 货币 | 灵石图标       |  1 |    `24×24` |   `72×72 px (@3x)` | `icon_currency_spirit_stone.png`     |

说明：

- 五个系统入口从左到右固定为：设置、成就、排行榜、邮件、日常进度。
- 系统按钮可见图片为 `40×40`，放在至少 `48×48` 的透明交互节点中央；触控热区不烘焙进图片。
- 设置使用普通态；成就、排行榜、邮件、日常进度当前使用禁用态和通用封印遮罩。
- 灵石余额显示在右下角；数值由 Cocos Label 渲染。
- 顶部灵晶与底部灵石是两种独立资源，图标不得只换色或共用轮廓。
- 当前不制作宝石、充值、礼包、付费商店、广告或加号购买按钮。

当前已生成 6 张：设置、成就、排行榜、邮件、日常进度、灵石图标。仍待生成三张系统
按钮状态、通用封印遮罩与灵石余额底板；这些素材完成前使用 Cocos 节点/Tint，不得把
缺失状态误写成已经生成。

#### 4.1.3 可选的整体底板

若单独按钮和资源格放在场景上显得零散，可再补两张可选素材，不计入基础 21 张：

| 素材         | 375 基准逻辑尺寸 |           最终 PNG 尺寸 | 文件名                                | 用途               |
| ---------- | ---------: | ------------------: | ---------------------------------- | ---------------- |
| 顶部 HUD 总底板 |  `375×104` | `1125×312 px (@3x)` | `ui_camp_top_hud_panel.png`        | 统一头像、资源栏和主线区域的底色 |
| 底部工具栏总底板   |   `375×48` | `1125×144 px (@3x)` | `ui_camp_bottom_toolbar_panel.png` | 统一左侧按钮和右侧灵石区域的底色 |

这两张底板必须保持半透明、低对比和可拉伸，不能大面积遮住大厅背景。

## 4.2 生成 UI 需要提供给 GPT Image 的截图

#### 4.2.1 必须上传

每次制作顶部或底部 UI 时，建议同时上传：

1. `assets/refer/lobby_mid.jpg`
   - 只参考头像、五资源、主线提示、底部五图标和货币栏的信息层级。
   - 不参考原作的具体图标、银色边框、金币、宝石、加号按钮和付费入口。
2. `assets/bundles/camp/env_camp_panorama_bg.png`
   - 只用于匹配《昆吾禁地》当前营地的黛青、暖褐、青铜、暗金和灯火色盘。
   - 不把背景或建筑画入 UI 素材。
3. 当前项目的 HUD 灰盒运行截图，建议保存为：
   - `assets/refer/camp_hud_layout_375x817.png`
   - 截图应使用 `375×817` 可视窗口，完整显示顶部和底部灰盒、安全区以及中央背景。
   - 只用于确定本项目自身的占位尺寸、留白和触控区域，避免照搬参考游戏比例。

其中 `camp_hud_layout_375x817.png` 当前为待补截图。正式生成 UI 前，应先从 Cocos 预览中截取一张。

#### 4.2.2 辅助上传

- `assets/refer/lobby_left.jpg`
- `assets/refer/lobby_right.jpg`

这两张只用于说明：顶部 HUD 和底部 HUD 在大厅横滑到左右边界时保持固定，不跟随建筑移动。无需让 GPT Image 重新设计三套 HUD。

#### 4.2.3 参考图使用边界

- 三张《地下城堡 2》截图只用于信息层级、紧凑程度和固定 HUD 关系。
- 禁止复制其中的银色头像框、红色圆环资源框、齿轮造型、按钮花纹、金币、蓝宝石、礼包、红点和付费栏。
- 所有正式素材必须转换为《昆吾禁地》的中国修仙语义。
- GPT Image 每次只生成一类可拆分资产；不要一次生成完整游戏截图后再切图。

## 4.3 UI 共用输出要求与提示词

#### 4.3.1 输出要求

- 本项目的 UI 唯一设计基准和玩家可见窗口为 `375×817`。顶部和底部 HUD 固定在该视窗内，不随 2.8 倍大厅背景横滑。
- 下文每张素材同时标注两种尺寸：“逻辑尺寸”是放入 `375×817` UI 时的显示尺寸；“最终 PNG 尺寸”是实际交付文件尺寸，本批统一使用 `@3x`。
- 例如头像逻辑显示为 `50×50`，最终交付 PNG 为 `150×150 px (@3x)`；导入 Cocos 后必须保持 `50×50` 的 UITransform 显示尺寸，不能按原图像素显示成三倍大小。
- `1080×1920` 和 `360×640` 均不再用于本清单的 UI 尺寸推导。
- 每个资产单独输出透明 PNG，不能嵌入 Base64，也不能贴在展示板或棋盘格背景上。
- GPT Image 若不能直接输出提示词指定的精确尺寸，可先生成更大的高清母版，再离线等比缩放、裁切或补透明边，最终文件必须严格符合对应的 `@3x` PNG 尺寸。
- 横向底板可先生成宽幅大图，最终按指定尺寸交付，并在运行时配置九宫格边框。
- 图标主体居中，占透明画布约 70%–80%，同组图标的视觉体量一致。
- 底板中央必须保留干净区域，方便 Cocos 放置 Label。
- 普通、按下、禁用三态必须保持完全相同的外轮廓和透明边界。
- 运行时使用 Nearest 过滤；最终缩小时保证像素簇清晰。
- 不在图片中画名称、数字、倒计时、红点、锁形文字或“未开放”文字。
- 触控热区由 Cocos 节点保证至少 `48×48dp`，不需要把透明热区画成可见边框。

#### 4.3.2 所有 UI 共用提示词

把下面这段放在每条 UI 具体提示词前面：

```text
为原创竖屏2D像素修仙游戏《昆吾禁地》制作营地大厅UI资产。参考截图只用于理解顶部与底部固定HUD的信息层级和紧凑程度，不得复制其中的图标、边框、金币、宝石或付费按钮。

整体视觉与当前营地背景一致：低饱和黛青、暖褐、旧青铜、暗金、灰绿和少量朱砂；材质使用风化木、旧青铜、黑玉、白玉和符纹纸。2D pixel art，清晰像素簇，硬边缘，无抗锯齿，轮廓简洁，在375×817竖屏的小尺寸下仍清楚。中国修仙题材，古拙、克制、安静，不使用西方奇幻或现代科技语言。

只输出指定的单个UI资产，透明背景，主体居中，四周保留均匀透明空间。对应具体提示词会同时指定375×817基准下的逻辑显示尺寸和最终@3x PNG尺寸，必须严格遵守。不要展示手机界面、完整游戏截图、使用示意图、模型手持效果或素材拼版。
```

每条提示词末尾都加：

```text
最终交付PNG尺寸必须严格保持为对应具体提示词指定的@3x W×H px，不得因修改风格或负面项改变画布尺寸；导入游戏后的逻辑显示尺寸同样不得改变。不要任何文字、汉字、英文字母、数字、货币符号、红点、角标、水印、Logo、棋盘格背景或投影展示板；不要复制参考截图的银色边框、红色资源圆环、齿轮细节、金币、蓝宝石、礼包和加号购买按钮；不要平滑矢量、3D渲染、写实照片、油画笔触、霓虹科幻、欧美纹章、哥特花边或日式神社风格。
```

## 4.4 顶部 UI 提示词

#### 4.4.1 默认玩家占位头像

```text
制作一个可替换的玩家占位头像，只画胸像，不画UI边框。角色是一名身份尚未揭示的昆吾山年轻修士，深黛色朴素道袍，肩部有少量旧青铜护片，脸部处于柔和阴影中，只保留克制的中性轮廓，不设定明确性别、发色、门派徽记或剧情身份。使用暖黄侧光勾出面部和衣领，缩小后仍能看出是修士而不是怪物。在375×817 UI中的逻辑显示尺寸必须严格为50×50，最终交付透明PNG尺寸必须严格为150×150 px（@3x）；正方形画布，胸像居中。
```

文件：`portrait_player_placeholder.png`

> 玩家角色设定确认后再替换这张，占位头像不作为最终角色原画。

#### 4.4.2 头像装饰框

```text
制作大厅玩家头像的独立正方形装饰框。框体由深色木材、旧青铜包角和极少量暗红绳结构成，四角有简洁的昆吾封印几何纹。中央必须完全镂空透明，可叠放任意头像；边框宽度均匀，外轮廓方正稳重，缩小后仍清楚。在375×817 UI中的逻辑显示尺寸必须严格为50×50，最终交付透明PNG尺寸必须严格为150×150 px（@3x）。不要在框内画人物，不要做成银色欧式相框。
```

文件：`ui_camp_avatar_frame.png`

> 本图是当前已导入的头像专用框。资源、物品与修士小头像后续统一迁移到
> [通用方形图像槽](06_通用方形图像槽.md)；新槽位生成并完成多内容合成验收前，
> 当前 HUD 仍继续使用本文件，不提前替换或删除。

#### 4.4.3 通用资源格底板

```text
制作顶部五种资源共用的横向小型资源格底板。使用深色半透明黑玉内底、旧青铜细边和少量暗金铆点；左侧预留资源图标位置，右侧预留数字位置。外轮廓简洁、横向可拉伸、四角适合九宫格切分。在375×817 UI中的逻辑显示尺寸必须严格为58×51，最终交付透明PNG尺寸必须严格为174×153 px（@3x）。中央没有任何图标或数字，不要圆形红色外框。
```

文件：`ui_camp_resource_slot.png`

#### 4.4.4 五种顶部资源图标

五张图使用同一镜头、外轮廓体量和受光方向。先生成灵粮图标并确认，再把已确认图作为后续四张的风格参考。

共用追加提示词：

```text
制作单个资源图标，不含按钮底板和圆环。在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。主体占正方形透明画布约75%，使用左上方暖光和右下方冷色阴影。轮廓必须在小尺寸下可辨，不依赖文字或颜色名称。
```

| 资源 | 具体提示词                                                                                                                | 文件名                                |
| -- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| 灵粮 | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一小束饱满的金褐色灵谷穗与一枚温润谷粒，带极少量暖金灵气，强调粮食与补给，不做成面包或西式麦穗徽章。       | `icon_resource_spirit_grain.png`   |
| 灵木 | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。两段带年轮的深褐灵木与一枚灰绿色嫩芽，木纹清楚，强调木材与生长，不做成整棵树或普通叶子图标。           | `icon_resource_spirit_wood.png`    |
| 玄铁 | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一块不规则深黑银色原矿，断面有冷灰金属光泽和少量暗红矿脉，强调沉重的原生矿石，不做成成品钢锭。          | `icon_resource_dark_iron.png`      |
| 灵晶 | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一簇通透的青蓝色灵气晶体，内部有柔和青白光，轮廓轻盈清澈；不得与底部灵石共用外形。                | `icon_resource_spirit_crystal.png` |
| 庚精 | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一枚经过炼化的浅银暗金金属精粹，形似锐利短梭与小型锭块的结合，边缘规整、质感精炼；必须与玄铁的粗糙原矿明显不同。 | `icon_resource_geng_jing.png`      |

#### 4.4.5 主线任务提示底板

```text
制作大厅当前主线任务的一行横向提示底板。造型结合展开的窄幅旧符纸与深木托条，左侧预留任务图标位置，中间和右侧留出长条文字区。使用低对比暗褐底、旧青铜细边和少量朱砂收口，不能抢过营地建筑。在375×817 UI中的逻辑显示尺寸必须严格为250×25，最终交付透明PNG尺寸必须严格为750×75 px（@3x）。底板需横向可拉伸并适合九宫格切分，中央不要画卷轴纹理、文字或任务内容。
```

文件：`ui_camp_main_task_panel.png`

#### 4.4.6 主线任务图标

```text
制作大厅主线任务图标：一枚卷起一角的宗门任务玉简，外覆窄条朱砂符纸和一根暗红系绳，轮廓像任务凭证而不是邮件信封。在375×817 UI中的逻辑显示尺寸必须严格为18×18，最终交付透明PNG尺寸必须严格为54×54 px（@3x）。独立透明图标，不含按钮底板、文字或感叹号。
```

文件：`icon_camp_main_task.png`

## 4.5 底部 UI 提示词

#### 4.5.1 通用系统按钮普通态

```text
制作大厅底部系统入口的通用圆形按钮框普通态。外圈是旧青铜与深木双层结构，内底为低反光黑玉，四个方向各有一个极简昆吾阵纹卡口。中央完全留空透明或使用均匀深色内底，方便叠放不同系统图标。在375×817 UI中的逻辑显示尺寸必须严格为40×40，最终交付透明PNG尺寸必须严格为120×120 px（@3x）；按钮图片居中放入至少48×48的交互节点。外轮廓紧凑、对称、小尺寸清晰，不要复制参考图的银色圆框和齿轮结构。
```

文件：`ui_camp_system_button_normal.png`

#### 4.5.2 按下态与禁用态

生成普通态后，必须使用普通态图片进行编辑，不要重新自由生成，以保证轮廓一致。

按下态编辑提示词：

```text
把输入的通用系统按钮编辑为按下态。在375×817 UI中的逻辑显示尺寸必须严格为40×40，最终交付透明PNG尺寸必须严格为120×120 px（@3x）。保持画布、透明边界、圆形轮廓、材质和全部装饰位置完全不变；只让内底向下压入少量像素，边缘暗一些，下缘出现一圈克制暖光。不要新增图标、文字或特效。
```

文件：`ui_camp_system_button_pressed.png`

禁用态编辑提示词：

```text
把输入的通用系统按钮编辑为禁用态。在375×817 UI中的逻辑显示尺寸必须严格为40×40，最终交付透明PNG尺寸必须严格为120×120 px（@3x）。保持画布、透明边界、圆形轮廓和装饰位置完全不变；关闭暖光，整体转为低饱和灰褐和暗青铜，内底略暗，但边缘仍可辨。不要只降低透明度，不要添加锁、文字或封条，封印遮罩会作为另一张图片叠加。
```

文件：`ui_camp_system_button_disabled.png`

#### 4.5.3 五个系统入口图标

五张图标不含按钮外框，使用同一画布、同一旧青铜与灰白玉材质、同一受光方向。

共用追加提示词：

```text
制作单个底部系统入口图标，透明背景，不含按钮框。在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。主体占正方形画布约70%，轮廓粗细统一，小尺寸下依靠剪影即可识别。
```

| 入口   | 具体提示词                                                                                                         | 文件名                            |
| ---- | ------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| 设置   | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一枚小型青铜阵盘与三个可调节的玉石节点，具有“调整设置”的直觉，但不要现代齿轮、扳手或开关。    | `icon_camp_settings.png`       |
| 成就   | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一枚白玉功勋牌，中心是抽象山峰与放射灵光，具有“达成荣誉”的感觉，不要奖杯、五角星或欧美勋章。   | `icon_camp_achievements.png`   |
| 排行榜  | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。三枚由低到高排列的宗门名次玉牌与顶部小旗，表达顺序和名次，不出现数字、皇冠或文字榜单。       | `icon_camp_leaderboard.png`    |
| 邮件   | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一封用朱砂符纸封口的飞书信札，带一小片羽形灵光，必须像信件，不与主线任务玉简混淆。         | `icon_camp_mail.png`           |
| 日常进度 | 在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。一枚竹制日程筹牌，周围排列四个小刻度点，其中一个点亮，表达每日进度；不要日历数字、对勾文字或钟表。 | `icon_camp_daily_progress.png` |

#### 4.5.4 通用暂未开放封印遮罩

```text
制作可叠在禁用按钮上的通用封印遮罩。使用两条交叉的窄木封条和一枚小型朱砂封印结，整体呈克制的X形，能看出入口尚未开放，但仍能透过空隙识别底下图标。在375×817 UI中的逻辑显示尺寸必须严格为40×40，最终交付透明PNG尺寸必须严格为120×120 px（@3x）。独立透明背景，不含锁形图标、“未开放”文字或红点。
```

文件：`ui_camp_unavailable_seal.png`

#### 4.5.5 灵石余额底板

```text
制作大厅右下角灵石余额的横向小型底板。左侧预留灵石图标凹槽，右侧留出数字区域；使用深色半透明黑玉内底、旧青铜窄边和少量暗金端头。造型比顶部生产资源格略厚重，表现这是通用货币，但不能出现充值、购买或付费诱导。在375×817 UI中的逻辑显示尺寸必须严格为112×48，最终交付透明PNG尺寸必须严格为336×144 px（@3x）。横向可拉伸，适合九宫格切分，不含数字、加号和货币符号。
```

文件：`ui_camp_spirit_stone_panel.png`

#### 4.5.6 灵石图标

```text
制作底部通用货币“灵石”的独立图标。一枚经过打磨的暖黄褐色灵石令牌，外形接近厚实的八边玉石，中间有简洁的昆吾山纹和柔和暗金灵光。在375×817 UI中的逻辑显示尺寸必须严格为24×24，最终交付透明PNG尺寸必须严格为72×72 px（@3x）。轮廓稳重、价值感明确，但不能画成金币、铜钱、宝石购买按钮或带货币符号的硬币。必须与顶部青蓝色晶簇状“灵晶”在颜色、轮廓和材质上完全不同。
```

文件：`icon_currency_spirit_stone.png`

#### 4.5.7 可选整体底板提示词

顶部 HUD 总底板：

```text
制作顶部HUD使用的宽幅半透明总底板。使用极低对比的深黛青黑玉与细旧青铜边，只在左上和右上保留少量昆吾符纹角花；中央大面积透明或半透明，不能遮住营地天空。在375×817 UI中的逻辑显示尺寸必须严格为375×104，最终交付透明PNG尺寸必须严格为1125×312 px（@3x）。横向可拉伸，不包含头像框、资源图标、任务图标、文字或数字。
```

文件：`ui_camp_top_hud_panel.png`

底部工具栏总底板：

```text
制作底部工具栏使用的宽幅半透明总底板。使用深黛青黑玉、旧木和极细青铜上沿，左右端自然收口，中部保持透明，让五个系统按钮和右侧灵石栏分别叠加。在375×817 UI中的逻辑显示尺寸必须严格为375×48，最终交付透明PNG尺寸必须严格为1125×144 px（@3x）。不能包含按钮、图标、货币、文字、红点、宝石或购买入口。
```

文件：`ui_camp_bottom_toolbar_panel.png`

## 4.6 推荐制作顺序

1. 先截取 `camp_hud_layout_375x817.png`，确认顶部和底部实际留白。
2. 先生成头像框、资源格、任务底板、普通系统按钮和灵石底板，统一材质语言。
3. 生成灵粮图标并确认像素密度，再用它作为其余资源图标的风格参考。
4. 生成设置图标并确认线条粗细，再用它作为其余四个系统图标的风格参考。
5. 用普通按钮原图编辑出按下态和禁用态，禁止重新自由生成。
6. 最后生成封印遮罩，并在大厅灰盒截图上合成检查。
7. 所有高清母版保存到 `ArtSource/`；裁切、缩放和压缩后的 `@3x` 运行时 PNG 放入对应 Cocos Bundle，并由编辑器生成 `.meta`。
8. 导入后将各 Sprite 的 UITransform 设置为表格中的 375 基准逻辑尺寸，禁止直接按 `@3x` PNG 原始像素尺寸显示。

## 4.7 顶部与底部 UI 验收

1. 顶部是否按顺序显示灵粮、灵木、玄铁、灵晶、庚精五种互不混淆的图标？
2. 底部灵石是否与顶部灵晶在轮廓、颜色和材质上明显不同？
3. 设置、成就、排行榜、邮件和日常是否仅凭剪影就能区分？
4. 成就、排行榜、邮件、日常是否明确处于禁用/封印状态，同时仍能看出原图标？
5. 普通、按下、禁用三态是否保持完全一致的尺寸、轮廓和透明边界？
6. 所有底板是否保留干净文字区，并可做九宫格拉伸？
7. 是否没有把资源名称、数值、任务文案、倒计时或“未开放”文字烘焙进图片？
8. 是否完全隐藏宝石、充值、礼包、广告、加号购买按钮和其他付费入口？
9. 顶部与底部 HUD 在 `lobby_left`、`lobby_mid`、`lobby_right` 三种横滑位置中是否保持固定？
10. UI 叠在当前营地背景上是否具有足够对比，同时没有大面积遮挡天空和建筑？
11. 每张 PNG 的实际像素尺寸是否与表格中的 `@3x` 尺寸完全一致，UITransform 是否与逻辑尺寸完全一致？
12. 系统按钮和主线任务的可见图片是否分别置于至少 `48×48`、`250×48` 的交互节点内？
13. 图标缩小到 25% 后是否仍清楚，透明区域是否为真实 Alpha？
14. 是否没有直接复制参考截图中的图标、边框、按钮花纹和付费栏？
